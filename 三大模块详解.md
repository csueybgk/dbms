# 三大模块详解 —— 对照课程图片逐条实现

本文件把课程图片中要求的**三大模块**逐条拆到具体代码，每一条给出「图片要求 → 落地类 → 关键实现 → 运行演示」。配合 `README.md` 的总览，可直接用于讲解与答辩。

- ① SQL 编译器
- ② 存储系统
- ③ 数据库引擎
- （附）客户端与服务器端交互

> 运行演示命令都以 `.\run.ps1`（PowerShell）或 `java -cp target/dbms-1.0.0.jar` 给出；假设已 `mvn package`。

---

# ① SQL 编译器

一句话：**把 SQL 文本变成一串可执行的动作**。分四步：词法 → 语法 → 语义 → 执行计划。

## ①.1 词法分析：识别关键字 / 标识符 / 常量 / 运算符

**落地**：`compiler/token/TokenType.java`、`compiler/token/Token.java`、`compiler/Lexer.java`

`TokenType` 把需要的词法单元列出来（关键字、标识符、数字、字符串、运算符、括号/逗号/分号）：

```java
public enum TokenType {
    CREATE, TABLE, INSERT, INTO, VALUES, SELECT, FROM,
    WHERE, AND, OR, NOT, ORDER, BY, ASC, DESC, SHOW, TABLES,
    JOIN, ON, AS, INNER, LEFT, GROUP, INDEX, INDEXES,
    TRUE, FALSE, BEGIN, COMMIT, ROLLBACK,
    IDENTIFIER, NUMBER, STR_LIT, OPERATOR,
    LPAREN, RPAREN, COMMA, SEMICOLON, STAR, DOT, EOF;
}
```

`Lexer` 逐字符扫描，一次产出下一个 token。要点：

- **标识符/关键字**：以字母或 `_` 开头，后接字母数字下划线；先查关键字表（`TokenType.fromKeyword` 拿小写词反查枚举名），查不到就是标识符。加一个关键字只要往枚举里加一项，`Lexer` 不用改。
- **常量**：数字（整数→`Long`，浮点→`Double`）、单引号字符串（`STR_LIT`）、`TRUE/FALSE`。
- **运算符**：`= <> < > <= >=`（`!=` 归一化为 `<>`，且识别时采用**双字符优先**）。

```java
// 数字常量——注意不能用三元表达式
Object val;
if (isFloat) val = Double.valueOf(num);
else         val = Long.valueOf(num);
```

> ⚠️ 坑：`isFloat ? Double.valueOf(num) : Long.valueOf(num)` 会触发 Java 的「二元数值提升」，把整型也提升成 `double`，导致 `1` 被解析成 `1.0`。这里必须用 `if/else` 保留原始类型。`INT32/INT64` 在 `StorageEngine.cast` 里再统一规整。

## ①.2 语法分析：构建语法树，支持四类语句

**落地**：`compiler/ast/*`（`CreateStmt` / `InsertStmt` / `SelectStmt` / `ShowStmt` / `Cond`）、`compiler/Parser.java`

递归下降分析器，`Parser.parse()` 依据文法生成 AST。文法如下（在 `Parser` 类注释里也写了）：

```
stmt   := create | insert | select | show
create := CREATE TABLE IDENT '(' colDef (',' colDef)* ')'
        | CREATE INDEX IDENT ON IDENT '(' IDENT ')'
insert := INSERT INTO IDENT VALUES '(' value (',' value)* ')'
select := SELECT ['*' | IDENT (',' IDENT)*] FROM IDENT
          [WHERE cond] [ORDER BY IDENT [ASC|DESC]]
show   := SHOW TABLES | SHOW TABLE IDENT | SHOW INDEX[ES] [ON IDENT]
cond   := cmp (OR cmp)* ; cmp := cmp2 (AND cmp2)* ; cmp2 := IDENT op value
```

`WHERE` 条件递归解析成 `Cond` 树（`CMP` / `AND` / `OR`），执行引擎的 `Filter` 算子据此逐行判定：

```java
public class Cond {
    public enum Kind { CMP, AND, OR }
    public final Kind kind;
    public final String column;   // CMP
    public final String op;       // CMP: = <> < > <= >=
    public final Object value;    // CMP
    public final List<Cond> children; // AND/OR
}
```

## ①.3 语义分析：存在性 / 类型 / 列数检查

**落地**：`compiler/Analyzer.java`

以 AST + 系统目录为依据做**静态校验**（不落地、不改数据）。错误码以 `SE-` 开头：

| 检查项 | 对应语句 | 错误码 |
|--------|---------|--------|
| 表必须存在 | Insert/Select/Show | `TB-0001`（`Catalog.getTable` 抛） |
| 列必须存在 | Select 列 / WHERE 列 / ORDER BY 列 | `SE-0004` |
| 值个数 = 列数 | Insert | `SE-0003` |
| 每个值可 cast 到列类型 | Insert / WHERE 字面量 | `SE-0005` |
| 列名不重复、至少一列 | Create | `SE-0002` / `SE-0001` |
| 索引列必须存在、索引名不重复 | Create Index | `SE-0004` / `SE-0007` |

```java
private void analyzeInsert(InsertStmt ins) {
    Table table = catalog.getTable(ins.tableName);
    Schema schema = table.schema();
    if (ins.values.size() != schema.columnCount())
        throw new Error("SE-0003", "值个数与列数不一致: ...");
    for (int i = 0; i < schema.columnCount(); i++)
        StorageEngine.cast(schema, i, ins.values.get(i)); // SE-0005
}
```

## ①.4 执行计划生成：逻辑执行计划

**落地**：`compiler/Plan.java`、`compiler/PlanBuilder.java`

把 AST 转成**逻辑算子树**，交给执行引擎跑。`SELECT` 的树形如下（叶子在左、依次上推）：

```
Project(列)          ← 最终输出哪些列
   │
 Sort(col, asc|desc) ← 可选 ORDER BY（在投影前排，否则排序列被投影丢了）
   │
 Filter(cond)        ← 可选 WHERE 谓词（永远保留，见下）
   │
 IndexScan(table)    ← 该列有索引且 WHERE 的合取项能用上时（否则退回下一行）
 SeqScan(table)      ← 全表扫描
```

`DDL/DML` 则是**叶子动作算子**：`CreateTable(table,schema)`、`CreateIndex(name,table,column)`、`Insert(table,row)`；管理语句是 `ShowTables` / `ShowTable` / `ShowIndexes`。

```java
private Plan buildSelect(SelectStmt sel) {
    // 最简单的物理计划选择：命中索引走 IndexScan，否则全表扫
    Operator root = indexScanFor(table, sel.where);
    if (root == null) root = new SeqScan(se, table);
    if (sel.where != null)  root = new Filter(root, schema, sel.where);
    if (sel.orderBy != null) root = new Sort(root, orderIdx, sel.orderDesc);
    root = new Project(root, cols);
    return new Plan(root, names);
}
```

注意 **`Filter` 照旧叠在扫描之上**：索引只负责"少读页"，判定行还是交给 `Filter`，所以选错/选宽了都不会算错结果。详见 [（附）B+ 树索引与 IndexScan](#附b-树索引与-indexscan--图片底部高级功能)。

`PlanBuilderTest` 对树形做了断言：`select id from users where age > 24` → `Project(Filter(SeqScan))`；建索引后同一句变成 `Project(Filter(IndexScan))`，行结果完全相同。

---

# ② 存储系统

一句话：**用「页」把数据放到磁盘上，再用「缓存」减少磁盘 I/O，并对外暴露接口供数据库模块调用。**

## ②.1 页式存储：页分配 / 释放 / 读写

**落地**：`storage/Page.java`（槽位化页）、`storage/DiskManager.java`（磁盘读写）、`storage/PageManager.java`（页管理接口）

**一页固定 4096 字节**（`common/Consts.PAGE_SIZE`）。页布局是经典的**槽位化页（slotted page）**：

```
偏移 0 ......  页头(16B): magic | pageNo | tableId | recordCount | freeStart
              ↓ 记录区（从页头起往上长） 每格是一条记录的字节
        ↑ 槽位目录（从页尾起往下长） 每格 4B = [offset:2B][length:2B]
偏移 4095 ...  槽位 k 位于 PAGE_SIZE - (k+1)*4
```

`Page` 的核心方法 `addRecord(byte[])`：在记录区尾部腾出空间、写入、反向建档槽位：

```java
public int addRecord(byte[] data) {
    int slot = recordCount;
    int offset = freeStart - data.length;      // 记录区从 freeStart 往回收
    byte[] dest = ...
    writeShort(slotOffset(slot), (short) offset);
    writeShort(slotOffset(slot) + 2, (short) data.length);
    freeStart = (short) offset;
    recordCount++;
    return slot;
}
```

`DiskManager`：**一个数据库 = 一个目录，一个表 = 一个文件 `table-{tableId}.db`**；文件就是定长页数组，页 `pageNo` 位于 `pageNo*4096`。增删页即追加/截断文件。

```java
public void writePage(int tableId, int pageNo, byte[] data) {
    raf(tableId).seek((long) pageNo * Page.SIZE);
    raf(tableId).write(data);
}
```

`PageManager`：对上层提供「按号读页、分配新页、刷盘、释放尾页」的接口，并负责查缓存。

```java
public Page getPage(int tableId, int pageNo) {
    Page p = cache.get(key(tableId, pageNo));   // 命中缓存直接返回
    if (p != null) return p;
    byte[] raw = disk.readPage(tableId, pageNo); // 未命中则读盘
    if (raw == null) return null;
    Page pg = new Page(raw);
    cache.put(key(tableId, pageNo), pg);
    return pg;
}
```

## ②.2 缓存机制：LRU / FIFO、命中统计与日志

**落地**：`storage/Cache.java`（接口）、`storage/AbstractCache.java`（命中统计）、`storage/LruCache.java` / `storage/FifoCache.java`

`Cache` 是存储系统向上暴露的接口之一（满足图片"提供接口供数据库模块调用"）。两种策略都基于 `LinkedHashMap`：

- **LRU**：`accessOrder = true`，访问过的页移动到末尾，容量超了就淘汰最久未用的（`removeEldestEntry`）。
- **FIFO**：`accessOrder = false`，按插入顺序，先进先出。

命中统计放在基类，淘汰时打日志：

```java
public abstract class AbstractCache implements Cache {
    protected int hits, misses;
    protected void recordHit()  { hits++; }
    protected void recordMiss() { misses++; }
    public String stats() {
        return String.format("hits=%d misses=%d hitRate=%.1f%%", hits, misses, 100.0 * hits / (hits + misses));
    }
}
```

## ②.3 存储系统对外接口

`Cache` / `PageManager` 是接口和门面，**数据库引擎只依赖它们读写页**，不直接碰 `DiskManager`/`RandomAccessFile`。运行时可看到缓存日志：

```
[CACHE] evict LRU key=...    # 淘汰
CACHE[LRU capacity=16 hits=1 misses=0 hitRate=100.0%]
```

---

# ③ 数据库引擎

一句话：**在存储系统之上，把一张表抽象成「行」，并执行 SQL 算出的算子序列。**

## ③.1 执行引擎：CreateTable / Insert / SeqScan / Filter / Project

**落地**：`engine/exec/Executor.java`、`engine/exec/op/*`（`Operator` 抽象基类 + 五个基本算子 + `Sort`/`Join`/`Aggregate`/`IndexScan` + `Show*`）

每个算子继承 `Operator`，提供 `execute()` 返回 `List<Row>`，并持有 `child` 进行层叠：

```java
public abstract class Operator {
    protected final Operator child;
    public abstract List<Row> execute();
    public abstract String name();
    public Operator child() { return child; }   // 供测试/计划展示
}
```

- **CreateTable**（叶子）：`catalog.createTable(name, schema)`，登记系统目录。
- **Insert**（叶子）：`StorageEngine.insert(table, row)`，返回受影响行数。
- **SeqScan**（叶子/基座）：`se.scan(table)`，全表扫描。
- **IndexScan**（叶子/基座）：走 B+ 树取落在范围里的 RID 再回表（见最末一节）。
- **Filter**：对子管线每一行按 `Cond` 谓词判定，保留满足者。
- **Project**：每行取出 `SELECT` 指定的列（按源列下标），丢弃其余。

`Executor` 只是驱动算子树并包装成 `Result`：

```java
public Result execute(Plan plan) {
    List<Row> rows = plan.root.execute();
    return new Result(plan.columns, rows);
}
```

`Filter` 的谓词求值会先把 WHERE 字面量 `cast` 到列类型再比较（保证口径一致），数值统一按 `double` 比较：

```java
case CMP:
    int idx = schema.indexOf(c.column);
    Object lit = StorageEngine.cast(schema, idx, c.value);
    return Compare.apply(c.op, row.get(idx), lit);
```

> **NULL 的另一半在 `Compare` 里**：`apply`（谓词）与 `compare`（排序 / 索引定位 / `MIN`·`MAX`）对 NULL 的规则是**相反**的 —— 谓词侧遵循三值逻辑（与 NULL 的任何比较都是 UNKNOWN，`WHERE` 不保留该行），排序侧则必须给 NULL 一个**确定位置**（升序垫底、降序最前）。两条规则必须分开实现：共用一个的话，NULL 会走兜底的 `String.valueOf` 变成字符串 `"null"`，而 `'n'`(110) 大于所有数字字符，NULL 就被当成了"比任何数都大"。本项目 SQL 无法表达 NULL 字面量，NULL 只由 `LEFT JOIN` 的未匹配行合成（见《（附）多表联查与聚合》的 `Join.nulls`），所以这个坑只在 LEFT JOIN 之后才暴露。改动与 8 个回归用例见《项目理解手册》§5.4 / §10.2。

## ③.2 存储引擎：行与页映射、磁盘组织

**落地**：`engine/storage/StorageEngine.java`、`engine/table/FieldType.java`

**行↔页映射**：一行（记录）序列化成字节数组，放进某页的槽位；页满自动追加新页。读取则逐页、逐槽位反序列化。

```java
public int[] insert(Table table, Row row) {
    byte[] rec = rowToBytes(table.schema(), row);
    for (int pageNo = 0; pageNo < pm.pageCount(tableId); pageNo++) {
        Page page = pm.getPage(tableId, pageNo);
        if (page.freeSpace() >= rec.length + Page.SLOT_SIZE) {
            int slot = page.addRecord(rec);
            if (slot >= 0) { pm.persist(page); return new int[]{pageNo, slot}; }
        }
    }
    int newPageNo = pm.newPage(tableId);
    pm.getPage(tableId, newPageNo).addRecord(rec);
    pm.persist(...);
    return new int[]{newPageNo, 0};
}
```

**记录字节格式**（`rowToBytes`）：每列 `[4B 长度][字节]`，变长字符串天然支持任意长。`FieldType` 负责值与字节互转：

```java
public enum FieldType {
    INT32(1), INT64(2), FLOAT64(3), BOOL(4), STRING(5), DATETIME(6);
    // encode / decode 各类型
}
```

**磁盘组织**：`StorageEngine` 通过 `PageManager` → `DiskManager` 落到 `table-{id}.db`，文件即定长页数组。

## ③.3 系统目录：元数据作为特殊表

**落地**：`engine/table/Catalog.java`、`engine/table/Table.java`、`engine/table/Schema.java`

**系统目录本身就是三张表**，用**和普通表完全一样的 `StorageEngine`** 读写：

- `sys_tables` (table_id int32, name string)        —— 表名 → 表 id
- `sys_columns`(table_id int32, cid int32, name string, type int32) —— 表结构
- `sys_indexes`(table_id int32, name string, column_name string, cid int32) —— 索引元数据

用户表 id 从 10 开始（1/2/3 让给系统表）。建表即往这两张表插元数据行：

```java
public Table createTable(String name, Schema schema) {
    int tid = nextTableId();
    se.insert(sysTables, Row.of(tid, name));
    int cid = 0;
    for (Column c : schema.columns())
        se.insert(sysColumns, Row.of(tid, cid++, c.name(), (int) c.type().tag()));
    ...
}
```

于是 `show tables` / `show table users` / `show indexes` 就是**查这些系统目录表**，重启后再 `open` 时 `Catalog` 从 `sys_tables` 重载全部表结构（见 `Catalog.reload()`）。

索引也照这个套路：**元数据进 `sys_indexes`，内容从基表重建**。所以 `reload()` 不只是重载表结构，还要重建全部内存索引 —— 这一步同时充当了"事务回滚撤销未提交索引"的钩子。

---

# （附）客户端与服务器端交互

**落地**：`protocol/*`（分帧 + 编解码）、`server/*`、`client/*`

## 通信协议

`protocol/Package` = 一个错误标志 + 一段 UTF-8 负载。`protocol/Packager` 用**长度前缀**分帧，避免粘包：

```
[4-byte payload_len][1-byte err_flag][payload 字节]
```

- 客户端 → 服务端：payload = SQL 文本；`err_flag = 0`
- 服务端 → 客户端：payload = 编码后的结果或错误文本；`err_flag = 0/1`

`Encoder`/`Decoder` 在结果与字节间互转（列名 + 行，值为文本）：

```
[int 列数] {[int 长度][utf8 列名]}*
[int 行数] {[int 长度][utf8 值]}*
```

## 服务端

`server/Server`：监听端口，每来一个连接 spawn 一个线程走 `ConnectionHandler`。`ConnectionHandler` 循环 `读SQL → Database.execute → 编码回包`：

```java
while (true) {
    Package pkg = Packager.read(in);
    String sql = new String(pkg.payload, UTF_8);
    try {
        Result r = session.execute(sql);
        Packager.write(out, new Package(false, Encoder.encodeResult(r)));
    } catch (Error e) {
        Packager.write(out, new Package(true, Encoder.encodeError(e)));
    }
}
```

## 客户端

`client/Shell`：交互 REPL，逐行读入，累积到「不在引号内的分号」才发送；支持 `\dt`/`\d <table>`/`\q`；`-e` 单条、`-f` 批量。`client/Renderer` 把 `Result` 渲染成表格：

```
+----+------+
| id | name |
+----+------+
| 2  | bob  |
+----+------+
1 row(s)
```

## 一次真实的往返

```bash
.\run.ps1 open data\db          # 终端1：服务端，监听 9999
.\run.ps1 client                # 终端2：客户端 REPL
dbms> select id, name from users where age > 24 order by score desc;
```

服务端日志会打印收到的 SQL 与缓存命中统计，客户端打印上表的查询结果 —— 这就是图片要求的「客户端与服务端交互」。

---

# （附）事务与并发 —— 图片底部高级功能①

一句话：**服务端已经是多线程的，为了让共享的 `Database` 在多个连接下安全、并支持回滚，加了「表级共享/排他锁」+「影子页事务」。**

## 为什么需要它

`server/Server.accept()` 每来一个连接 spawn 一个线程走 `ConnectionHandler`，所有连接共享**同一个** `Database`（同一个 `PageManager`/`Catalog`/`DiskManager`）。但：

- `LruCache/FifoCache`（内部 `LinkedHashMap`）、`Catalog.tables` 等不是线程安全的；
- 更关键，`StorageEngine.insert` 直接拿**共享缓存里的那个 `Page` 对象**调 `addRecord()` 并当场 `persist` 落盘——写进去就脏了，想"回滚"没有切入点。

于是做两件事：**① 把共享结构做成线程安全**；**② 用影子页工作集支持显式事务**。

## 表级共享/排他锁：`txn/LockManager`

表级 S/X 锁，同一张表上（✅ 可授予 / ❌ 等待）：

| 请求 \ 当前 | 空 | 多个 S | 单个 S | X |
|------------|----|--------|--------|---|
| SHARED(S)  | ✅ | ✅ | ✅ | ❌ 等 |
| EXCLUSIVE(X)| ✅ | ❌ 等 | ✅ 升级 | 他人 ❌ 等 / 自己 ✅ 重入 |

读-读不互斥；写-写、读-写互斥；唯一共享持有者可升级为排他（事务内先读后写）；同一事务重入直接成功。
`LockManager.acquire(txn, table, mode)` 阻塞到可授予或超时（5s，`Consts.LOCK_TIMEOUT_MS`），超时抛 `TX-0003`：

```java
// 唯一共享持有者可升级为排他；否则等所有读者退散
if (mode == X && !g.sTxns.isEmpty()) {
    if (g.sTxns.size() == 1 && g.sTxns.contains(txn)) return true;
    return false;
}
```

**死锁规避**：一次语句/一个事务涉及多张表时，`Session` 一律**按 tableId 升序** acquire——谁都在同一全局顺序上抢锁，不会互相等成环；再以超时兜底。

## 影子页事务：`txn/Txn`

`Txn` 挂在**当前线程**（每连接一个线程）上：`static ThreadLocal<Txn> CURRENT`。它持有一份"影子页工作集"，`PageManager` 执行时读 `Txn.current()` 决定路径：

- **无活动事务** → 与之前**完全一致**（直接读写共享缓存/磁盘）；裸调 `db.execute` 走的就是这条，字节级零改动，既有测试不受影响。
- **有活动事务** → `getPage` 返回工作集**深拷贝**；`persist`/`newPage` 只记进工作集，不动共享缓存/磁盘；`COMMIT` 才写盘，`ROLLBACK` 直接丢弃。

```java
public Page getPage(int tableId, int pageNo) {
    Txn t = Txn.current();
    if (t != null && t.active()) {
        Page inTxn = t.get(tableId, pageNo);          // 读己之写：工作集命中则直接用
        if (inTxn != null) return inTxn;
        Page shared = cache.get(key(tableId, pageNo));
        byte[] raw = shared != null ? shared.data() : disk.readPage(tableId, pageNo);
        if (raw == null) return null;
        // ⚠️ 关键：必须深拷贝。Page(byte[]) 不拷贝底层数组，浅拷贝会改到共享缓存里的页。
        Page copy = new Page(Arrays.copyOf(raw, Page.SIZE));
        t.put(tableId, pageNo, copy);                  // 只进工作集，不进共享缓存
        return copy;
    }
    // ...原路径：cache.get -> disk.read -> new Page(raw) -> cache.put
}
```

`COMMIT` 把整个工作集写盘——`DiskManager.writePage` 在超出 EOF 处写会自动扩展文件，所以事务新增的页即使乱序落盘也对；`ROLLBACK` 只清空工作集（从未碰共享缓存/磁盘，天然干净）。

## 编排中枢：`db/Session`

**事务控制语句由 `Session` 直接处理，不生成执行计划**（`PlanBuilder` 遇到 `TxnStmt` 会报 `PL-0002`）；数据语句由它加锁后再交 `Database.executeStmt`：

```java
public Result execute(String sql) {
    Stmt stmt = db.parse(sql);
    if (stmt instanceof TxnStmt) return handleTx((TxnStmt) stmt);   // BEGIN/COMMIT/ROLLBACK
    return runData(stmt);
}
```

- `handleTx`：`BEGIN` 设 `Txn.setCurrent(new Txn(locks.nextId()))`；`COMMIT` 调 `pm.commitTxn(t)` 写盘并 `locks.releaseTxn(t.id())`；`ROLLBACK` 先 `Txn.clearCurrent()`、再 `pm.rollbackTxn(t)` 丢弃工作集、`catalog.reload()` 撤销未提交建表（注意**先脱离事务再 reload**，否则读到的是影子页而非磁盘基态）。
- `runData`：**显式事务**内 → 加锁跨语句持有到 `COMMIT/ROLLBACK`；**自动提交** → 短命持锁执行后立刻放锁（`finally` 里 `releaseTxn`），行为与无事务一致。

**每条语句需要哪些锁**（`lockRequest`，按 tableId 升序）：

| 语句 | 锁模式 | 锁的表 |
|------|--------|--------|
| select ... from T | S | sys_tables, T |
| show tables | S | sys_tables |
| show table T | S | sys_tables, T |
| insert into T | X | sys_tables, T |
| create table T | X | sys_tables, sys_columns |

对 `sys_tables` 统一加 S 是廉价防御：串行化对目录页的读写，避免并发 DDL 破坏目录一致性。

## 三种隔离来源

1. **读己之写**：事务内 `getPage` 读的是工作集副本，`SeqScan` 能看到自己刚 `insert` 的行。
2. **不脏读**：写事务对表拿 X 锁并**持有到提交**，其他会话对该表读写都被挡，看不到未提交数据。
3. **回滚干净**：工作集从不落盘；事务内建表用 `catalog.reload()` 从元数据重建，抵消未提交表。

## 相关错误码

| 码 | 含义 |
|----|------|
| TX-0001 | 没有活动事务就 COMMIT/ROLLBACK |
| TX-0002 | 已经在事务中又 BEGIN |
| TX-0003 | 等锁超时（死锁兜底） |
| TX-0004 | 事务内不允许 freeLastPage |

## 运行演示

```sql
dbms> begin;
dbms> insert into users values (4, 'dave', 35, 99);
dbms> select * from users;      -- 事务内：能看到 dave（读己之写）
dbms> rollback;
dbms> select * from users;      -- 回滚后：dave 没了
```

提交则是 `begin; insert(4); commit;` → `select` 可见，且 `run.ps1 open` 重启后仍在。

并发（开第二个客户端）：终端 A `begin; insert(4);`（已拿写锁未提交）→ 终端 B `select * from users` 应**卡住**，A `commit;` 后 B 才返回并看到 dave。

## 测试

| 测试类 | 数量 | 覆盖 |
|--------|-----|------|
| `txn/LockManagerTest` | 6 | S 共存、S/X 互斥与超时、阻塞后释放、重入升级、他人读不可升级、跨表释放 |
| `db/TxnTest` | 7 | 回滚无痕、提交后重启仍在、读己之写、建表回滚、重复 BEGIN、无事务 COMMIT、双线程隔离 |
| `server/ConcurrencyTest` | 2 | 真实双连接：写锁阻塞读、并发 insert 不丢更新 |

> 既有的 `DatabaseTest` 走裸 `db.execute`（无 Session、无事务上下文）→ 原路径零改动，50 个旧用例全绿；服务器/客户端路径走 `Session.execute` + 自动提交短命事务，单连接串行无竞争，行为等价。

---

# （附）崩溃恢复（WAL 预写日志）—— 事务的持久性与原子性补全

上面的事务靠**影子页**做到"回滚干净、提交落盘"，但还有一个漏洞：`commit` 是把工作集**逐页**写进数据文件的，若进程在写页**中途崩溃**（断电/`kill`），磁盘上是**撕裂态**（有的页新、有的页旧），重启后无法判断该事务到底算不算提交成功，甚至丢数据。用 **WAL（预写日志）** 补上。

## 提交协议（先写日志，再落页）

`PageManager.commitTxn(Txn)` 现在两步走：

```java
public void commitTxn(Txn t) {
    if (t == null) return;
    // 先写日志并 fsync：此刻提交已持久且原子（有完整 COMMIT 标记）
    log.writeCommit(t.id(), t.pages());
    for (Map.Entry<Long, Page> e : t.pages().entrySet()) {
        Page pg = e.getValue();
        disk.writePage(pg.tableId(), pg.pageNo(), pg.data());   // 之后再落页
        cache.put(e.getKey(), pg);
    }
    t.reset();
}
```

`storage/Log`（`wal.log`）记录格式（大端、长度前缀定界，天然容忍写日志时崩溃留下的撕裂尾部）：

```
[4-byte recordLen][1-byte type][8-byte txnId]
  type=0 BEGIN   —— 无更多载荷
  type=1 PUT     —— [4-byte tableId][4-byte pageNo][PAGE_SIZE 字节后像]
  type=2 COMMIT  —— 无更多载荷
```

## 崩溃恢复（`PageManager` 构造时执行）

对外服务前调用 `Log.recover(DiskManager)`：

1. 逐条解析 WAL 到首个**不完整(撕裂)记录**即停；
2. 收集有 `COMMIT` 标记的 txnId，**只重放这些事务的 PUT**（`disk.writePage(表,页号,后像)`）——没有 `COMMIT` 标记（未提交）的事务被**自然丢弃**；
3. `disk.force()`（fsync 数据文件）后清空 WAL。

放在 `PageManager` 构造里，是因为 `AnalyzerTest/PlanBuilderTest/StorageTest/CatalogTest` 都直接 `new PageManager(new DiskManager(dir), ...)`，恢复要覆盖这些路径。

## 关键取舍

- **redo-only，不用 undo**：未提交事务的影子页从不落盘，崩溃即天然回滚，无需 undo 日志。
- **后像整页写**：日志直接存 4096 字节页后像，恢复按页覆盖，简单且幂等，不解析页内槽位。
- **WAL 只覆盖事务**：自动提交单语句仍走"直接写盘"路径（其原子性靠单语句页写；作为扩展方向可改为每语句 WAL）。

## 运行演示

```sql
dbms> begin;
dbms> insert into users values (5, 'eve', 27, 88.0);
dbms> commit;            -- 先写 WAL 再落页；此刻直接结束服务端进程（模拟崩溃）
-- 重启（run.ps1 open data\db）：eve 仍在（已提交事务被重放）；
-- 若在途中未 COMMIT 就崩溃，重启后该行消失（未提交事务被丢弃）。
```

## 测试

| 测试类 | 数量 | 覆盖 |
|--------|-----|------|
| `storage/LogTest` | 4 | 已提交事务重放(数据文件没写入也补回)、未提交丢弃、撕裂尾部容错、重放幂等 |
| `db/CrashRecoveryTest` | 2 | 崩溃重开：已提交事务仍在、未提交事务消失 |

---

# （附）多表联查与聚合 —— 图片底部高级功能②

把「查询」从单表扫描提升到多表连接 + 聚集计算，触及编译器与执行引擎两处。

## 语法

```sql
select <列 | 聚合(列)>, ... from t1 [AS a]
       [INNER|LEFT] JOIN t2 [AS b] ON a.col = b.col   -- 可多表左深连接
       [where ...] [group by 列, ...] [order by 列|聚合输出名 asc|desc];
聚合函数: count(*) count(col) sum(col) avg(col) min(col) max(col)
```

例：

```sql
select u.name, u.dept, o.amount
  from users u left join orders o on u.id = o.uid
  where u.dept = 'mkt' order by o.amount desc;

select u.dept, count(*), avg(o.amount)
  from users u join orders o on u.id = o.uid
  group by u.dept order by count(*) desc;
```

## 关键设计

**① `Cond` 扩展列-列比较。** 原 `Cond` 只能表达 `column op 字面量`，而 JOIN 的 `ON` 是 `左.列 op 右.列`。于是给 `Cond` 增加三个可空字段 `qualifier / column2 / qualifier2` 与工厂 `cmp2(q1,col1,op,q2,col2)`；`column2 != null` 即列-列比较。`CondEval` 统一了 `Filter`/`Join` 的求值：列-列直接取两列值参与 `Compare.apply`，列-字面量则把字面量 `StorageEngine.cast` 到左列类型再比。

**② `CombinedSchema` 解决列限定与歧义。** 多表拼接后，同一列名可能来自多张表。`CombinedSchema` 按 FROM/JOIN 顺序拼接所有列，并记录每列所属的限定名（别名或表名）；`resolve(限定符,列名)` 解析限定名，裸名须全局唯一，否则抛 `SE-0004`（`列名有歧义`）。其 `toSchema()` 用唯一名（`限定.列`）生成 Schema，配合按位置取类型的 `StorageEngine.cast(Schema, colIndex, v)` 安全地做类型规整。

**③ 算子。** `op/Join` 对左行 × 右行做嵌套循环，满足 `ON` 就拼接一行（左列在前、右列在后）；无 `ON` 即笛卡尔积；`LEFT JOIN` 右表无匹配时仍产出一行、右列置 `NULL`。`op/Aggregate` 按分组键分组，每组产出一行：`COUNT→Long`，`SUM`（参数列整型→Long，浮点→Double），`AVG→Double`，`MIN/MAX→原值`；`COUNT(*)` 计行数，其余聚合忽略 `NULL`。

**④ `isRich()` 双管线。** `SelectStmt.isRich()` 为真（多表 / 别名 / 限定列 / 聚合 / GROUP BY / JOIN ON）时走新管线 `SeqScan → [Join×N] → [Filter(WHERE)] → [Aggregate|Project] → [Sort(ORDER BY)]`；为假则走原有单表路径，**使既有三模块行为与测试零改动**（语义/计划/数据库测试 65 用例全部保留）。

## 相关错误码

| 错误码 | 位置 | 含义 |
|--------|------|------|
| `SE-0004` | Analyzer / CombinedSchema | 列不存在（含 ON/WHERE/ORDER 里的列）、或裸列名有歧义 |
| `SE-0006` | Analyzer / PlanBuilder | 有聚合/分组时，非聚合列未出现在 GROUP BY |

## 测试

| 测试类 | 数量 | 覆盖 |
|--------|-----|------|
| `ParserTest` | +7 | JOIN/别名/限定列、LEFT/CROSS JOIN、聚合函数、GROUP BY、聚合别名、限定 ORDER 的解析 |
| `AnalyzerTest` | +5 | JOIN 合法、ON 未知列、裸列歧义、非聚合列未分组、ORDER 未知列 |
| `PlanBuilderTest` | +3 | `Project(Join(SeqScan,SeqScan))` 形状、`Aggregate` 根、`Sort(Aggregate)` |
| `db/JoinAggTest` | 7 | 端到端：INNER/LEFT JOIN（含 NULL 保留）、COUNT/SUM/AVG/MIN/MAX、GROUP BY、聚合排序、JOIN 上聚合 |

---

# （附）B+ 树索引与 IndexScan —— 图片底部高级功能③

把「查询」从"只会全表扫"提升到"能按范围取行"。触及**存储/执行/编译器**三处：`engine/index/*` 提供结构，`op/IndexScan` 提供算子，`PlanBuilder` 决定选不选。

## 语法

```sql
create index <索引名> on <表>(<列>);     -- 单列索引
show indexes [on <表>];                 -- 查看已建索引（不写 ON 即全部）
```

## 为什么要 B+ 树

`SeqScan` 每查一次都要把整张表的每一页都读出来。若在 `age` 上有一棵**按键有序**的结构，`where age = 30` 就只需取键等于 30 的那几条，`age > 24` 只需从 24 往右取一段。B+ 树正是为此：**矮胖**（每层分叉多，树矮则 I/O 少）、**叶子有序且相连**（范围查沿叶链右扫，不必回根）、**内部节点只存键**（同样大小的节点能放下更多分叉）。

## 结构：`engine/index/BPlusTree.java`

阶取 5（每节点最多 4 键，`MAX_KEYS = 4`），小阶是为了小数据就能触发分裂、便于演示与测试。

```
                 [ 30 | 60 ]                       ← 内部节点：只存分隔键
              ／      ｜      ＼
     [10 20]  →  [30 40]  →  [60 70]              ← 叶子：存 (键,RID)，next 串成有序链
```

两条与课本一致的约束：

1. **所有数据只在叶子**，内部节点只存分隔键 → 任何查询都走到叶子，树高一致；
2. **叶子用 `next` 串成有序链表** → 范围查找沿链表右扫。

**键的约定**（决定了所有查找/插入的走向）：内部节点的 `keys[i]` 是其**右子树的下界**，即 `subtree(children[i]) ⊆ [keys[i-1], keys[i])`。相等键一律归右孩子，于是"第一个大于 key 的位置"这一个函数同时充当内部节点的**选孩子**规则与叶子节点的**插入点**规则：

```java
private static int upperBound(Node n, Object key) {
    int i = 0;
    while (i < n.keys.size() && Compare.compare(n.keys.get(i), key) <= 0) i++;
    return i;
}
```

**分裂**：节点键数超过 `MAX_KEYS` 就一分为二，中间键上提。叶子的分隔键**复制上提**（键仍留在右叶子，因为数据只能在叶子）；内部节点的分隔键**移出上提**（原位置不再保留）。根分裂时新建根，**树高 +1**：

```java
Split s = insertInto(root, key, rid);
if (s != null) {                       // 根分裂 → 树高 +1
    Node newRoot = new Node(false);
    newRoot.keys.add(s.key);
    newRoot.children.add(root);
    newRoot.children.add(s.right);
    root = newRoot;
}
```

**重复键**：索引列（`age` 这种）重复值很常见，所以叶子存的是**平行列表** `keys[]` 与 `rids[]`（而不是按键去重的 Map），`search` 返回全部相等键的 RID。**键比较复用 `Compare.compare`**，与 `ORDER BY`/`MIN`/`MAX` 完全一致，不会出现"排序一套序、索引另一套序"。

## 算子：`engine/exec/op/IndexScan.java`

```java
@Override public List<Row> execute() {
    List<Row> out = new ArrayList<>();
    for (int[] rid : tree.range(lo, loInc, hi, hiInc)) {   // 1. 树上取 RID（已按键有序）
        Row r = se.fetch(table, rid[0], rid[1]);           // 2. 回表取行
        if (r != null) out.add(r);                         //    失效 RID 跳过
    }
    return out;
}
```

`lo/hi` 为 `null` 表示该侧无界，`loInc/hiInc` 表示是否含端点。`StorageEngine.fetch` 走 `PageManager`，所以**事务内取到的是影子页里的深拷贝** —— 索引扫描同样能读到自己的未提交数据。

## 物理计划选择：`PlanBuilder.indexScanFor`

规则刻意保守，因为**正确性优先于加速**：

- 只在 `WHERE` 的**合取项**里挑（顶层 `CMP`，或 `AND` 树里的 `CMP`）；**不钻进 `OR`** —— OR 的任一支不成立就不能缩小范围，硬拆会漏行；
- 只认 `列 op 字面量` 且 `op ∈ {= < > <= >=}`；`<>` 和列-列比较用不上 B+ 树的范围性；
- 列上得有索引，比较值能 `cast` 到列类型（Analyzer 已校验过）。

命中后按 op 换算扫描区间：

| SQL 条件 | 索引区间 |
|---------|---------|
| `age = 30` | `[30, 30]` 闭 |
| `age > 24` | `(24, +∞)` |
| `age >= 24` | `[24, +∞)` |
| `age < 30` | `(-∞, 30)` |
| `age <= 30` | `(-∞, 30]` |

**没建索引时行为与改动前逐字节一致**（`indexScanFor` 返回 null → `SeqScan`），既有 93 个用例一个没动。

## 持久化与事务

| 关注点 | 做法 |
|--------|------|
| 元数据 | 进系统目录表 `sys_indexes`（普通表）→ 自然落盘、随事务提交/回滚 |
| 索引内容 | **内存派生数据**，不落盘；`Catalog.reload()` 时重扫基表重建 |
| 新插入的行 | `Insert` 算子插入成功后调 `IndexManager.onInsert` 把新 RID 记进该表所有索引（系统无 delete/update，`insert` 是唯一写路径，**一处钩子即可**） |
| 事务回滚 | `Session` 的 `ROLLBACK` 本来就会调 `catalog.reload()`；reload 重建索引 = 撤销未提交的 `create index` 与未提交的 insert |
| 并发 | 建索引对目标表加 X 锁（要扫全表，必须挡住并发写）；索引扫描读表加 S 锁 —— 由表级锁保证内存索引不会被别的会话边扫边改 |

**为什么把树放在内存**：索引是**派生数据**，完全可以从基表重建。这样索引与影子页事务 / WAL 天然解耦（提交后重建即生效，回滚后重建即撤销），不必让 B+ 树的每一页也走一遍提交协议。代价是重启要重扫一遍基表；把树的节点也落成页是明确的扩展方向。

## 相关错误码

| 错误码 | 位置 | 含义 |
|--------|------|------|
| `SE-0007` | Analyzer / Catalog | 索引名已存在（新增） |
| `SE-0004` | Analyzer / Catalog | 索引列不存在 |
| `TB-0001` | Catalog | 索引的目标表不存在 |

## 运行演示

```sql
dbms> create table users (id int32, name string, age int32, score float64);
dbms> insert into users values (1, 'alice', 23, 95.5);
dbms> insert into users values (2, 'bob', 30, 88.0);
dbms> insert into users values (3, 'carol', 22, 91.5);
dbms> select id, name from users where age > 22 order by id;   -- 2 行（全表扫）
dbms> create index idx_age on users(age);
dbms> show indexes;                                            -- idx_age / users / age
dbms> select id, name from users where age > 22 order by id;   -- 同样 2 行（走索引）
dbms> select id, name from users where age = 30;               -- 1 行
dbms> insert into users values (4, 'dave', 30, 99.0);
dbms> select id, name from users where age = 30 order by id;   -- 2 行：新插入的 dave 也进了索引
```

对比点：同一条 `where age > 22` 在**建索引前后行完全一致**，变的只是底层算子（`SeqScan` → `IndexScan`）。

## 测试

| 测试类 | 数量 | 覆盖 |
|--------|-----|------|
| `engine/index/BPlusTreeTest` | 7 | 100 条插入触发多次分裂且树高 ≥3、每个键都查得到；重复键返回全部 RID 且按插入序；范围开/闭区间、单侧无界、跨叶子仍有序；清空后可复用；空树查询安全 |
| `compiler/ParserTest` | +3 | `create index` 解析、`show index/indexes [on t]`、缺 `ON` 报语法错 |
| `compiler/AnalyzerTest` | +5 | 合法语句；未知列 `SE-0004`；未知表 `TB-0001`；重复索引名 `SE-0007` |
| `compiler/PlanBuilderTest` | +9 | `CreateIndex`/`ShowIndexes` 叶子；有索引→`Filter(IndexScan)`、区间端点与开闭正确；无索引/`OR`/`<>`/谓词在别列→仍是 `SeqScan` |
| `db/IndexTest` | 13 | 端到端：建索引前后**逐行一致**、等值（含重复键）、范围、与 Filter 并存、排序组合、insert 维护、字符串列、`show indexes`、重复名拒绝、重启重建、事务回滚撤销、事务提交保留 |
| `client/E2ETest` | +2 断言 | `DEMO.sql` 里的 `create index` / `show indexes` 走真实 Socket 跑通 |

---

# 如何向老师演示（精选）

1. **词法/语法**：故意输入 `create table users id`，报 `✗ [SY-0001] ...` —— 语法分析器拦截。
2. **语义**：`select * from ghost` 报 `✗ [TB-0001]`；`select nosuch from users` 报 `✗ [SE-0004]` —— 语义分析拦截。
3. **执行计划**：`PlanBuilder` 生成的算子树体现在 `select` 的视图上——加上 `where`/`order by` 后结果正确排序、只出指定列（对应 SeqScan→Filter→Sort→Project）。
4. **页式/缓存**：服务端日志的 `CACHE[... hitRate=...]` 即缓存命中统计；`show table users` 展示系统目录里存的元数据。
5. **落盘恢复**：建表插数据 → 杀服务端 → `run.ps1 open` → `select * from users` 数据仍在。
6. **客户端/服务端**:两个终端一 serve、一 client 实打实地用 Socket 交互。
7. **事务**：`begin; insert(4); select`（能看到 dave）→ `rollback;` 再看（没了）；开第二个 client，A `begin; insert(4);` 不提交，B `select` 被卡住，A `commit` 后 B 才返回。
8. **多表联查与聚合**：`select u.name, o.amount from users u join orders o on u.id=o.uid;` 出拼接表；`left join` 保留无订单的用户（右列 NULL）；`select u.dept, count(*), avg(o.amount) ... group by u.dept;` 出分组聚合；`order by count(*) desc` 按聚合结果排序。
9. **崩溃恢复**：`begin; insert(5,'eve'); commit;`（先写 WAL 再落页）→ 直接结束服务端进程（模拟崩溃）→ `run.ps1 open` 重启 → `eve` 仍在；再试在途中未 COMMIT 就崩，重启后该行消失。
10. **B+ 树索引**：`select id, name from users where age > 22 order by id;`（全表扫，2 行）→ `create index idx_age on users(age);` → `show indexes;` → 再跑同一句（同样 2 行，但底层换成了 `IndexScan`）→ `insert` 一条 age=30 再查（新行也在索引里）。再补一句 `create index idx_age on users(score);` 演示 `SE-0007` 重复索引名被拦。

> 高级功能中**事务与并发**（见 §（附）事务与并发）、**崩溃恢复 WAL**（见 §（附）崩溃恢复）、**多表联查与聚合**（见 §（附）多表联查与聚合）、**复杂索引 B+ 树**（见 §（附）B+ 树索引与 IndexScan）已实现；查询优化（代价估算/多列复合索引/索引下推）、`RIGHT/FULL JOIN`、`HAVING`、`DISTINCT`、子查询、多表达式 `GROUP BY`、标量函数、`DROP INDEX`、undo 日志与 checkpoint 仍属加分项，代码在 `PlanBuilder` / `Operator` / `CondEval` / `BPlusTree` 处预留了扩展位，可作加分讲解。
