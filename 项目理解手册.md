# DBMS 项目理解手册

> 这份文档的目的：让你**先自己把项目吃透**，然后再用自己的话讲给别人听。
> 它不是"讲稿"（一句一句照念的那种），而是"理解手册"——每个模块都回答三件事：
> **为什么要有它？它是怎么做的？代码在哪一行？**

---

## 序：这份手册怎么用

### 三条阅读路径

| 你的时间 | 怎么读 |
|---|---|
| 只有 20 分钟 | 读 **第 0 章**（全局地图）+ **第 11 章**（答辩要点）。知道"有哪些模块、每个模块一句话"，就够了 |
| 有 2 小时 | 按 **第 1→9 章** 顺序读一遍，重点看标注 `★ 关键设计` 的地方 |
| 要彻底吃透 | 每章读完对照源码看一遍，把每章的"自问自答"问题答一遍，再看第 10 章的测试自己跑一遍 |

### 三份文档的分工

| 文档 | 定位 | 什么时候看 |
|---|---|---|
| `README.md` | **对外说明书**：特性、构建、快速开始、测试统计 | 别人拿到项目，想跑起来 |
| `三大模块详解.md` | **对照课程图片的逐条回答**：图片上每一条要求 → 对应到哪段代码 | 老师对着图片提问时 |
| **本手册** | **内部理解手册**：每个设计决策的"为什么"，数据结构长什么样，数据怎么流动 | 你自己准备汇报时 |

三份文档不冲突：本手册讲"我想通了什么"，另两份讲"我做了什么、怎么跑"。

### 一个提醒

这个项目里**每一个设计都是有取舍的**。手册里凡出现 `★ 权衡` 标记的地方，都是"当时为什么不选另一条路"。汇报时老师最爱问的就是这个。把这些记住，比背代码有用。

---

# 第 0 章 全局地图

## 0.1 一句话：这个项目是什么

> 一个**用纯 Java 8 从零写的、零第三方依赖的关系型数据库**，支持 SQL 编译、页式存储、算子执行、客户端/服务器通信、事务与并发控制、WAL 崩溃恢复、多表联查与聚合、以及 B+ 树索引。

"从零"的意思是：

- 没有用 `java.sql` / JDBC 驱动
- 没有用任何数据库库（SQLite、H2、Derby 都没有）
- 没有用 Netty、Spring、Jackson 等框架
- 只有一个 `pom.xml`，依赖只有 JUnit（测试用）
- 磁盘格式、网络协议、SQL 语法、存储引擎——**全部是自己定义的**

这一点是汇报时的**最大加分项**，因为它意味着"每一层你都能讲到底层"。

## 0.2 分层架构图

从用户输入一条 SQL，到结果回到屏幕，数据经过下面这些层。**从下往上看是"存储怎么组织"，从上往下看是"一条 SQL 怎么走"**：

```
┌──────────────────────────────────────────────────────────────────┐
│  用户                                                             │
│  "select id, name from users where age > 22 order by id;"        │
└────────────────────────────┬─────────────────────────────────────┘
                             │  ① 键盘输入
┌────────────────────────────▼─────────────────────────────────────┐
│  客户端  client/                                                  │
│   Shell    —— 缓冲输入，遇到分号才发送；支持 \dt \d \q            │
│   Renderer —— 把结果渲染成 ASCII 表格 + "N row(s)"                │
└────────────────────────────┬─────────────────────────────────────┘
                             │  ② Socket（TCP，默认 9999）
┌────────────────────────────▼─────────────────────────────────────┐
│  协议  protocol/                                                  │
│   Package  —— {error标志, payload字节}                            │
│   Packager —— 帧格式：[4B 长度][1B 错误标志][payload]，防粘包       │
│   Encoder/Decoder —— 结果集的二进制序列化 / 反序列化               │
└────────────────────────────┬─────────────────────────────────────┘
                             │  ③ 网络传输
┌────────────────────────────▼─────────────────────────────────────┐
│  服务端  server/                                                  │
│   Server           —— ServerSocket.accept()，每个连接一个线程      │
│   ConnectionHandler—— 一个连接一个 Session，循环"读SQL→执行→回包"  │
└────────────────────────────┬─────────────────────────────────────┘
                             │  ④ 进入数据库
┌────────────────────────────▼─────────────────────────────────────┐
│  会话层  db/                                                      │
│   Session —— 事务控制(BEGIN/COMMIT/ROLLBACK) + 按语句加表级锁      │
│   Database—— 门面：把编译层和执行层串起来                          │
└────────────────────────────┬─────────────────────────────────────┘
                             │  ⑤
┌────────────────────────────▼─────────────────────────────────────┐
│  ① SQL 编译器  compiler/          ← 课程三大模块之一              │
│   Lexer     —— 文本 → Token 流                                    │
│   Parser    —— Token 流 → AST（递归下降）                          │
│   Analyzer  —— AST → 语义校验（存在性/类型/列数）                   │
│   PlanBuilder—— AST → 算子树（含索引选择）                          │
└────────────────────────────┬─────────────────────────────────────┘
                             │  ⑥ 算子树
┌────────────────────────────▼─────────────────────────────────────┐
│  ③ 数据库引擎  engine/            ← 课程三大模块之二              │
│   exec/Executor —— 驱动算子树                                     │
│   exec/op/*     —— 13 个算子（SeqScan/Filter/Project/Sort/...）    │
│   index/*       —— B+ 树 + 索引管理                                │
│   table/*       —— 表结构/系统目录/行                              │
│   storage/      —— 行↔页映射                                      │
└────────────────────────────┬─────────────────────────────────────┘
                             │  ⑦ 读写页
┌────────────────────────────▼─────────────────────────────────────┐
│  ② 存储系统  storage/             ← 课程三大模块之三              │
│   Page       —— 槽位化页（记录区 + 槽位目录）                      │
│   PageManager—— 页的分配/读/写/释放（事务感知）                    │
│   Cache      —— LRU / FIFO 缓存 + 命中率统计                       │
│   DiskManager—— 页 → 文件偏移                                        │
│   Log        —— WAL 预写日志（崩溃恢复）                            │
└────────────────────────────┬─────────────────────────────────────┘
                             │  ⑧
┌────────────────────────────▼─────────────────────────────────────┐
│  事务与并发  txn/                                                 │
│   LockManager —— 表级 S/X 锁、等待队列、超时                        │
│   Txn         —— ThreadLocal 影子页工作集                          │
└────────────────────────────┬─────────────────────────────────────┘
                             │  ⑨
┌────────────────────────────▼─────────────────────────────────────┐
│  磁盘                                                            │
│   data/db/table-1.db   ← sys_tables 的数据（也是一个表文件）        │
│   data/db/table-2.db   ← sys_columns                              │
│   data/db/table-3.db   ← sys_indexes                              │
│   data/db/table-10.db  ← 用户表 users                             │
│   data/db/wal.log      ← 预写日志                                 │
└──────────────────────────────────────────────────────────────────┘
```

## 0.3 目录结构与包职责

```
dbms/
├── README.md              对外说明书
├── 三大模块详解.md         对照课程图片的逐条实现说明
├── 项目理解手册.md          ← 你正在读的这份
├── DEMO.sql               一键演示脚本（11 条语句，覆盖全部特性）
├── pom.xml                零第三方运行时依赖；JUnit 仅测试
├── run.ps1 / run.bat / run.sh   一键启动脚本
└── src/
    ├── main/java/com/course/dbms/
    │   ├── common/        公共底座：常量、错误、日志
    │   │   ├── Consts.java      页大小 4096、默认端口 9999、锁超时 5000ms
    │   │   ├── Error.java       带错误码的运行时异常
    │   │   └── Log.java         极简日志（零依赖）
    │   ├── storage/       ② 存储系统
    │   │   ├── Page.java            槽位化页
    │   │   ├── PageManager.java     页管理器（事务感知）
    │   │   ├── DiskManager.java     磁盘/文件管理
    │   │   ├── Cache.java           缓存接口
    │   │   ├── AbstractCache.java   缓存基类（命中率统计）
    │   │   ├── LruCache.java        LRU 实现
    │   │   ├── FifoCache.java       FIFO 实现
    │   │   └── Log.java             WAL 预写日志
    │   ├── txn/           事务与并发
    │   │   ├── Lock.java            锁模式枚举 S/X
    │   │   ├── LockManager.java     表级锁管理器
    │   │   └── Txn.java             事务 + 影子页工作集
    │   ├── compiler/      ① SQL 编译器
    │   │   ├── Lexer.java           词法分析
    │   │   ├── Parser.java          递归下降语法分析
    │   │   ├── Analyzer.java        语义分析
    │   │   ├── PlanBuilder.java     执行计划生成（含索引选择）
    │   │   ├── Plan.java            计划：算子树根 + 输出列名
    │   │   ├── token/               Token / TokenType
    │   │   └── ast/                 语句 AST（13 个类）
    │   ├── engine/        ③ 数据库引擎
    │   │   ├── Result.java          查询结果（列名 + 行）
    │   │   ├── table/               表结构层
    │   │   │   ├── FieldType.java       6 种字段类型 + 编解码
    │   │   │   ├── Column.java          一列定义
    │   │   │   ├── Schema.java          有序列集合
    │   │   │   ├── Row.java             一行值
    │   │   │   ├── Table.java           表元信息
    │   │   │   ├── CombinedSchema.java  多表拼接后的逻辑列集合
    │   │   │   └── Catalog.java         系统目录（3 张系统表）
    │   │   ├── storage/StorageEngine.java   行↔页映射
    │   │   ├── index/               B+ 树索引
    │   │   │   ├── BPlusTree.java       内存 B+ 树
    │   │   │   ├── Index.java           索引元数据 + 树
    │   │   │   └── IndexManager.java    索引注册表 + 维护
    │   │   └── exec/
    │   │       ├── Executor.java        驱动程序
    │   │       └── op/                  13 个算子 + 2 个工具类
    │   ├── protocol/      通信协议
    │   │   ├── Package.java / Packager.java
    │   │   └── Encoder.java / Decoder.java
    │   ├── server/        服务端
    │   │   ├── Server.java / ConnectionHandler.java / ServerLauncher.java
    │   ├── client/        客户端
    │   │   ├── ClientLauncher.java / Shell.java / Renderer.java
    │   └── db/            顶层门面
    │       ├── Database.java
    │       └── Session.java
    └── test/java/com/course/dbms/    19 个测试类，138 个测试用例
```

## 0.4 一次 SQL 的完整生命周期

以 `select id, name from users where age > 22 order by id;` 为例，走一遍**完整链路**。这是全书的主线，后面每一章都是在这条线上展开一个环节。

**步骤 1：客户端缓冲**
`Shell.run()` 逐行读入，把输入累加到 `buf`。只有读到**不在引号内的分号**（`topLevelSemicolon`）才算一条完整语句，才发送。所以你可以跨行写 SQL。

**步骤 2：打包 + 发送**
`Shell.send(sql)` → `Packager.write(out, new Package(false, sql.getBytes(UTF_8)))`。
帧格式：`[4字节长度][1字节错误标志=0][SQL 的 UTF-8 字节]`。

**步骤 3：服务端收包**
`Server` 在启动时 `accept()` 到一个连接，就起一个新线程跑 `ConnectionHandler`。这个线程为连接建一个 `Session`，然后循环读包。

**步骤 4：进 Session，先看是不是事务控制语句**

```java
// db/Session.java:48
public Result execute(String sql) {
    Stmt stmt = db.parse(sql);
    if (stmt instanceof TxnStmt) return handleTx((TxnStmt) stmt);  // BEGIN/COMMIT/ROLLBACK
    return runData(stmt);
}
```

注意：**先解析，再判断类型**。因为"要不要加锁"取决于语句类型，所以必须先拿到 AST。

**步骤 5：加锁**
`runData` → `acquireLocks(stmt, lockId)`。
对 SELECT：`sys_tables` 加 S 锁 + 该表加 S 锁。加锁顺序**按 tableId 升序**（`req.sort(...)`），这是防死锁的关键。

**步骤 6：编译**

```java
// db/Database.java:58
public Result executeStmt(Stmt stmt) {
    analyzer.analyze(stmt);        // 语义校验
    Plan plan = planner.build(stmt); // 生成算子树
    return executor.execute(plan);   // 执行
}
```

这里 `stmt` 已经在步骤 4 解析过了——**没有重复解析**，这是设计好的：`Session` 拿到 AST 判断类型，`Database.executeStmt` 直接复用这个 AST。

**步骤 7：语义分析**（`Analyzer.analyzeSelect`）
检查：

- `users` 表存在吗？→ 不存在抛 `TB-0001`
- `id`、`name` 列存在吗？→ 不存在抛 `SE-0004`
- `where age > 22` 里的 `age` 存在吗？`22` 能转成 `age` 的类型（INT32）吗？→ 不能抛 `SE-0005`
- `order by id` 里的 `id` 存在吗？

**步骤 8：生成计划**（`PlanBuilder.buildSelect`）
产出这棵树（`toString()` 打印的形态）：

```
Project[0, 1](Sort(col=0, asc)(Filter(age > 22)(IndexScan(users))))
```

或者没有索引时：

```
Project[0, 1](Sort(col=0, asc)(Filter(age > 22)(SeqScan(users))))
```

**步骤 9：执行**（`Executor.execute`）
`plan.root.execute()` 自上而下递归拉取：

- `Project.execute()` 调 `child.execute()`（Sort）
- `Sort.execute()` 调 `child.execute()`（Filter），拿到行后排序
- `Filter.execute()` 调 `child.execute()`（SeqScan 或 IndexScan），逐行判定 `CondEval.eval`
- `SeqScan.execute()` → `StorageEngine.scan(table)` → `PageManager.getPage(...)` → 缓存命中返回 / 未命中读磁盘

**步骤 10：返回**
`Executor` 把 `List<Row>` 和 `plan.columns` 包成 `Result`，一路退回到 `ConnectionHandler`，`Encoder.encodeResult` 序列化，`Packager.write` 回包。

**步骤 11：客户端渲染**
`Shell.send` 收到包 → `Decoder.decodeResult` → `Renderer.render`：

```
+----+-------+
| id | name  |
+----+-------+
| 1  | alice |
| 2  | bob   |
| 3  | carol |
+----+-------+
3 row(s)
```

**步骤 12：放锁**
`runData` 的 `finally` 块里 `locks.releaseTxn(lockId)`。

> ★ 关键设计：**全物化（fully materialized）执行模型**
> 每个算子都返回 `List<Row>`，而不是"一次吐一行"的迭代器。真正的数据库（Volcano 模型）用 `open()/next()/close()` 流式拉取，好处是能早停（`limit 1` 不用扫全表）、内存占用低。
> 本项目选全物化的理由：**代码量小一半，调试直观**——`execute()` 就是一个普通方法，打断点能看到完整的中间结果。代价是内存占用与无早停，对课程规模的数据量完全够用。
> （注：`StorageEngine.iterate` 提供了流式接口，但算子层没用它——留作扩展点。）

## 0.5 代码规模与测试总览

| 项 | 数字 |
|---|---|
| 主源码文件 | 74 个 `.java`（另含 `package-info` 等） |
| 主源码行数 | 约 5222 行 |
| 测试文件 | 19 个测试类 |
| 测试用例 | **138 个，全部通过** |
| 运行时第三方依赖 | **0** |
| 最大文件 | `PlanBuilder.java`（345 行） |
| 第二大 | `Parser.java`（320 行） |
| 第三大 | `Analyzer.java`（263 行） |

## 0.6 第 0 章自问自答

**Q：这个项目最核心的一句话卖点是什么？**
A：零依赖、全自研、每一层都可视可讲。从 TCP 帧格式到页内的字节布局，全部是自己定的。

**Q：如果老师问"这跟直接用 SQLite 有什么区别"？**
A：SQLite 是个库，我们是客户端/服务器架构，且有真正的 SQL 编译管线（词法→语法→语义→计划→执行）和表级锁管理器。而且我们能展示"一条 SQL 从字节到字节的完整路径"。

**Q：13 个算子是哪 13 个？**
A：SeqScan、IndexScan、Filter、Project、Sort、Join、Aggregate、CreateTable、CreateIndex、Insert、ShowTables、ShowTable、ShowIndexes。

---

# 第 1 章 公共底座（common）

这三个类很小，但很重要——它们是"所有模块都说同一种语言"的基础。

## 1.1 Consts：集中管理常量

`common/Consts.java`，39 行。为什么要有它？避免"魔法数字散落各处"。

```java
public static final int PAGE_SIZE = 4096;             // 页大小（字节）
public static final int DEFAULT_PORT = 9999;          // 默认端口
public static final String DEFAULT_HOST = "127.0.0.1";
public static final String DB_FILE = "db.db";         // （历史遗留，当前按表分文件）
public static final String TABLE_FILE = "table";      // 每个表的数据文件前缀
public static final String WAL_FILE = "wal.log";      // 预写日志
public static final int META_PAGE = 0;                // 0 号页保留给全局状态
public static final short PAGE_MAGIC = (short) 0xDB30;// 页头魔术数
public static final byte PAGE_VERSION = 1;            // 页版本
public static final long LOCK_TIMEOUT_MS = 5000;      // 等锁上限
```

> ★ 权衡：`PAGE_SIZE = 4096` 是经典数据库页尺寸（大多数 OS 的页面大小、大多数磁盘的扇区倍数）。选大了：页少、随机 IO 少，但一次读浪费带宽；选小了：元数据开销占比高。4096 是几十年验证下来的平衡点，也便于讲解（正好一个 4KB 缓冲区）。

**为什么表文件按 `table-{tableId}.db` 分而不是所有表塞一个 `db.db`？**

`Consts.DB_FILE` 是早期设计的遗留。改成"一表一文件"后：

- 增删表不用改一个巨大的文件
- 单个表的页号是连续的 0,1,2,...（不用全局页号）
- 出问题好定位（哪个表坏了看哪个文件）

代价：跨表事务要写多个文件（WAL 正好解决这个问题——日志是全局单文件）。

## 1.2 Error：带错误码的异常

`common/Error.java`，37 行。

```java
public class Error extends RuntimeException {
    private final String code;
    public Error(String code, String message) { ... }
    public String code() { return code; }
    @Override public String toString() { return "✗ [" + code + "] " + getMessage(); }
}
```

**为什么继承 `RuntimeException` 而不是 `Exception`？**

因为这条错误要穿过 8 层调用栈（算子 → 执行器 → 计划 → 分析 → Session → 连接处理器）原样冒到客户端。如果继承 `Exception`，每层方法签名都要 `throws Error`，或者用 `try/catch` 包一层再重抛——代码会脏得没法看。`RuntimeException` 让它自动穿透，只有在 `ConnectionHandler` 这一个地方统一捕获。

**错误码前缀的含义**（类注释里写明了）：

| 前缀 | 模块 |
|---|---|
| `ST` | Storage 存储层 |
| `CS` | Cache 缓存层 |
| `TB` | TaBle 表 |
| `CT` | CaTalog 目录 |
| `LX` | LeXer 词法 |
| `SY` | SYntax 语法 |
| `SE` | SEmantics 语义 |
| `PL` | PLan 计划 |
| `EX` | EXecution 执行 |
| `TX` | Transaction 事务 |
| `SV` | SerVer 服务端 |

还有几个快捷构造方法（`Error.st()` / `Error.cs()` / `Error.tb()` / `Error.ct()`），避免写死错误码时遗漏。

## 1.3 Log：极简日志

`common/Log.java`，26 行。

```java
public enum Level { DEBUG, INFO, WARN, ERROR }
private static Level level = Level.INFO;

private static void log(Level l, String msg) {
    if (l.ordinal() < level.ordinal()) return;   // 级别过滤
    System.out.println(String.format("[%s] %s", l, msg));
}
```

用 `ordinal()` 比值——枚举声明顺序就是级别顺序（DEBUG=0 < INFO=1 < WARN=2 < ERROR=3），所以"l 级别低于当前阈值就丢弃"这一行就够。

**为什么不用 log4j / slf4j？** 零依赖是硬约束。而且课程项目里日志的作用主要是**演示**（让老师看到缓存命中统计、WAL 重放），输出到 stdout 正好。

**用法举例**：`AbstractCache.logStats()` 会打 `[INFO] CACHE[LRU capacity=64 hits=12 misses=3 hitRate=80.0%]`——汇报时这是个很好的可视化点。

## 1.4 第 1 章自问自答

**Q：为什么错误码要分模块前缀？**
A：便于快速定位是"哪一层"出的问题。`SE-0004` 一眼就知道是语义层（列不存在），`TB-0001` 是表不存在。测试里也大量用 `e.code().startsWith("SE")` 来断言错误层级。

**Q：`Error` 直接 `System.out.println(e.toString())` 会输出什么？**
A：`✗ [SE-0004] 列不存在: xxx（表 users）`。

---

# 第 2 章 存储系统（storage）

这是课程图片"② 存储系统"对应的完整实现。四个关键词：**页式存储、页分配/释放/读写、缓存机制、对外接口**。

## 2.1 为什么要有"页"这一层

磁盘的最小读写单位不是字节，而是**扇区/块**（通常 512B 或 4KB）。如果你每次只改一个字节就写一次磁盘，会有两个灾难：

1. **性能**：一次系统调用只传 1 字节，99.98% 的时间花在调用开销上
2. **原子性**：写 100 字节的过程中断电，磁盘上可能是"前 50 字节新、后 50 字节旧"的**撕裂态**

所以数据库的标准做法是：**以固定大小的"页"为最小 IO 单位**。改数据 = 把整页读进内存、在内存里改、再整页写回。

> ★ 关键认识：**页是数据库和磁盘之间的"货币"**。上层（表、索引、算子）只跟页打交道，不关心磁盘扇区。这一层抽象让后面的一切成为可能。

## 2.2 Page：槽位化页（slotted page）

文件：[`storage/Page.java`](src/main/java/com/course/dbms/storage/Page.java)，164 行。这是整个存储层最核心的数据结构。

### 为什么需要"槽位"

朴素做法：页就是 `[记录0][记录1][记录2]...` 紧密排列。问题：

- 怎么知道第 3 条记录从哪个字节开始？——只能从头遍历（O(n)）
- 记录被删除后留下空洞，新记录大小不一样就填不进去
- 变长字符串（`string` 类型）怎么办？

**槽位化页**解决这些问题：把页分成两部分——**记录数据从头部往后长，槽位目录从尾部往前长**，中间是空闲区。

### 页布局

```
页大小 PAGE_SIZE = 4096 字节，大端序（Big-Endian）

偏移 0                                          偏移 4095
┌──────────────────────────────────────────────────────┐
│                     页头（16 字节）                   │
│ 0-1   magic(short)     = 0xDB30  （"DB0"）            │
│ 2-5   pageNo(int)      本页页号                       │
│ 6-9   tableId(int)     所属表 id                      │
│ 10-13 recordCount(int) 当前记录（槽位）数量            │
│ 14-15 freeStart(short) 记录区末尾 = 空闲区起点         │
├──────────────────────────────────────────────────────┤
│                    ↓ 记录区（从前往后长）              │
│  记录0 [bytes] 记录1 [bytes] 记录2 [bytes] ...        │
│                                                       │
│              ← 空闲区（freeSpace）→                    │
│                                                       │
│                    ↑ 槽位目录（从后往前长）            │
│  ... 槽位1 [offset,length] 槽位0 [offset,length]      │
└──────────────────────────────────────────────────────┘
                                                    偏移 4095
```

每个槽位占 **4 字节**：`[2字节 offset][2字节 length]`。

槽位 i（对应记录 i）的**地址**是 `SIZE - (i+1) * SLOT_SIZE`——注意这跟直觉相反：**槽位 0 在页的最末尾，槽位 1 在它前面**。所以槽位目录是"倒着编号"的。

这样设计的好处：槽位下标是**稳定的**。记录 0 永远对应槽位 0（就在页尾），不需要因为"前面删了一条"而重排。

### 核心方法逐个讲

**`addRecord(byte[] rec)` —— 追加一条记录，返回槽位下标**

```java
// Page.java:93
public int addRecord(byte[] rec) {
    int count = recordCount();
    int start = freeStart();
    int needSlot = (count + 1) * SLOT_SIZE;
    if (start + rec.length + needSlot > SIZE) {
        return -1;                          // 空间不足，调用方应另开新页
    }
    System.arraycopy(rec, 0, data, start, rec.length);    // ① 记录写到记录区末尾
    int addr = slotAddr(count);                            // ② 槽位写到槽位区
    putShort(addr, (short) start);
    putShort(addr + 2, (short) rec.length);
    putInt(OFF_REC_COUNT, count + 1);                      // ③ 更新页头
    putShort(OFF_FREE_START, (short) (start + rec.length));
    return count;
}
```

三个细节值得注意：

1. **空间判断**是 `start + rec.length + needSlot > SIZE`——不仅要放记录本身，还要给**新槽位**留 4 字节。这是新手最容易漏的地方。
2. **返回 -1 而不是抛异常**——"页满了"是正常控制流（`StorageEngine.insert` 会去下一页），不是错误。
3. `System.arraycopy` 而不是循环赋值——底层是内存拷贝指令，快得多。

**`getRecord(int i)` —— 读回第 i 条记录**

```java
// Page.java:113
public byte[] getRecord(int i) {
    if (i < 0 || i >= recordCount()) return null;          // 越界
    int off = slotOffset(i);
    int len = slotLength(i);
    if (len <= 0 || off + len > HEADER_SIZE + SIZE) return null;   // 空槽位 / 损坏
    byte[] out = new byte[len];
    System.arraycopy(data, off, out, 0, len);
    return out;
}
```

`len <= 0` 的检查是为**删除**预留的——本项目没有 DELETE，但槽位化页天然支持"把槽位长度置 0 表示空槽"。

**`records()` —— 顺序遍历，跳过空槽位**

```java
// Page.java:124
public Iterable<byte[]> records() {
    return () -> new java.util.Iterator<byte[]>() {
        private int i = 0;
        @Override public boolean hasNext() {
            while (i < recordCount() && slotLength(i) <= 0) i++;   // 跳过空槽
            return i < recordCount();
        }
        @Override public byte[] next() {
            int idx = i;
            byte[] r = getRecord(idx);
            i++;
            return r;
        }
    };
}
```

返回 `Iterable<byte[]>` 而不是 `List<byte[]>`——避免一次性把整页的记录都拷进内存。这是项目里**唯一一处借用流式思想**的地方。

**`freeSpace()` —— 剩余可用字节**

```java
// Page.java:84
public int freeSpace() {
    int recEnd = freeStart();                          // 记录区末尾
    int slotBottom = SIZE - recordCount() * SLOT_SIZE; // 槽位区顶端
    return slotBottom - recEnd;                        // 中间的空隙
}
```

`StorageEngine.insert` 用它判断"这页塞得下吗"：`page.freeSpace() >= rec.length + Page.SLOT_SIZE`。

### 字节序：为什么手写而不是用 ByteBuffer

`getInt` / `putInt` / `getShort` / `putShort` 都是手写的位运算：

```java
// Page.java:149
private void putInt(int off, int v) {
    data[off]     = (byte) (v >> 24);   // 最高字节写在最前面 = 大端序
    data[off + 1] = (byte) (v >> 16);
    data[off + 2] = (byte) (v >> 8);
    data[off + 3] = (byte) v;
}
```

**为什么不用 `ByteBuffer`？** 两个理由：

1. `ByteBuffer.getInt()` 默认是**大端**（`BIG_ENDIAN`），但其实可以设 `order()`。这里手写是为了**显式**——看代码就知道字节顺序，不用去查 ByteBuffer 的默认行为。
2. 页内读写是**最热的路径**，手写位运算没有对象分配开销。（`FieldType.encode/decode` 里用了 ByteBuffer，因为那里是"整块值编码"，不是逐字节操作。）

**网络编程提醒**：`DataInputStream.readInt()` / `DataOutputStream.writeInt()` 也**都是大端**。所以本项目的磁盘格式和网络格式**天然一致**——这是一个刻意的选择，减少心智负担。

### ★ 权衡：为什么用 `short` 存 offset/length

`Page.SIZE = 4096`，最大偏移 4095，`short`（有符号 16 位，范围 -32768~32767）装得下。但如果把 `PAGE_SIZE` 改成 65536 以上，`short` 就会溢出——代码里所有 `getShort(...) & 0xFFFF` 的写法就是在**按无符号解释**，最多支持到 65535。

**单项记录长度上限**：`short` 存 length，所以单条记录最长 65535 字节。但一条记录实际受限于页大小——`addRecord` 的空间检查会挡住。

## 2.3 DiskManager：页 ←→ 文件偏移

文件：[`storage/DiskManager.java`](src/main/java/com/course/dbms/storage/DiskManager.java)，144 行。

### 磁盘组织

**一个数据库 = 一个目录。目录下每个表 = 一个文件 `table-{tableId}.db`。**

文件本身就是**定长页数组**：

```
table-10.db
┌──────────┬──────────┬──────────┬──────────┐
│  页 0    │  页 1    │  页 2    │  页 3    │  ← 每页 4096 字节
│ offset 0 │ offset   │ offset   │ offset   │
│          │  4096    │  8192    │  12288   │
└──────────┴──────────┴──────────┴──────────┘

页 pageNo 的字节偏移 = pageNo * PAGE_SIZE
当前页数 = 文件长度 / PAGE_SIZE
```

于是"分配新页"= 在文件末尾追加 4096 字节；"释放页"= 把文件截短 4096 字节。

### 核心方法

**`raf(int tableId)` —— 拿文件句柄（惰性）**

```java
// DiskManager.java:48
private synchronized RandomAccessFile raf(int tableId) {
    RandomAccessFile f = files.get(tableId);
    if (f != null) return f;                    // 已打开，直接返回
    File path = file(tableId);
    if (!path.exists()) path.createNewFile();   // createNewFile 不覆盖已有文件
    f = new RandomAccessFile(path, "rw");
    files.put(tableId, f);
    return f;
}
```

用 `RandomAccessFile` 而不是 `FileOutputStream`——因为需要 **`seek()` 随机定位**（页号 → 偏移）。`RandomAccessFile` 是唯一同时支持"随机读+随机写+查长度"的 Java 标准 API。

**`synchronized`**：服务端多个连接线程共享同一个 `DiskManager`，文件句柄表必须串行访问。

**`writePage` —— 覆盖写一页**

```java
// DiskManager.java:100
public synchronized void writePage(int tableId, int pageNo, byte[] data) {
    if (data.length != Page.SIZE) throw Error.st("page size mismatch on write");
    RandomAccessFile f = raf(tableId);
    f.seek((long) pageNo * Page.SIZE);
    f.write(data);
}
```

> ★ 关键点（注释里也写了）：**`RandomAccessFile` 在"超出 EOF 处写"会自动扩展文件**。
> 这意味着事务提交时即使**乱序**写页（先写页 5 再写页 2），只要最后写到的最大页号对，文件长度就是对的。
> 举例：文件只有 3 页（0,1,2），事务提交一个 5 页的工作集。先写页 5 → 文件自动填充 3、4 页为**零字节**并写到第 6 页末尾。那些零页是无效的（`Page(byte[])` 构造时会读 `tableId`/`pageNo` 为 0，`pageCount` 计算的是文件长度所以会把它们算进去）——但在本项目里，事务提交的工作集总是**从页 0 开始连续覆盖**的，不会出现这种空洞。这是一个"够用但不完美"的简化。

**`truncateLastPage` —— 释放最后一页**

```java
// DiskManager.java:119
long newLen = f.length() - Page.SIZE;
if (newLen < 0) throw Error.st("cannot truncate below zero");
f.setLength(newLen);
```

**`force()` —— 全体 fsync**

```java
// DiskManager.java:131
for (RandomAccessFile f : files.values()) {
    f.getFD().sync();     // 强制刷到物理磁盘
}
```

只在 WAL 重放后调用——保证**数据真的落盘了，再清空日志**。顺序反了就会丢数据。

**关闭钩子**

```java
// DiskManager.java:36
Runtime.getRuntime().addShutdownHook(new Thread(this::close, "db-disk-close"));
```

进程被 Ctrl+C 或 `taskkill` 杀掉时，JVM 会跑关闭钩子——保证文件句柄正常关闭、缓冲区不丢。

### ★ 权衡：为什么不用 `FileChannel`

`FileChannel` 支持 `force()`、`map()`（内存映射）、`transferTo()`（零拷贝），性能更好。但 `RandomAccessFile` 的 API 更直白（`seek`/`readFully`/`write`），对教学项目更友好。而且 `RandomAccessFile.getFD().sync()` 同样能 fsync。

## 2.4 Cache：页缓存与 LRU/FIFO

这条线的关键问题是：**磁盘 IO 比内存慢 5 个数量级**。同一页可能被反复读写（比如扫表时反复读页 0 的页头），每次都读盘就太浪费了。

解决方案：**把读过的页在内存里留一份**——这就是页缓存。

### 接口设计：`Cache`

文件：[`storage/Cache.java`](src/main/java/com/course/dbms/storage/Cache.java)，29 行。

```java
public interface Cache {
    Page get(long key);
    void put(long key, Page page);
    void remove(long key);
    int size();
    String policy();      // "LRU" / "FIFO"
    String stats();       // 命中率文本
    void logStats();      // 打到日志
}
```

> ★ 关键设计：**上层只依赖接口，不依赖实现**。
> `PageManager` 拿到的是 `Cache`（接口），`Database` 构造时传 `new LruCache(64)`。想换成 FIFO？改一个字：`new FifoCache(64)`。`Database` 甚至专门留了一个构造函数 `Database(PageManager pm)` 允许外部注入——就是为了演示时能切换缓存策略。

**key 是什么？** `long`，高 32 位 tableId、低 32 位 pageNo：

```java
// PageManager.java:35
private static long key(int tableId, int pageNo) {
    return ((long) tableId << 32) | (pageNo & 0xffffffffL);
}
```

**为什么不用 `PageKey` 对象？** `HashMap` 查一个 `Long` 比查一个自定义对象快（不用算自定义 `hashCode`），而且没有小对象分配。`& 0xffffffffL` 是把 int 当无符号扩展——虽然实际页号不会到负数，但写成这样更严谨。

### `AbstractCache`：把"统计"这件事收上来

文件：[`storage/AbstractCache.java`](src/main/java/com/course/dbms/storage/AbstractCache.java)，37 行。

```java
public abstract class AbstractCache implements Cache {
    protected final int capacity;
    private long hits;
    private long misses;

    protected void recordHit()  { hits++; }
    protected void recordMiss() { misses++; }

    @Override public String stats() {
        long total = hits + misses;
        double rate = total == 0 ? 0 : (double) hits / total * 100;
        return String.format("%s capacity=%d hits=%d misses=%d hitRate=%.1f%%",
                policy(), capacity, hits, misses, rate);
    }

    @Override public void logStats() {
        Log.info("CACHE[" + stats() + "]");
    }
}
```

**模板方法模式**：`AbstractCache` 负责"统计 + 格式化 + 打日志"这些跟淘汰策略无关的事，子类只管"什么时候淘汰哪一页"。`total == 0 ? 0 : ...` 是防除零。

**命中率输出样例**：`LRU capacity=64 hits=37 misses=5 hitRate=88.1%`。这是演示时的好素材——可以让老师看到"第二次扫同一张表，命中率明显上升"。

### `LruCache`：最近最少使用

文件：[`storage/LruCache.java`](src/main/java/com/course/dbms/storage/LruCache.java)，48 行。

```java
public class LruCache extends AbstractCache {
    private final LinkedHashMap<Long, Page> map;

    public LruCache(int capacity) {
        super(capacity);
        this.map = new LinkedHashMap<Long, Page>(16, 0.75f, true) {   // ← accessOrder = true
            @Override protected boolean removeEldestEntry(Map.Entry<Long, Page> eldest) {
                boolean evict = size() > LruCache.this.capacity;
                if (evict) Log.debug("LRU evict page#" + eldest.getKey());
                return evict;
            }
        };
    }

    @Override public synchronized Page get(long key) {
        Page p = map.get(key);
        if (p != null) recordHit(); else recordMiss();
        return p;
    }
    // put / remove / size / policy 都 synchronized
}
```

**整个 LRU 的实现就是两个 Java 细节**：

1. **`LinkedHashMap` 的第三个构造参数 `accessOrder = true`**
   - `false`（默认）：链表按**插入顺序**排列
   - `true`：链表按**访问顺序**排列——每次 `get` 或 `put`，该条目被移到链表**尾部**
   - 于是：链表**头部 = 最久未使用**，**尾部 = 最近使用**

2. **`removeEldestEntry` 回调**
   - `LinkedHashMap.put` 之后会调用它，参数是链表头（最老的条目）
   - 返回 `true` 就删除它
   - 这里写成 `size() > capacity`——即"插入后如果超出容量就淘汰头部"

**就这么两行，LRU 就实现了。** 这是 Java 集合框架里少见的"特性正好对上需求"的例子。

**注意 `LruCache.this.capacity`** —— 匿名内部类里访问外部类的字段，必须写 `Outer.this.field`，因为 `AbstractCache.capacity` 是 `protected` 但匿名类是**另一个类**。写成裸 `capacity` 也能编译（继承链上能访问），但显式写出来更清楚。

### `FifoCache`：先进先出

文件：[`storage/FifoCache.java`](src/main/java/com/course/dbms/storage/FifoCache.java)，38 行。

跟 `LruCache` 唯一的差别：`LinkedHashMap(16, 0.75f, **false**)`。

**FIFO 和 LRU 的行为差异**——为什么要有 FIFO 做对照？

考虑这个访问序列（容量 3）：`A B C A D`

| 策略 | 步骤 | 结果 |
|---|---|---|
| **LRU** | 访问 A → A 移到尾；插入 D 时淘汰**最久未访问**的 | 淘汰 B（因为 A 刚被访问过） |
| **FIFO** | 访问 A **不改变顺序**；插入 D 时淘汰**最早插入**的 | 淘汰 A（无视它刚被访问过） |

**LRU 更好**，因为它利用了"刚访问过的页很可能马上又被访问"（时间局部性）。FIFO 完全无视访问历史。

**那为什么还要写 FIFO？** 两个理由：

1. **演示对照**：能让老师直观看到"同样的负载，LRU 命中率 88%、FIFO 命中率 62%"，证明策略选择真的有影响
2. **验证接口抽象是对的**：换策略只需改一个构造调用，这正是 §2.4 开头说的"上层只依赖接口"的价值

### ★ 权衡：`synchronized` 的粒度

所有缓存方法都加了 `synchronized`（锁的是 `this`，即缓存对象）。这会让缓存成为**串行点**——但正确性优先：服务端多连接并发时，如果 `LinkedHashMap` 被并发修改，会抛 `ConcurrentModificationException` 或直接死循环（JDK 7 的 HashMap 并发扩容死循环是著名事故）。

生产级方案是 **CLOCK 算法 + 每槽位独立锁**（InnoDB 的做法）或 **分段锁**，本项目不需要。

## 2.5 PageManager：页式存储的对外接口

文件：[`storage/PageManager.java`](src/main/java/com/course/dbms/storage/PageManager.java)，143 行。这是存储系统**真正对外**的类——`Catalog`、`StorageEngine`、`Session` 都跟它打交道。

它解决的问题：**把"缓存 + 磁盘 + 事务"三件事组合成一个统一的页访问接口。**

### 构造函数里藏了一件大事

```java
// PageManager.java:24
public PageManager(DiskManager disk, Cache cache) {
    this.disk = disk;
    this.cache = cache;
    this.log = new Log(disk.dir());
    // 对外服务前先做崩溃恢复：重放已提交事务（redo），丢弃未提交事务
    this.log.recover(disk);
}
```

**打开数据库的第一件事就是崩溃恢复。** 这个位置是刻意的：在任何人能调用 `getPage` 之前，磁盘必须已经是"一致的"状态。

### 五个核心方法

| 方法 | 作用 | 事务内行为 |
|---|---|---|
| `getPage(tableId, pageNo)` | 取一页 | 返回**工作集深拷贝** |
| `newPage(tableId)` | 分配新页，返回新页号 | 只进工作集，不写盘 |
| `persist(page)` | 把页刷盘 | 只进工作集 |
| `freeLastPage(tableId)` | 释放末尾页 | **直接报错** `TX-0004` |
| `pageCount(tableId)` | 当前页数 | 计入本事务新增页 |

### `getPage` 双路径（这是事务的核心）

```java
// PageManager.java:40
public Page getPage(int tableId, int pageNo) {
    long k = key(tableId, pageNo);
    Txn t = Txn.current();

    if (t != null && t.active()) {                             // ── 路径 A：事务内
        Page inTxn = t.get(tableId, pageNo);
        if (inTxn != null) return inTxn;                       // read-your-writes
        Page shared = cache.get(k);
        byte[] raw = shared != null ? shared.data() : disk.readPage(tableId, pageNo);
        if (raw == null) return null;
        // 关键：深拷贝。Page(byte[]) 不拷贝底层数组，浅拷贝会改到共享缓存里的页。
        Page copy = new Page(Arrays.copyOf(raw, Page.SIZE));
        t.put(tableId, pageNo, copy);                          // 只进工作集，不进共享缓存
        return copy;
    }

    Page p = cache.get(k);                                     // ── 路径 B：无事务
    if (p != null) return p;
    byte[] raw = disk.readPage(tableId, pageNo);
    if (raw == null) return null;
    Page pg = new Page(raw);
    cache.put(k, pg);
    return pg;
}
```

**路径 B** 就是教科书里的页缓存逻辑：命中返回、未命中读盘并放入缓存。

**路径 A** 是"影子页"机制的入口：

1. 先看工作集里有没有 → 有就直接返回（这叫 **read-your-writes**：事务能读到自己未提交的修改）
2. 没有 → 从共享缓存/磁盘拿原始字节
3. **深拷贝**一份放进工作集
4. 之后这个事务对页的所有修改都改在工作集的拷贝上——**共享缓存和磁盘完全不受影响**

> ★ 注释里强调的坑：`new Page(byte[] data)` 构造时**不拷贝**底层数组（`this.data = data`）。所以如果直接 `new Page(raw)` 放进工作集，改工作集里的页就等于改了共享缓存里的同一块内存——隔离性直接失效。
> 必须 `Arrays.copyOf(raw, Page.SIZE)` 先复制字节数组。
> 这是一个"看起来对、跑起来错"的典型 bug，值得在汇报时讲。

**深拷贝的代价**：每次事务内第一次访问一页，都要拷 4096 字节。这是**用空间换隔离性**——没有 undo log（那需要记录"改之前是什么样"），而是"整页复制一份，回滚就扔掉"。

### `newPage`：页号怎么算

```java
// PageManager.java:64
public int newPage(int tableId) {
    Txn t = Txn.current();
    if (t != null && t.active()) {
        int pageNo = disk.pageCount(tableId) + t.newCount(tableId);   // 磁盘页数 + 本事务已分配数
        Page pg = new Page(tableId, pageNo);
        t.put(tableId, pageNo, pg);
        t.bumpNewCount(tableId);
        return pageNo;
    }
    int pageNo = disk.pageCount(tableId);
    Page pg = new Page(tableId, pageNo);
    disk.writePage(tableId, pageNo, pg.data());
    cache.put(key(tableId, pageNo), pg);
    return pageNo;
}
```

**注意事务内不写盘**——只是把页对象放进工作集，并记一笔"本事务在这个表上分配了 N 页"。

`newPages` 这个计数器的意义：**让分配出来的页号不与磁盘上的已有页冲突**。

场景：磁盘上 `users` 有 3 页（0,1,2）。事务分配第 4 页：

- 第一次 `newPage`：`pageNo = 3 + 0 = 3`，计数变 1
- 第二次 `newPage`：`pageNo = 3 + 1 = 4`，计数变 2

如果只算 `disk.pageCount()`，两次都会分配页号 3，第二次就把第一次的页覆盖了。

**`pageCount` 也对应地加了 `t.newCount`**，这样 `StorageEngine.insert` 的扫描循环 `for (pageNo = 0; pageNo < n; pageNo++)` 才能看到事务内新分配的页。

### `persist`：写回

```java
// PageManager.java:81
public void persist(Page page) {
    Txn t = Txn.current();
    if (t != null && t.active()) {
        t.put(page.tableId(), page.pageNo(), page);   // 只进工作集
        return;
    }
    disk.writePage(page.tableId(), page.pageNo(), page.data());
    cache.put(key(page.tableId(), page.pageNo()), page);
}
```

无事务时：立即写盘 + 更新缓存。这就是"自动提交"的语义——一条 INSERT 立即持久。

### 提交与回滚

```java
// PageManager.java:115
public void commitTxn(Txn t) {
    if (t == null) return;
    // 先写日志并 fsync：此刻提交已持久且原子（有完整 COMMIT 标记）；之后崩在落页也能重放
    log.writeCommit(t.id(), t.pages());
    for (Map.Entry<Long, Page> e : t.pages().entrySet()) {
        long k = e.getKey();
        Page pg = e.getValue();
        disk.writePage(pg.tableId(), pg.pageNo(), pg.data());
        cache.put(k, pg);
    }
    t.reset();
}

// PageManager.java:130
public void rollbackTxn(Txn t) {
    if (t == null) return;
    t.reset();                 // 丢弃工作集。从未碰共享缓存/磁盘，无需恢复。
}
```

> ★ **这就是整个项目最漂亮的设计**：
> - **COMMIT = 写 WAL + fsync + 写数据页 + 清工作集**
> - **ROLLBACK = 清工作集（一行代码）**
>
> 回滚不需要 undo log，因为事务从未污染过共享状态。这是"影子页"（shadow paging）相对"undo log"的巨大简化。
>
> 代价：事务内的页修改是**拷贝**，占内存；且事务内每次首次访问一页都要 `Arrays.copyOf`。

### 第 2.5 节自问自答

**Q：事务内读一页，会污染缓存吗？**
A：不会。深拷贝的那份只放在 `Txn.pages`（工作集），不进 `Cache`。所以别的会话读同一页，拿到的还是磁盘上的旧版本——这就实现了**写未提交对其他会话不可见**。

**Q：如果事务改了很多页，内存会不会爆？**
A：会。本项目的影子页是**全量拷贝**，一个大事务（比如插 100 万行）会把整张表拷进内存。生产库用 undo log 解决（只记"改前值"），本项目是教学规模，可以接受。这也解释了为什么 `Txn` 是个"简化但不失真"的教学实现。

## 2.6 Log：WAL 预写日志

文件：[`storage/Log.java`](src/main/java/com/course/dbms/storage/Log.java)，181 行。

### WAL 要解决什么问题

考虑这个时序（`commitTxn` 的前两步）：

1. WAL 已经写了
2. 开始写数据页：页 0 完成、页 1 完成、**断电** ← 页 2、3 还是旧的

或者反过来（如果先写数据页后写 WAL）：

1. 数据页写了一部分，**断电**
2. WAL 没写 → 没有 COMMIT 标记 → 这个事务会被当成"未提交"丢弃
3. 但数据页上已经有它的修改了 → **数据不一致**

**WAL（Write-Ahead Logging）的规定**：日志必须在数据页之前落盘，且**带 COMMIT 标记**才算提交。

关键洞察：**只要有完整的 COMMIT 记录在日志里，即使数据页还没写完，重启后也能从日志"补"回去。**

### 记录格式

```
[4字节 recordLen][1字节 type][8字节 txnId] + 载荷

type = 0  BEGIN   —— 无载荷（事务开始标记）
type = 1  PUT     —— 载荷 = [4字节 tableId][4字节 pageNo][PAGE_SIZE 字节页后像]
type = 2  COMMIT  —— 无载荷（提交标记）
```

**为什么每条记录带长度前缀？** 为了容忍**撕裂尾部**——如果进程在写日志写到一半时崩溃，最后一条记录的字节不完整。恢复时：

```java
// Log.java:81
int len = in.readInt();
if (in.available() < len) break;    // 撕裂尾部：不完整记录，停止解析
```

一看到"声明的长度比实际剩下的字节多"，就知道遇到了半截记录，**停止解析，前面的都有效**。这是自定界（self-delimiting）格式的标准做法。

### 提交协议

```java
// Log.java:50
public void writeCommit(long txnId, Map<Long, Page> pages) {
    writeRecord(TYPE_BEGIN, txnId, 0, 0, null);
    for (Page p : pages.values()) {
        writeRecord(TYPE_PUT, txnId, p.tableId(), p.pageNo(), p.data());   // 每页一条后像
    }
    writeRecord(TYPE_COMMIT, txnId, 0, 0, null);
    sync();                                                    // fsync！
}
```

**注意 `sync()` 在最后**：`raf.getFD().sync()` 强制操作系统把文件系统缓存刷到**物理磁盘**。没有这一步，日志可能还在 OS 的 page cache 里，断电就丢了。

**BEGIN 记录有什么用？** 逻辑上可以没有（PUT + COMMIT 就够了）。它存在是为了**边界清晰**——看日志文件时能一眼看出"这个事务从哪开始"。恢复代码里注释也写了"仅作分界标记，恢复无需特别处理"。

### 恢复

```java
// Log.java:72
public void recover(DiskManager disk) {
    byte[] all = readAll();
    if (all == null || all.length == 0) return;

    Set<Long> committed = new HashSet<>();      // 第一遍：扫出所有有 COMMIT 的 txnId
    List<PutRec> puts = new ArrayList<>();      // 及其所有 PUT 记录
    // ... 解析 ...

    for (PutRec p : puts) {
        if (committed.contains(p.txnId)) {      // 第二遍：只重放已提交的
            disk.writePage(p.tableId, p.pageNo, p.image);
        }
    }
    disk.force();                                // 刷盘
    truncate();                                  // 清空日志
}
```

**为什么要两遍？** 因为日志里可能出现：

```
BEGIN txn1
PUT  txn1 ...
COMMIT txn1
BEGIN txn2
PUT  txn2 ...        ← 没有 COMMIT
```

**单遍解析做不到**——读到 txn2 的 PUT 时还不知道后面有没有 COMMIT。所以必须**先全部读进内存、标记哪些 txnId 提交过，再统一重放**。

**`truncate()` 在最后**：恢复成功后清空日志。因为已提交的数据已经落到数据文件了，日志留着只会让下次恢复重复做无用功。

**为什么是 redo-only（没有 undo）？**

因为**未提交事务从未写到数据文件**。影子页机制保证了这一点：事务的所有修改都在内存工作集里。所以恢复时只需要"补上已提交的"，不需要"撤销未提交的"。

> ★ 这是**影子页 + WAL** 组合带来的简化。如果用 undo log 的架构（如 InnoDB），恢复时要复杂得多：分析阶段、重做阶段、撤销阶段（ARIES 三阶段算法）。本项目用一个更简单的机制换来了同样的正确性保证。

### 四个测试场景（`storage/LogTest.java`）

| 测试 | 验证什么 |
|---|---|
| `recoveryAppliesCommittedTxnThatNeverHitDataFile` | 有 COMMIT 但数据页没写 → 恢复后数据在 |
| `discardsUncommittedTxnOnRecover` | 有 PUT 无 COMMIT → 恢复后数据不在 |
| `tornTailStopsGracefully` | 日志尾部截断 → 不崩溃，前面的记录正常恢复 |
| `replayIsIdempotent` | 同一份日志重放两次结果相同（幂等） |

**幂等性为什么重要？** 因为恢复可能被打断（恢复过程中又崩了）。如果重放不是幂等的，第二次恢复会出错。这里幂等是天然的——PUT 是**整页覆盖**，写两次和写一次结果一样。

## 2.7 第 2 章自问自答

**Q：一个页能存多少行？**
A：取决于行大小。页头 16 字节，每行还要 4 字节槽位。假设一行 30 字节，则 `(4096 - 16) / (30 + 4) ≈ 120` 行。

**Q：为什么不是"一行一个文件"？**
A：文件系统的最小分配单位是块（4KB），一行一个文件会浪费 99% 的磁盘；而且打开几万个文件句柄会耗尽系统资源。页的意义就是"批量"。

**Q：`Page` 里为什么既有 `tableId` 也有 `pageNo` 冗余存储？**
A：从磁盘读回来的字节数组是**无上下文**的——`new Page(raw)` 只能从字节里读出这两个值。而且它们可以当**校验**用（页头 magic + pageNo 能检测出"读错页"）。

**Q：如果两个事务同时 `newPage` 同一张表会怎样？**
A：不会冲突。因为 `Session` 对 INSERT 语句加的是**表级 X 锁**——同一张表上同时只能有一个写事务。锁把并发写串行化了。见第 8 章。

---

# 第 3 章 事务与并发（txn）

课程图片底部"高级功能①"。三个类：`Lock`、`LockManager`、`Txn`。

## 3.1 为什么需要事务与锁

考虑两个客户端同时操作 `users` 表：

```
时刻  A 连接                              B 连接
 t1   begin;
 t2   insert into users values (1,...);
 t3                                        select * from users;   ← 能看见 (1,...) 吗？
 t4   rollback;
```

如果 t3 能看见 (1,...)，那就是**脏读**（dirty read）——读到了别人**最终会回滚**的数据。隔离性要保证 t3 看不到。

本项目用**表级锁 + 影子页**两层机制实现隔离：

- **锁**：写事务持有表的 X 锁到提交，所以 B 的读会被**阻塞**（不是读到脏数据，而是等）
- **影子页**：事务的修改不落盘不进共享缓存，所以就算锁失效也读不到

**两层防御**，任何一层单独都够用，但两层一起才是稳妥的（锁还可能因为超时或 bug 失效）。

## 3.2 Lock：锁模式

文件：[`txn/Lock.java`](src/main/java/com/course/dbms/txn/Lock.java)，14 行。

```java
public enum Lock {
    SHARED,       // 共享读锁：多个持有者并存；与 X 互斥
    EXCLUSIVE;    // 排他写锁：独占；与一切互斥

    public boolean isShared()    { return this == SHARED; }
    public boolean isExclusive() { return this == EXCLUSIVE; }
}
```

**兼容矩阵**：

| 已持有 \ 请求 | SHARED | EXCLUSIVE |
|---|---|---|
| **无锁** | ✅ 授予 | ✅ 授予 |
| **SHARED** | ✅ 授予（读-读不互斥） | ❌ 等待（读-写互斥） |
| **EXCLUSIVE** | ❌ 等待（写-读互斥） | ❌ 等待（写-写互斥） |

## 3.3 LockManager：表级锁管理器

文件：[`txn/LockManager.java`](src/main/java/com/course/dbms/txn/LockManager.java)，165 行。

### 数据结构

```java
private final Map<Integer, Grant> grants = new HashMap<>();   // tableId -> 该表的锁状态
private final AtomicLong idSeq = new AtomicLong(1);           // 事务/持锁 id 发生器

private static final class Grant {
    Long xTxn = null;                          // 排他持有者（null = 无）
    final Set<Long> sTxns = new HashSet<>();   // 共享持有者集合（可多个）
    final List<Waiter> waiters = new ArrayList<>();  // 等待队列
}
```

`nextId()` 用 `AtomicLong` 保证并发下不重复。无论是显式事务（BEGIN 时分配）还是自动提交语句（每条语句分配一个临时 id），都从这里拿号。

### `acquire`：加锁

```java
// LockManager.java:40
public synchronized void acquire(long txn, int table, Lock mode, long timeout) {
    Grant g = grants.get(table);
    if (g == null) { g = new Grant(); grants.put(table, g); }

    // 重入：已是排他持有者，任意模式都直接给（事务内读自己写的行）
    if (g.xTxn != null && g.xTxn.longValue() == txn) return;
    // 重入：已是共享持有者且请求共享
    if (mode == Lock.SHARED && g.sTxns.contains(txn)) return;

    long deadline = System.currentTimeMillis() + timeout;
    Waiter mine = new Waiter(txn, toInt(mode));
    g.waiters.add(mine);                       // ← 关键：先入队，再等
    try {
        while (true) {
            if (canGrant(g, txn, toInt(mode))) {
                g.waiters.remove(mine);
                applyGrant(g, txn, toInt(mode));
                notifyAll();
                return;
            }
            long remain = deadline - System.currentTimeMillis();
            if (remain <= 0) {
                g.waiters.remove(mine);
                throw new Error("TX-0003", "lock timeout on table " + table + " want " + mode);
            }
            wait(remain);                       // 释放锁并等待，最多 remain 毫秒
        }
    } catch (InterruptedException e) {
        g.waiters.remove(mine);
        Thread.currentThread().interrupt();
        throw new Error("TX-0003", "lock interrupted on table " + table, e);
    }
}
```

**几处精妙的地方**：

**① 先入队再等（而不是"看看能不能拿，不能就拿去等"）**

如果写成"先判断 `canGrant`，不行就 `wait`"，会有**丢失唤醒**（lost wakeup）问题：判断和 `wait` 之间如果有别的线程释放了锁并 `notifyAll`，这个通知就丢了——然后你会一直等到超时。

**先把自己加进 `waiters` 再进循环**，就堵住了这个窗口。这也是为什么 `canGrant` 的 SHARED 分支要检查 `waiters` 里有没有 X 等待者——它必须能看到自己。

**② `while (true)` 而不是 `if`**

`wait()` 返回后条件可能已经变了（被唤醒不代表条件成立——这叫**虚假唤醒** spurious wakeup）。所以必须重新判断。标准写法就是 `while (!canGrant) wait();`。

**③ `wait(remain)` 带超时**

不能无限等——否则一个卡住的事务会永久阻塞其他所有会话。`Consts.LOCK_TIMEOUT_MS = 5000`（5 秒）后抛 `TX-0003`。

**④ `InterruptedException` 的处理**

```java
Thread.currentThread().interrupt();   // 恢复中断标志
```

`wait()` 抛异常时会**清除**中断标志。如果不再置回去，上层就没法知道"这个线程被要求中断了"。这是 Java 并发编程的标准纪律。

### `canGrant`：授予判断（含写者饥饿规避）

```java
// LockManager.java:123
private boolean canGrant(Grant g, long txn, int mode) {
    if (mode == X) {                                    // ── 请求排他锁
        if (g.xTxn != null && g.xTxn.longValue() != txn) return false;  // 别人持有 X
        if (g.xTxn != null) return true;                               // 自己持有 X（重入）
        if (!g.sTxns.isEmpty()) {
            // 唯一共享持有者(即自己)才可升级；否则等所有读者退散
            if (g.sTxns.size() == 1 && g.sTxns.contains(txn)) return true;
            return false;
        }
        return true;
    } else {                                            // ── 请求共享锁
        if (g.xTxn != null) return false;                              // 有写者，不可
        // 避免写者饥饿：若已有排他等待者(独占先到者优先)，共享不让路
        for (Waiter w : g.waiters) if (w.mode == X) return false;
        return true;
    }
}
```

**X 的分支有三个层次**：

1. 被人持有 X → 拒绝
2. 自己持有 X → 重入允许（事务内"读自己刚写的行"）
3. 有人持有 S → 只有在**自己是唯一的 S 持有者**时才允许（这叫**锁升级**）

**为什么不允许多人持有 S 时升级 X？** 因为"升级"意味着要把别人的 S 锁踢掉，而对方可能正在读——从一致性的角度，只能**等所有读者退出**再升级。这是数据库锁升级的经典规则。

**S 分支的写者饥饿规避**（注释里明确的）：

考虑这个场景：

```
A: 拿 S 锁 → 读 → 释放
B: 拿 S 锁 → 读 → 释放
C: 请求 X 锁 → 阻塞（A 和 B 读着），进入 waiters
D: 请求 S 锁 → ?
```

如果 D 直接拿到 S，那么 A 读完释放、B 读完释放、D 还在读……**C 永远等不到**。这就是**写者饥饿**。

解决：**如果 `waiters` 里已经有人在等 X，新来的 S 请求就排队**（不给）。这样 C 前面的读者退散后，C 就能拿到锁。这就是"独占的先到者优先"。

> ★ 权衡：这个策略**牺牲了读的并发度**（读者要排队）来**保证写的及时性**。另一种策略是"读者优先"（读永不等），但会导致写饥饿。真正的数据库常用"先来先服务 + 读写公平"的队列，本项目选了简单且能解决饥饿的方案。

### `releaseTxn`：一次性释放

```java
// LockManager.java:75
public synchronized void releaseTxn(long txn) {
    boolean any = false;
    Iterator<Map.Entry<Integer, Grant>> it = grants.entrySet().iterator();
    while (it.hasNext()) {
        Grant g = it.next().getValue();
        boolean touched = false;
        if (g.xTxn != null && g.xTxn.longValue() == txn) { g.xTxn = null; touched = true; }
        if (g.sTxns.remove(txn)) touched = true;
        for (Iterator<Waiter> w = g.waiters.iterator(); w.hasNext();) {
            if (w.next().txn == txn) { w.remove(); touched = true; }   // 也别留等待项
        }
        if (touched) { any = true; if (empty(g)) it.remove(); }
    }
    if (any) notifyAll();
}
```

**为什么用一个方法释放"这个 txn 的所有锁"？**

因为一个事务可能锁了多张表（比如 `ShowStmt.indexes` 会锁 `SYS_INDEXES` + 目标表）。逐个释放太容易漏，而且如果中途抛异常会出现"锁泄漏"。

**一次全放**，语义清晰：事务结束 = 它持有的所有锁全部释放。

**`empty(g)` 时 `it.remove()`** —— 把无用的 `Grant` 从 map 里删掉，避免内存泄漏（否则每张被锁过的表都永远留一个空 `Grant`）。

**`if (any) notifyAll()`** —— 只有真的释放了什么才唤醒等待者（减少无谓唤醒）。

### ★ 死锁：本项目的处理方式

**死锁**：A 持有表 1 等表 2，B 持有表 2 等表 1 —— 互相等，永远解不开。

本项目的处理：**预防 + 兜底**。

1. **预防**：`Session.lockRequest()` 返回的加锁列表**按 tableId 升序排序**：

```java
// Session.java:175
req.sort((a, b) -> Integer.compare(a[0], b[0]));
```

这样所有会话都按**全局同一顺序**抢锁。经典的**资源有序分配法**——只要大家按同一顺序申请，就不可能成环，死锁不可能发生。

2. **兜底**：5 秒超时抛 `TX-0003`。万一有没想到的环（比如未来加了行级锁），也不会永久卡死。

## 3.4 Txn：影子页工作集

文件：[`txn/Txn.java`](src/main/java/com/course/dbms/txn/Txn.java)，58 行。

```java
public class Txn {
    public enum State { ACTIVE, COMMITTED, ABORTED }

    private static final ThreadLocal<Txn> CURRENT = new ThreadLocal<>();
    private final long id;                                          // 锁管理器里的 txnId
    private State state = State.ACTIVE;
    private final Map<Long, Page> pages = new HashMap<>();          // 影子页工作集
    private final Map<Integer, Integer> newPages = new HashMap<>(); // tableId -> 本事务新增页数

    public static Txn current() { return CURRENT.get(); }
    public static void setCurrent(Txn t) { CURRENT.set(t); }
    public static void clearCurrent() { CURRENT.remove(); }
    // get/put/pages/newCount/bumpNewCount/reset
}
```

### 为什么用 ThreadLocal？

**因为每个客户端连接一个线程。** 存储层（`PageManager`）在很深的地方被调用，如果要让它知道"当前在哪个事务里"，有两条路：

1. **参数传递**：`getPage(tableId, pageNo, Txn t)` —— 那 `StorageEngine.scan`、`Catalog.reload`、每个算子……全部方法签名都要加一个 `Txn` 参数。**污染整个代码库**。
2. **ThreadLocal**：`Txn.current()` 在任何地方都能拿到当前线程的事务。**零侵入**。

选了 2。

> ★ 权衡：ThreadLocal 的隐患是**忘记清理**会在线程复用时泄漏（把上一个会话的事务带到下一个）。本项目的防线：
> - `BEGIN` 时 `setCurrent`，`COMMIT`/`ROLLBACK` 时 `clearCurrent`
> - 每个连接一个线程，线程结束就结束（`ConnectionHandler` 的 `try-with-resources` 会关闭 socket）
> - `Session.runData` 里每次都用 `Txn.current() != null && active()` 判断，不在事务里就是 `null`/非 ACTIVE

**`Txn` 的 key 是 `long`**，跟缓存用同一套编码（高 32 位 tableId、低 32 位 pageNo）——这是刻意的复用。

### 状态机

```
        BEGIN
          ↓
      ┌────────┐
      │ ACTIVE │
      └───┬────┘
   COMMIT │ ROLLBACK
     ↓    ↓
COMMITTED  ABORTED
```

`active()` 返回 `state == State.ACTIVE`。`PageManager` 里每个方法都检查 `t != null && t.active()`——**一个已提交/已回滚的事务对象不会再影响页访问路径**。这是防御性的，正常流程下 `Txn.current()` 在事务结束后就是 `null`。

### `reset()`：让 Txn 对象可重用

```java
public void reset() { pages.clear(); newPages.clear(); }
```

注意它**不改 state**——`commitTxn` 里调 `reset()` 之后，`Session` 再调 `cur.setState(State.COMMITTED)`。分开做是因为 `reset()` 是"清空工作集"，`setState` 是"标记生命周期结束"，两件事。

## 3.5 三种隔离来源（重要！）

汇报时如果被问"你的隔离性是怎么实现的"，答案是**三个层次的组合**：

| 层次 | 机制 | 保证什么 |
|---|---|---|
| **① 表级锁** | 写事务持 X 锁到提交 | **阻塞**并发读，让它们等而不是读到脏数据 |
| **② 影子页** | 事务修改只在内存工作集 | 即使锁失效，其他会话也**看不到**未提交修改（它们读的是共享缓存/磁盘） |
| **③ 自动提交短锁** | 无事务时每条语句加锁后立即释放 | 单条语句本身原子 |

**举个具体例子说明①②的分工**：

```
A: begin;  insert into users values (99, 'zoe', 25);
    → 加 X 锁，写进影子页（内存），users 表文件没变

B: select * from users;
    → 要加 S 锁 → 被 A 的 X 锁挡住 → 在 LockManager.wait() 里等
    → 5 秒超时或 A 提交后继续
    → 无论哪种情况，B 都读不到 99 这一行
```

如果 A 只是"自动提交"的一条 INSERT（没有 BEGIN），那么：

```
A: insert into users values (99, 'zoe', 25);
    → 无事务上下文 → 加 X 锁 → 直接 se.insert() 写盘 + 更新缓存 → 立即释放 X 锁
    → 数据立即持久，别的会话立刻能看到（这是"自动提交"的语义）
```

## 3.6 编辑：`Session` 如何编排这一切

文件：[`db/Session.java`](src/main/java/com/course/dbms/db/Session.java)，178 行。这是事务的**编排中枢**。

### 事务控制语句直接处理，不生成计划

```java
// Session.java:58
private Result handleTx(TxnStmt t) {
    switch (t.op) {
        case BEGIN:
            if (Txn.current() != null && Txn.current().active()) {
                throw new Error("TX-0002", "已在事务中，请先 COMMIT 或 ROLLBACK");
            }
            Txn tx = new Txn(locks.nextId());
            tx.setState(Txn.State.ACTIVE);
            Txn.setCurrent(tx);
            return Result.message("transaction started");

        case COMMIT:
            requireActive();                              // 没有活动事务 → TX-0001
            Txn cur = Txn.current();
            db.pages().commitTxn(cur);                    // ← 写 WAL + 写数据页 + 清工作集
            locks.releaseTxn(cur.id());                   // ← 放锁
            cur.setState(Txn.State.COMMITTED);
            Txn.clearCurrent();                           // ← 脱离 ThreadLocal
            return Result.message("transaction committed");

        case ROLLBACK:
            requireActive();
            Txn cur2 = Txn.current();
            locks.releaseTxn(cur2.id());                  // ← 先放锁
            Txn.clearCurrent();                           // ← 先脱离事务，让 reload 读磁盘而非影子页
            db.pages().rollbackTxn(cur2);                 // ← 丢弃工作集（从不写盘）
            db.catalog().reload();                        // ← 撤销事务内未提交的建表/建索引
            cur2.setState(Txn.State.ABORTED);
            return Result.message("transaction rolled back");
    }
}
```

**`COMMIT` 的顺序为什么是"先提交页再放锁"？**

如果反过来（先放锁再提交页），会有这个窗口：锁已释放 → 别的会话拿到锁读表 → 但页还没写盘 → **读到旧数据**（虽然是提交后的读，却读到提交前的状态）。这违反了持久性。

**`ROLLBACK` 里两个顺序很讲究**：

```java
Txn.clearCurrent();            // ① 先脱离 ThreadLocal
db.pages().rollbackTxn(cur2);  // ② 再丢弃工作集（此时 Txn.current() 已是 null）
db.catalog().reload();         // ③ 最后重载目录
```

**为什么 ① 必须在 ③ 之前？** 因为 `reload()` 会通过 `StorageEngine.scan(sysTables)` 读数据。如果此时 `Txn.current()` 还指向那个事务，`PageManager.getPage` 会走**事务路径**——返回工作集里的**未提交**的深拷贝！

结果就是：**回滚后目录里还留着未提交的表**。注释里明确写了这个坑："先脱离事务，让 reload 读磁盘而非影子页"。

> ★ 这是一个非常经典的"顺序 bug"，值得在汇报时专门讲：**同一个方法调用，在事务内外走完全不同的代码路径**。所以"什么时候脱离事务上下文"必须精确控制。

### 数据语句的两条路径

```java
// Session.java:99
private Result runData(Stmt stmt) {
    Txn active = Txn.current();
    boolean inTxn = active != null && active.active();

    if (inTxn) {
        // 显式事务：current 已由 BEGIN 设好，锁跨语句持有到 COMMIT/ROLLBACK
        acquireLocks(stmt, active.id());
        return db.executeStmt(stmt);
    }

    // 自动提交：短命持锁，执行后立即放锁
    long lockId = locks.nextId();
    acquireLocks(stmt, lockId);
    try {
        return db.executeStmt(stmt);
    } finally {
        locks.releaseTxn(lockId);       // finally 保证异常时也放锁
    }
}
```

**显式事务**：用事务自己的 id 加锁，**不释放**——锁会一直持有到 COMMIT/ROLLBACK（因为 `handleTx` 里才调 `releaseTxn`）。这就是"写锁持有到提交"的实现。

**自动提交**：临时分配一个 id，`finally` 里立即释放。**注意 `finally`**——如果 `executeStmt` 抛异常（比如列不存在的语义错误），锁也必须放掉，否则泄漏。

## 3.7 相关错误码

| 错误码 | 场景 |
|---|---|
| `TX-0001` | 没有活动事务就 COMMIT/ROLLBACK |
| `TX-0002` | 已在事务中又 BEGIN（不支持嵌套） |
| `TX-0003` | 等锁超时（5 秒）或被中断 |
| `TX-0004` | 事务内调用 `freeLastPage` |
| `TX-0005` | 未知的事务操作 |

## 3.8 第 3 章自问自答

**Q：为什么不支持嵌套事务？**
A：需要 SAVEPOINT 机制（部分回滚），实现复杂度陡增。`Parser.parseTx` 里对 `ROLLBACK TO SAVEPOINT` **明确报语法错误**而不是静默吞掉——这是负责任的做法（见 `Parser.java:87`）。

**Q：读-读真的不互斥吗？**
A：是。`canGrant` 的 S 分支只检查 `g.xTxn != null`（有写者就拒），不看 `sTxns`。所以 N 个读事务可以同时持有同一张表的 S 锁。

**Q：锁的粒度为什么是表级不是行级？**
A：行级锁需要给每行维护锁记录（占用内存与时间），还需要处理"间隙锁"（防止幻读）。表级锁实现简单、开销低，对课程规模的数据量完全够用，且配合影子页已经能保证正确性。**这是"正确性优先于并发度"的取舍。**

**Q：事务隔离级别是什么？**
A：可串行化（Serializable）——因为表级 X 锁持有到提交，写-读、写-写全互斥。代价是并发度低。

**Q：`ConcurrencyTest` 测了什么？**
A：
- `writerTxnBlocksConcurrentReader`：A 在事务里写，B 读会被阻塞
- `concurrentAutocommitInsertsNoLostUpdate`：多线程同时自动提交 INSERT，不会丢更新

---

# 第 4 章 SQL 编译器（compiler）

课程图片"① SQL 编译器"。四个阶段：**词法分析 → 语法分析 → 语义分析 → 执行计划生成**。

## 4.1 整体管线

```
   "select id from users where age > 22"
                  ↓
      ┌───────────────────────┐
      │  Lexer  词法分析       │   文本 → Token 流
      │  Lexer.java           │
      └───────────┬───────────┘
                  ↓  [SELECT] [IDENTIFIER: id] [FROM] [IDENTIFIER: users] ...
      ┌───────────────────────┐
      │  Parser  语法分析      │   Token 流 → AST（递归下降）
      │  Parser.java          │
      └───────────┬───────────┘
                  ↓  SelectStmt{all=false, columns=[id], tableName=users, where=CMP(age,>,22)}
      ┌───────────────────────┐
      │  Analyzer  语义分析    │   AST + Catalog → 校验通过 / 抛 Error
      │  Analyzer.java        │   （不改数据，只读目录）
      └───────────┬───────────┘
                  ↓  （同一个 AST，只是确认合法）
      ┌───────────────────────┐
      │  PlanBuilder  计划生成 │   AST → 算子树
      │  PlanBuilder.java     │   ★ 这里做物理计划选择（索引 or 全表扫）
      └───────────┬───────────┘
                  ↓  Plan{root=Project(...), columns=[id]}
              Executor
```

> ★ 理解要点：这四个阶段是**单向流水**的，没有回溯。每个阶段只依赖前一个的输出。
> 好处：每一阶段可以独立测试（`ParserTest` 18 个、`AnalyzerTest` 20 个、`PlanBuilderTest` 19 个）。

## 4.2 Token 与 TokenType

### `TokenType`：词法单元类型

文件：[`compiler/token/TokenType.java`](src/main/java/com/course/dbms/compiler/token/TokenType.java)，35 行。

```java
public enum TokenType {
    // 关键字
    CREATE, TABLE, INSERT, INTO, VALUES, SELECT, FROM,
    WHERE, AND, OR, NOT, ORDER, BY, ASC, DESC, SHOW, TABLES,
    JOIN, ON, AS, INNER, LEFT, GROUP,    // 多表联查 / 聚合
    INDEX, INDEXES,                       // 索引
    TRUE, FALSE,
    BEGIN, COMMIT, ROLLBACK,              // 事务控制

    // 基本单元
    IDENTIFIER,        // 标识符 / 类型名（含 int32/string 等）
    NUMBER,            // 整数或浮点数常量
    STR_LIT,           // 单引号字符串字面量（不与类型名 string 冲突）
    OPERATOR,          // = <> < > <= >=

    // 结构标点
    LPAREN, RPAREN, COMMA, SEMICOLON, STAR, DOT,
    EOF;

    /** 由小写关键字文本反查；非关键字返回 null。 */
    public static TokenType fromKeyword(String lower) {
        for (TokenType t : values()) {
            if (t.name().equalsIgnoreCase(lower)) return t;
        }
        return null;
    }
}
```

**几个设计决策**：

**① 为什么类型名（int32/string/...）不单独建枚举？**

类注释里写了："类型名保留为 IDENTIFIER，由语义层通过 `FieldType.fromName` 识别，减少枚举膨胀。"

如果建 `INT32`/`INT64`/`STRING`/... 6 个枚举值，会遇到一个冲突：`STRING` 既是类型名又是 `STR_LIT` 的字符串字面量——容易混淆。而且词汇层不需要知道"哪些标识符是类型名"（那是语义层的事）。**分层清晰**。

**② `NOT` 声明了但没实现？**

`NOT` 在枚举里但 `Parser` 没有对应的解析分支。这是**预留**——写了 `not implemented` 的枚举值比"将来加语法时要动枚举"更省事。汇报时如果被问，如实说"预留未实现"。

**③ `fromKeyword` 用 `equalsIgnoreCase` 反查**

`values()` 返回所有枚举常量，逐个比对名字。这样**加一个关键字只需要在枚举里加一行**，`fromKeyword` 自动支持——零维护成本。

### `Token`：一个词法单元

文件：[`compiler/token/Token.java`](src/main/java/com/course/dbms/compiler/token/Token.java)，26 行。

```java
public class Token {
    public final TokenType type;
    public final String text;      // 原始词面："select" / "123" / "'abc'"
    public final Object value;     // 解析后的 Java 值
    public final int pos;          // 在原始 SQL 中的字符偏移（错误定位用）
}
```

**`text` 和 `value` 为什么要分开？**

`text` 是**给人看的**（原始写法），`value` 是**给程序用的**（已经是 `Long`/`Double`/`String`/`Boolean`）。

举例：`123` → `text="123"`, `value=Long(123)`；`'abc'` → `text="'abc'"`, `value="abc"`（注意 text 带引号，value 不带）。

**`pos` 的作用**：错误信息里带上位置，比如

```
✗ [SY-0001] expected RPAREN but got id at 24
```

这个 `24` 就是字符偏移。调试时非常好用。

**`value` 的类型**：

| Token 类型 | value |
|---|---|
| `NUMBER` | `Long` 或 `Double` |
| `STR_LIT` | `String` |
| `TRUE`/`FALSE` | `Boolean` |
| `IDENTIFIER` | `String`（词面本身） |
| `OPERATOR` | `String`（`"="`/`"<>"`/...） |
| 其他 | `null` |

## 4.3 Lexer：词法分析

文件：[`compiler/Lexer.java`](src/main/java/com/course/dbms/compiler/Lexer.java)，121 行。

**结构**：

```java
public class Lexer {
    private final String sql;
    private int i = 0;               // 当前扫描位置

    public List<Token> tokenize() {
        List<Token> tokens = new ArrayList<>();
        while (true) {
            Token t = next();
            tokens.add(t);
            if (t.type == TokenType.EOF) break;    // EOF 是终点
        }
        return tokens;
    }
}
```

**`next()` 是手写的字符状态机**，按首字符分派：

**① 标识符 / 关键字**

```java
// Lexer.java:42
if (Character.isLetter(c) || c == '_') {
    while (i < sql.length() && (Character.isLetterOrDigit(sql.charAt(i)) || sql.charAt(i) == '_')) i++;
    String word = sql.substring(start, i);
    TokenType kt = TokenType.fromKeyword(word);
    if (kt != null) return tok(kt, word, boolValue(kt), start);   // 关键字
    return tok(TokenType.IDENTIFIER, word, word, start);           // 标识符
}
```

先按"字母/下划线开头，后面跟字母数字下划线"扫出一个完整的词，**再**去查它是不是关键字。这是标准做法——不用维护"哪些前缀是关键字"的复杂表。

`boolValue(kt)` 让 `true`/`false` 直接产出 `Boolean` 值，而不是留给 Parser 转换。

**② 数字（整数 / 浮点）**

```java
// Lexer.java:51
if (Character.isDigit(c)) {
    while (i < sql.length() && Character.isDigit(sql.charAt(i))) i++;      // 整数部分
    boolean isFloat = false;
    if (i < sql.length() && sql.charAt(i) == '.' && i + 1 < sql.length()
            && Character.isDigit(sql.charAt(i + 1))) {
        isFloat = true;
        i++; // '.'
        while (i < sql.length() && Character.isDigit(sql.charAt(i))) i++;   // 小数部分
    }
    String num = sql.substring(start, i);
    // 注意：不能用三元表达式。Double/Long 都是数值包装类型，条件运算符会做
    // 二元数值提升（拆箱加宽）导致整数也被转成 double。用 if/else 保留原类型。
    Object val;
    if (isFloat) val = Double.valueOf(num);
    else val = Long.valueOf(num);
    return tok(TokenType.NUMBER, num, val, start);
}
```

**这里有一行注释讲了一个 Java 语言的坑，非常值得讲**：

如果写成

```java
Object val = isFloat ? Double.valueOf(num) : Long.valueOf(num);   // ❌ 错！
```

Java 的**条件运算符**在两边都是包装数值类型时会做**二元数值提升**（binary numeric promotion）：把 `Long` 和 `Double` 都拆箱成基本类型、提升到 `double`、算完再装箱成 `Double`。

结果：`1` 被解析成 `Double(1.0)` 而不是 `Long(1)`。

这会导致后续 `StorageEngine.cast` 到 INT32 时行为异常（虽然 `((Number) v).intValue()` 还能用，但 `Long` vs `Double` 的类型判断会出错，比如 `Aggregate` 里 `integral(argType)` 的分支）。

**用 `if/else` 就绕过了这个坑**，因为每次赋值是独立的，不触发类型统一。

**`.` 的处理很讲究**：

```java
if (... && sql.charAt(i) == '.' && i + 1 < sql.length() && Character.isDigit(sql.charAt(i + 1)))
```

必须确认 `.` 后面**紧跟着数字**才算小数点。否则 `1.` 或 `t.col` 里的点会被误判——`t.col` 的点是 `DOT` 标点，不是小数点。（`t.col` 会先被识别为标识符 `t`，然后 `.` 走标点分支。）

**③ 字符串字面量**

```java
// Lexer.java:69
if (c == '\'') {
    i++;
    StringBuilder sb = new StringBuilder();
    while (i < sql.length() && sql.charAt(i) != '\'') {
        sb.append(sql.charAt(i));
        i++;
    }
    if (i >= sql.length()) throw err(start, "unterminated string literal");
    i++; // skip closing quote
    return tok(TokenType.STR_LIT, "'" + sb + "'", sb.toString(), start);
}
```

- `text` 保留引号（`"'abc'"`），`value` 是内容（`"abc"`）
- **不支持转义** —— 想输入含单引号的字符串做不到（`''` 不会被当成转义）。这是明确的范围限制，注释里写了。
- **未闭合**（跑到底都没找到右引号）→ 抛 `LX-0001`。这个检查很重要，否则会死循环或产出错误 token。

**④ 运算符**

```java
// Lexer.java:82
if (c == '=' || c == '<' || c == '>' || c == '!' ) {
    if (c == '!' && next == '=') { i += 2; return tok(OPERATOR, "!=", "<>", start); }  // != 归一化成 <>
    if (c == '<' && next == '=') { i += 2; return tok(OPERATOR, "<=", "<=", start); }
    if (c == '<' && next == '>') { i += 2; return tok(OPERATOR, "<>", "<>", start); }
    if (c == '>' && next == '=') { i += 2; return tok(OPERATOR, ">=", ">=", start); }
    i++;
    return tok(OPERATOR, String.valueOf(c), String.valueOf(c), start);   // 单字符 = < >
}
```

**`!=` 被归一化成 `<>`**（`value` 是 `"<>"`）——因为 `Compare.apply` 只认 `<>`。这样 `a != b` 和 `a <> b` 在语义层完全等价，是一种"在词法层做规范化"的技巧。

**⑤ 标点**

```java
switch (c) {
    case '(': i++; return tok(LPAREN, "(", null, start);
    case ')': i++; return tok(RPAREN, ")", null, start);
    case ',': i++; return tok(COMMA, ",", null, start);
    case ';': i++; return tok(SEMICOLON, ";", null, start);
    case '*': i++; return tok(STAR, "*", null, start);
    case '.': i++; return tok(DOT, ".", null, start);
    default: throw err(start, "unexpected character '" + c + "'");
}
```

**`default` 分支是这个项目的"注释不支持"的根源**：

```sql
-- 这是一行注释
```

`-` 落到 `default`，抛 `LX-0001: unexpected character '-' at 0`。

**为什么不做注释支持？** 需要处理 `--`（行注释）和 `/* */`（块注释），还要处理"注释不能出现在字符串里"（`'-- not a comment'`）。这是**功能取舍**，不是遗漏。`DEMO.sql` 里没有注释行就是为了避开这一点。

> 汇报时如果被问到，可以坦率说："不支持的语法我们都明确报错，不会静默吞掉——这是有意的。" 并可以现场指出"如果要加，`Lexer.next()` 里加一个分支即可"。

## 4.4 AST：抽象语法树

目录：[`compiler/ast/`](src/main/java/com/course/dbms/compiler/ast/)，13 个类。

### `Stmt`：所有语句的基类

```java
public abstract class Stmt {
    // 标记基类
}
```

**空基类（marker base class）** —— 不给任何方法，只为类型系统服务：可以写 `List<Stmt>`，可以用 `instanceof` 分派。

### 语句类型一览

| 类 | 对应 SQL | 关键字段 |
|---|---|---|
| `CreateStmt` | `create table t (...)` | `tableName`, `columns`（`List<Column>`） |
| `CreateIndexStmt` | `create index i on t(c)` | `indexName`, `tableName`, `columnName` |
| `InsertStmt` | `insert into t values (...)` | `tableName`, `values`（`List<Object>` 字面量） |
| `SelectStmt` | `select ...` | 见下 |
| `ShowStmt` | `show tables/table/indexes` | `list`, `tableName`, `indexes` |
| `TxnStmt` | `begin/commit/rollback` | `op`（`TxOp` 枚举） |

都是 `public final` 字段 —— **AST 是不可变的数据载体**。建好就不改。

### `SelectStmt`：一个类装两条路径（重点）

文件：[`compiler/ast/SelectStmt.java`](src/main/java/com/course/dbms/compiler/ast/SelectStmt.java)。

```java
public class SelectStmt extends Stmt {
    // —— 旧路径（纯单表无特性）——
    public final boolean all;          // true = select *
    public final List<String> columns; // all=false 时的列名列表
    public final String tableName;     // 第一个表的表名
    public final Cond where;
    public final String orderBy;       // 裸列名
    public final boolean orderDesc;

    // —— rich 路径（多表 / 别名 / 限定列 / 聚合 / GROUP BY）——
    public final List<TableRef> from;
    public final List<Cond> joinOn;        // 长度 = from.size()-1
    public final List<Boolean> joinOuter;  // 每个 JOIN 是否 LEFT
    public final List<SelectItem> items;
    public final List<ColRef> groupBy;

    public boolean isRich() { ... }
}
```

> ★ 这是这个项目里**最值得讲的一个设计决策**。
>
> **背景**：单表查询功能先做，做完后加多表联查。如果重写，要动 `Analyzer`、`PlanBuilder` 里已经写好的、测试覆盖的单表路径——**回归风险高**。
>
> **做法**：`SelectStmt` 同时持有两套字段。`isRich()` 判断走哪条路：
>
> ```java
> public boolean isRich() {
>     if (from == null || from.isEmpty()) return false;
>     if (from.size() > 1) return true;                                    // 多表
>     if (from.get(0).alias != null) return true;                          // 有别名
>     if (joinOn != null && !joinOn.isEmpty()) return true;                // 有 JOIN
>     if (groupBy != null && !groupBy.isEmpty()) return true;              // 有 GROUP BY
>     if (items != null) {
>         for (SelectItem it : items) {
>             if (it.isAgg()) return true;                                 // 聚合函数
>             if (it.col != null && it.col.qualified()) return true;       // 限定列 u.id
>             if (it.alias != null) return true;                           // 有别名
>         }
>     }
>     return false;                                                        // 纯单表 → 旧路径
> }
> ```
>
> 于是：
> - `select id from users` → `isRich() == false` → 走**老代码**，行为逐字节不变
> - `select u.id from users u` → `isRich() == true` → 走**新代码**
>
> **代价**：一个类里有冗余字段，有点丑。
> **收益**：**零回归风险**。这也解释了为什么 `PlanBuilder` 里有两个方法 `buildSelect` 和 `buildSelectRich`——同样的思路在计划层复制了一遍。
>
> 汇报时主动讲这个取舍，会显得你对工程判断有理解。

### `Cond`：WHERE / ON 条件

文件：[`compiler/ast/Cond.java`](src/main/java/com/course/dbms/compiler/ast/Cond.java)。

```java
public enum Kind { CMP, AND, OR }

public final Kind kind;
public final String column;      // CMP 用：左列名
public final String op;          // CMP 用：= <> < > <= >=
public final Object value;       // CMP 用：右值（列 vs 列时为 null）
public final List<Cond> children;// AND/OR 用
public final String qualifier;   // CMP：左列限定符（可空）
public final String column2;     // CMP 列-列：右列名（null = 跟字面量比）
public final String qualifier2;  // CMP 列-列：右列限定符
```

**两个静态工厂方法**：

```java
public static Cond cmp(String column, String op, Object value)                    // 裸列 vs 字面量
public static Cond cmp(String qualifier, String column, String op, Object value)  // 限定列 vs 字面量
public static Cond cmp2(String q1, String col1, String op, String q2, String col2)// 列 vs 列
public static Cond and(List<Cond> cs)
public static Cond or(List<Cond> cs)
```

**为什么 `CMP` 既表示"列 vs 字面量"又表示"列 vs 列"？**

用 `column2 != null` 区分。这样 `CondEval` 里只有一个 CMP 分支：

```java
// CondEval.java:26
case CMP: {
    int i1 = r.resolve(c.qualifier, c.column);
    Object v1 = row.get(i1);
    if (c.column2 != null) {                                  // 列 vs 列
        int i2 = r.resolve(c.qualifier2, c.column2);
        return Compare.apply(c.op, v1, row.get(i2));
    }
    // 列 vs 字面量：把字面量 cast 到左列类型，保证比较口径一致
    Object lit = StorageEngine.cast(castSchema, i1, c.value);
    return Compare.apply(c.op, v1, lit);
}
```

替代方案是分两个 Kind（`CMP_LITERAL` / `CMP_COLUMN`），但那样 `PlanBuilder`、`Analyzer`、`CondEval` 都要写两遍。用可空字段换代码量。

### `SelectItem`：SELECT 列表的一项

```java
public enum Kind { COL, AGG }

public final Kind kind;
public final ColRef col;    // COL 用
public final String func;   // AGG 用：count/sum/avg/min/max
public final ColRef arg;    // AGG 用：参数列（COUNT(*) 时为 null）
public final String alias;  // 输出别名（可空）

public static SelectItem col(ColRef col, String alias)
public static SelectItem agg(String func, ColRef arg, String alias)

public String outputName() {
    if (alias != null) return alias;
    return kind == Kind.AGG ? func : col.name;   // 无别名时的默认名
}
```

**`outputName()` 决定了最终结果的列头**：
- `count(*) as cnt` → `cnt`
- `count(*)` → `count`
- `u.name` → `name`（注意：不带限定符）

## 4.5 Parser：递归下降语法分析

文件：[`compiler/Parser.java`](src/main/java/com/course/dbms/compiler/Parser.java)，320 行。**这是项目里最长的类之一**。

### 完整的文法（注释里就有）

```
stmt     := create | insert | select | show
create   := CREATE TABLE IDENT '(' colDef (',' colDef)* ')'
          | CREATE INDEX IDENT ON IDENT '(' IDENT ')'
colDef   := IDENT TYPE      (TYPE ∈ int32/int64/float64/bool/string/datetime)
insert   := INSERT INTO IDENT VALUES '(' value (',' value)* ')'
select   := SELECT '*' | selItem (',' selItem)*
           FROM table (join)* [WHERE cond] [GROUP BY colRef (',' colRef)*]
           [ORDER BY orderKey [ASC|DESC]]
selItem  := colRef [AS IDENT] | aggFunc '(' [*|colRef] ')' [AS IDENT]
table    := IDENT [AS IDENT]                （隐式别名 IDENT 亦可）
join     := [INNER|LEFT] JOIN table [ON cond]
orderKey := colRef | aggFunc '(' [*|colRef] ')'   （聚合排序只取函数名）
colRef   := IDENT | IDENT '.' IDENT
cond     := cmp (OR cmp)* ; cmp := cmp' (AND cmp')* ; cmp' := colRef op (colRef|value)
show     := SHOW TABLES | SHOW TABLE IDENT | SHOW INDEX[ES] [ON IDENT]
```

**注意 `cond` 那一行的层次**：`cond → or → and → cmp`。这决定了**优先级**：`OR` 最低、`AND` 其次、比较最高。

所以 `a = 1 or b = 2 and c = 3` 解析成 `a=1 OR (b=2 AND c=3)` —— 跟所有编程语言的约定一致。

### 递归下降的骨架

```java
private final List<Token> tokens;
private int pos = 0;

public Stmt parse() {
    Stmt s = parseStatement();
    while (peek().type == TokenType.SEMICOLON) next();   // 容忍句尾多个分号
    expect(TokenType.EOF);                               // 必须走到底
    return s;
}
```

**四个工具方法构成整个解析器的基础**：

```java
// Parser.java:302
private Token peek() { return tokens.get(pos); }                    // 看一眼，不消费
private Token next() { Token t = tokens.get(pos); pos++; return t; } // 消费一个
private boolean match(TokenType tt) {                               // 若匹配则消费，否则不动
    if (peek().type == tt) { pos++; return true; }
    return false;
}
private Token expect(TokenType tt) {                                // 必须匹配，否则报错
    if (peek().type != tt) throw err(peek(), "expected " + tt + " but got " + peek().text);
    return next();
}
private String expectIdent() { return (String) expect(TokenType.IDENTIFIER).value; }
```

> ★ **这就是递归下降解析器的全部秘密**：
> - `match(X)` = "如果有 X 就吃掉" —— 处理**可选**部分
> - `expect(X)` = "必须是 X" —— 处理**必需**部分
> - `peek()` vs `next()` = "看" vs "拿" —— LL(1) 需要向前看一个 token
>
> 有了这五个方法，写语法规则就跟**抄文法**一样直接。

### 例子：`parseSelect` 逐段拆解

```java
// Parser.java:136
private Stmt parseSelect() {
    expect(TokenType.SELECT);

    // ① SELECT 列表
    boolean all = false;
    List<String> cols = new ArrayList<>();
    List<SelectItem> items = new ArrayList<>();
    if (match(TokenType.STAR)) {
        all = true;                                    // select *
    } else {
        do {
            SelectItem it = parseSelectItem();
            items.add(it);
            if (it.isAgg()) cols.add(it.alias != null ? it.alias : it.func);  // 旧路径用的裸列名
            else cols.add(it.col.name);
        } while (match(TokenType.COMMA));
    }

    // ② FROM
    expect(TokenType.FROM);
    List<TableRef> from = new ArrayList<>();
    from.add(tableRef());                              // 首表 + 可选别名

    // ③ JOIN 链
    List<Cond> joinOn = new ArrayList<>();
    List<Boolean> joinOuter = new ArrayList<>();
    while (true) {
        boolean left = false;
        if (match(TokenType.LEFT)) { left = true; expect(TokenType.JOIN); }
        else if (match(TokenType.INNER)) { expect(TokenType.JOIN); }
        else if (match(TokenType.JOIN)) { /* 裸 JOIN = 内连接 */ }
        else break;                                     // 不是 JOIN，退出循环
        from.add(tableRef());
        Cond on = match(TokenType.ON) ? parseCond() : null;
        if (left && on == null) throw err(peek(), "LEFT JOIN 需要 ON 条件");
        joinOn.add(on);
        joinOuter.add(Boolean.valueOf(left));
    }

    // ④ WHERE / GROUP BY / ORDER BY
    Cond where = null;
    if (match(TokenType.WHERE)) where = parseCond();
    List<ColRef> groupBy = new ArrayList<>();
    if (match(TokenType.GROUP)) {
        expect(TokenType.BY);
        do { groupBy.add(parseColRef()); } while (match(TokenType.COMMA));
    }
    String orderBy = null;
    boolean desc = false;
    if (match(TokenType.ORDER)) {
        expect(TokenType.BY);
        orderBy = parseOrderKey();
        if (match(TokenType.ASC)) { /* 默认升序 */ }
        else if (match(TokenType.DESC)) desc = true;
    }

    return new SelectStmt(all, cols, from.get(0).name, where, orderBy, desc,
            from, joinOn, joinOuter, items, groupBy.isEmpty() ? null : groupBy);
}
```

**几个值得注意的点**：

**① `do { ... } while (match(COMMA))`** —— "至少一个，逗号分隔"的标准写法。SELECT 列表、FROM 的 JOIN 链、GROUP BY 列表都用它。

**② `from.get(0).name` 同时填了旧字段 `tableName`** —— 即使走 rich 路径，旧字段也有值。这让"rich 路径下的单表"仍然能引用 `tableName`（虽然实际用的是 `from`）。

**③ `LEFT JOIN` 必须带 `ON`**

```java
if (left && on == null) throw err(peek(), "LEFT JOIN 需要 ON 条件");
```

因为 `left join` 不带 `ON` 等价于笛卡尔积，语义上没意义（虽然 SQL 标准允许）。这是**主动收紧语法**。

**④ `MATCH` 的顺序很重要（`LEFT` 必须在裸 `JOIN` 之前）**

```java
if (match(TokenType.LEFT)) ...
else if (match(TokenType.INNER)) ...
else if (match(TokenType.JOIN)) ...
```

如果先判 `JOIN`，那 `left join` 会在遇到 `left` 时直接 `break`（既不是 LEFT 也不是 JOIN？不，`match(LEFT)` 会失败……）。实际上顺序对了才工作正常：`match(LEFT)` 先尝试，成功就继续 `expect(JOIN)`。

**⑤ `orderBy` 用一个 `String` 而不是 `ColRef`**

`parseOrderKey()` 把它拍平成字符串（`"amount"` / `"u.amount"` / `"count"`）：

```java
// Parser.java:231
private String parseOrderKey() {
    String name = expectIdent();
    if (match(TokenType.DOT)) name = name + "." + expectIdent();
    if (match(TokenType.LPAREN)) {                   // count(*) 形式
        if (match(TokenType.STAR)) { /* 忽略参数 */ }
        else expectIdent();
        expect(TokenType.RPAREN);
    }
    return name;
}
```

所以 `order by count(*)` → `"count"`，`order by u.name` → `"u.name"`。

**代价**：后面要**再解析一次**这个字符串（`PlanBuilder.resolveOrder` 里有 `key.indexOf('.')`）。**收益**：`SelectStmt.orderBy` 一直是个简单 String，旧路径不需要改。

**这就是"沿已有结构扩展"的典型手法**——不动原有的类型，把新语法"塞进"旧字段。

### `parseCreate`：一句 `if` 扩展出一条新语句

```java
// Parser.java:94
private Stmt parseCreate() {
    expect(TokenType.CREATE);
    if (match(TokenType.INDEX)) return parseCreateIndex();     // ← 新增这一行
    expect(TokenType.TABLE);
    ...
}

// Parser.java:112
private Stmt parseCreateIndex() {
    String indexName = expectIdent();
    expect(TokenType.ON);
    String table = expectIdent();
    expect(TokenType.LPAREN);
    String column = expectIdent();
    expect(TokenType.RPAREN);
    return new CreateIndexStmt(indexName, table, column);
}
```

**加一个 B+ 树索引功能，`Parser` 只动了 2 行**（1 行分派 + 1 个新方法）。这是因为原设计已经按"关键字分派"组织好了。

### 错误处理

```java
private Error err(Token t, String msg) {
    return new Error("SY-0001", msg + " at " + t.pos);
}
```

**所有语法错误都是 `SY-0001`**，后面跟不同消息。这样测试可以直接 `assertTrue(e.code().startsWith("SY"))`。

### 第 4.5 节自问自答

**Q：为什么不用 ANTLR / JavaCC 生成解析器？**
A：零依赖约束。而且手写递归下降的代码**可读性更高**——一个语法规则对应一个方法，直接对照文法。生成器产出的代码是几百行状态表，没法讲。

**Q：LL(1) 需要向前看几个 token？**
A：本项目所有规则都是 LL(1)（看一个 token 就能决定），**除了** `parseSelectItem` 里判断"是聚合函数还是普通列"：

```java
// Parser.java:194
String first = expectIdent();
if (peek().type == TokenType.LPAREN) { /* 聚合函数 */ }
```

这里看的是**下一个** token（`IDENT` 之后是不是 `(`）。但这是 LL(1) 的一点变体——因为已经消费了 `IDENT`，所以实际是"看第二个 token"。仍然只需要 1 个前瞻缓冲。

**Q：如果 SQL 有语法错误，会解析到哪一步？**
A：`throw` 立即中断，不会把后面的解析完。错误信息里有字符偏移，便于定位。

## 4.6 Analyzer：语义分析

文件：[`compiler/Analyzer.java`](src/main/java/com/course/dbms/compiler/Analyzer.java)，263 行。

**核心原则**（类注释里写明）：**"以 AST + 系统目录为依据，做静态校验（不落地、不改数据）"**。

这是个重要的性质：**Analyzer 是只读的**。调多少次都不会改变任何状态。这让它非常好测试（`AnalyzerTest` 里可以反复 `ok()` 而不用担心副作用）。

### 分派

```java
// Analyzer.java:48
public void analyze(Stmt stmt) {
    if (stmt instanceof CreateStmt) analyzeCreate((CreateStmt) stmt);
    else if (stmt instanceof CreateIndexStmt) analyzeCreateIndex((CreateIndexStmt) stmt);
    else if (stmt instanceof InsertStmt) analyzeInsert((InsertStmt) stmt);
    else if (stmt instanceof SelectStmt) analyzeSelect((SelectStmt) stmt);
    else if (stmt instanceof ShowStmt) analyzeShow((ShowStmt) stmt);
    else if (stmt instanceof TxnStmt) { /* 事务控制语句由 Session 拦截，无静态校验 */ }
    else throw new Error("SE-0000", "unknown statement: " + stmt);
}
```

### 检查清单（按语句类型）

| 语句 | 检查什么 | 错误码 |
|---|---|---|
| **CREATE TABLE** | 至少一列；列名不重复 | `SE-0001` / `SE-0002` |
| | 类型名合法 | `SE-0002`（在 `FieldType.fromName`） |
| | 表名唯一 | `CT-0002`（在 `Catalog.createTable`，落地时才查） |
| **CREATE INDEX** | 表存在 | `TB-0001` |
| | 列存在 | `SE-0004` |
| | 索引名未被占用 | `SE-0007` |
| **INSERT** | 表存在 | `TB-0001` |
| | 值个数 == 列数 | `SE-0003` |
| | 每个值能 cast 到列类型 | `SE-0005` |
| **SELECT**（单表） | 表存在 | `TB-0001` |
| | SELECT 列存在 | `SE-0004` |
| | WHERE 列存在、字面量能 cast | `SE-0004` / `SE-0005` |
| | ORDER BY 列存在 | `SE-0004` |
| **SELECT**（rich） | 以上 + 别名不重复、列引用无歧义、ON 只引用到当前为止的表、分组规则 | `SE-0004` / `SE-0006` |
| **SHOW** | `show table x` 的 x 必须存在；`show indexes on x` 的 x 必须存在 | `TB-0001` |

### 为什么"表名唯一"不在 Analyzer 里查？

```java
// Analyzer.java:58
private void analyzeCreate(CreateStmt c) {
    if (c.columns.isEmpty()) throw new Error("SE-0001", "表至少需要一列");
    Set<String> seen = new HashSet<>();
    for (Column col : c.columns) {
        if (!seen.add(col.name().toLowerCase())) {
            throw new Error("SE-0002", "列名重复: " + col.name());
        }
    }
}
```

**注意它根本没查表名是否重复**——那是 `Catalog.createTable` 做的（抛 `CT-0002`）。

**为什么分两处？** 因为**判重的权威在"落地处"**。Analyzer 只是"静态预检"，真正的唯一性约束必须由持数据的那一方保证（否则并发场景下预检通过、落地冲突）。

> ★ 这是一个**通用原则**：约束检查要放在"唯一权威的写入点"，而不是分散在各处预检。预检是为了**快速失败 + 友好报错**，不是为了保证正确性。

### `analyzeInsert`：类型检查靠"试 cast"

```java
// Analyzer.java:84
private void analyzeInsert(InsertStmt ins) {
    Table table = catalog.getTable(ins.tableName);       // TB-0001 if missing
    Schema schema = table.schema();
    if (ins.values.size() != schema.columnCount()) {
        throw new Error("SE-0003", "值个数与列数不一致: 列 " + schema.columnCount()
                + " 个, 给了 " + ins.values.size() + " 个");
    }
    for (int i = 0; i < schema.columnCount(); i++) {
        StorageEngine.cast(schema, i, ins.values.get(i)); // SE-0005 on type mismatch
    }
}
```

**"试 cast"是个很聪明的做法**：不写"INT32 的合法输入是什么"的判断逻辑，而是**直接调用真正的转换方法**，看它抛不抛异常。

```java
// StorageEngine.cast —— 真正的转换
case INT32: return ((Number) v).intValue();    // 非 Number → ClassCastException → 包成 SE-0005
```

好处：**分析与执行用的是同一套类型规则**，不可能出现"分析说合法、执行却失败"。这叫**单一真相来源**（single source of truth）。

**代价**：做了两次 cast（分析一次、计划一次）。但 cast 是纯计算，没有副作用，这点开销可忽略。

### `analyzeSelectRich`：rich 路径的校验

这个方法有 60 多行，是最复杂的。核心是**用 `CombinedSchema` 逐步构建"可引用的列集合"**：

```java
// Analyzer.java:108
private void analyzeSelectRich(SelectStmt sel) {
    List<TableRef> from = sel.from;
    List<Table> tables = new ArrayList<>();
    Set<String> eff = new HashSet<>();

    // ① 逐表检查存在性 + 别名唯一
    for (TableRef tr : from) {
        Table t = catalog.getTable(tr.name);              // TB-0001 if missing
        tables.add(t);
        if (!eff.add(tr.effective().toLowerCase())) {
            throw new Error("SE-0004", "表/别名重复: " + tr.effective());
        }
    }

    // ② 逐表接入 CombinedSchema，并对每个 JOIN 的 ON 校验
    CombinedSchema cs = new CombinedSchema();
    for (int k = 0; k < from.size(); k++) {
        Table t = tables.get(k);
        String q = from.get(k).effective();
        for (int c = 0; c < t.schema().columnCount(); c++) {
            cs.add(q, t.schema().column(c).name(), t.schema().column(c).type());
        }
        if (k > 0) {
            Cond on = sel.joinOn.get(k - 1);
            if (on != null) checkCondRich(cs, on);        // ← ON 只能引用"到 k 为止"的表
        }
    }
    // ③ SELECT 项 / WHERE / GROUP BY / ORDER BY / 分组规则 ...
}
```

**注意 ② 里的时序**：`ON` 的校验发生在"第 k 张表刚加进 `cs`、第 k+1 张还没加"的时刻。所以

```sql
select * from a join b on a.id = c.id join c on ...
```

会在第二个 JOIN 处报 `c` 不存在——因为在校验 `a join b` 的 ON 时，`c` 还没加进来。**这正确地防止了"引用后面才出现的表"**。

### 分组规则检查

```java
// Analyzer.java:159
if (grouped) {
    for (SelectItem it : sel.items) {
        if (it.isAgg()) continue;
        if (!inGroupBy(sel.groupBy, it.col)) {
            throw new Error("SE-0006", "非聚合列 " + it.col.display() + " 必须在 GROUP BY 中");
        }
    }
}
```

**这实现的是 SQL 标准的"分组规则"**：

```sql
select name, count(*) from users group by age      -- ❌ 报 SE-0006
```

因为 `name` 不是分组键，一组里可能有多个不同的 `name`——数据库不知道该输出哪一个。标准做法就是**强制要求**：非聚合列必须出现在 GROUP BY 里。

```sql
select age, count(*) from users group by age       -- ✅
select count(*) from users                          -- ✅ 全局聚合，没有非聚合列
select u.name, avg(o.amount) from ... group by u.name  -- ✅
```

### `PL-0002`：事务语句不该走到这里

```java
// PlanBuilder.java:71
if (stmt instanceof TxnStmt) {
    throw new Error("PL-0002", "transaction control is handled by Session, not the plan");
}
```

**这是一个"防御性断言"**。理论上 `TxnStmt` 在 `Session.execute` 就被拦截了，不会到 `PlanBuilder`。但如果有人直接调 `Database.execute("begin")`（跳过 Session），就会在这里得到一个**明确的**错误，而不是"unknown statement"这种模糊信息。

## 4.7 PlanBuilder：执行计划生成

文件：[`compiler/PlanBuilder.java`](src/main/java/com/course/dbms/compiler/PlanBuilder.java)，345 行。**项目里最长的类**，因为它是"所有 SQL 特性最终汇聚成算子"的地方。

### 总入口

```java
// PlanBuilder.java:65
public Plan build(Stmt stmt) {
    if (stmt instanceof CreateStmt)      return buildCreate((CreateStmt) stmt);
    if (stmt instanceof CreateIndexStmt) return buildCreateIndex((CreateIndexStmt) stmt);
    if (stmt instanceof InsertStmt)      return buildInsert((InsertStmt) stmt);
    if (stmt instanceof SelectStmt)      return buildSelect((SelectStmt) stmt);
    if (stmt instanceof ShowStmt)        return buildShow((ShowStmt) stmt);
    if (stmt instanceof TxnStmt) throw new Error("PL-0002", "...");
    throw new Error("PL-0001", "unknown statement: " + stmt);
}
```

### 单表路径 `buildSelect`

```java
// PlanBuilder.java:101
private Plan buildSelect(SelectStmt sel) {
    if (sel.isRich()) return buildSelectRich(sel);       // ← 分流
    Table table = catalog.getTable(sel.tableName);
    Schema schema = table.schema();

    // ★ 物理计划选择：WHERE 的某个合取项上若有索引 → IndexScan，否则 SeqScan
    Operator root = indexScanFor(table, sel.where);
    if (root == null) root = new SeqScan(se, table);
    if (sel.where != null) root = new Filter(root, schema, sel.where);

    // 算投影列
    int[] cols;
    List<String> names = new ArrayList<>();
    if (sel.all) {
        cols = new int[schema.columnCount()];
        for (int i = 0; i < schema.columnCount(); i++) {
            cols[i] = i;
            names.add(schema.column(i).name());
        }
    } else {
        cols = new int[sel.columns.size()];
        for (int i = 0; i < sel.columns.size(); i++) {
            cols[i] = schema.indexOf(sel.columns.get(i));       // 列名 → 下标
            names.add(schema.column(cols[i]).name());
        }
    }

    // ORDER BY：注意在投影"之前"
    if (sel.orderBy != null) {
        int orderIdx = schema.indexOf(sel.orderBy);
        root = new Sort(root, orderIdx, sel.orderDesc);
    }

    root = new Project(root, cols);                          // 投影在最外
    return new Plan(root, names);
}
```

**树的形状是自下而上"包"出来的**：

```
root = SeqScan
root = Filter(root)      如果 where != null
root = Sort(root)        如果 order by != null
root = Project(root)     总是
```

所以 `execute()` 的调用顺序是 `Project → Sort → Filter → SeqScan` —— **数据流从下往上**，跟建树的顺序反过来。

**★ 为什么 `Sort` 在 `Project` 之前（下面）？**

因为排序键可能**不在** SELECT 列表里：

```sql
select name from users order by age desc
```

`Project` 只输出 `[name]`，如果先投影再排序，`age` 已经没了，排不了。

所以顺序必须是 `Project(Sort(Filter(Scan)))`——**先按源列排序，再投影掉不需要的列**。

> 这是 SQL 执行计划的一个经典问题。真正的优化器会判断"排序键是否在投影列表里"，如果在就交换两者（减少排序的数据宽度）。本项目**一律先排序**——简单且永远正确，代价是排序的数据更宽。

**`sel.orderBy` 到 `schema.indexOf` 的映射**：旧路径里 `orderBy` 是裸列名，直接用 `schema.indexOf` 找到源列下标。

### rich 路径 `buildSelectRich`

```java
// PlanBuilder.java:142
private Plan buildSelectRich(SelectStmt sel) {
    // ① 构建 CombinedSchema
    List<TableRef> from = sel.from;
    List<Table> tables = new ArrayList<>();
    CombinedSchema cs = new CombinedSchema();
    for (TableRef tr : from) {
        Table t = catalog.getTable(tr.name);
        tables.add(t);
        String q = tr.effective();                    // 别名优先，否则表名
        for (int c = 0; c < t.schema().columnCount(); c++) {
            cs.add(q, t.schema().column(c).name(), t.schema().column(c).type());
        }
    }
    final CombinedSchema csF = cs;                    // 有效 final，供 lambda 用
    CondEval.Resolver resolver = csF::resolve;        // 方法引用：列引用 → 行下标

    // ② 左深连接链
    Operator root = new SeqScan(se, tables.get(0));
    int leftWidth = tables.get(0).schema().columnCount();
    for (int k = 1; k < tables.size(); k++) {
        int rightWidth = tables.get(k).schema().columnCount();
        root = new Join(root, new SeqScan(se, tables.get(k)),
                sel.joinOn.get(k - 1), csF, resolver,
                sel.joinOuter.get(k - 1), leftWidth, rightWidth);
        leftWidth += rightWidth;
    }
    if (sel.where != null) root = new Filter(root, csF.toSchema(), resolver, sel.where);
    ...
}
```

**① `leftWidth` 的维护** —— 每个 `Join` 需要知道"左行有多宽"（用于 LEFT JOIN 时把右列填 null）。

```
第一次迭代：leftWidth = |a|，join(a, b) → 新左宽 = |a| + |b|
第二次迭代：leftWidth = |a|+|b|，join((a⋈b), c) → 新左宽 = |a|+|b|+|c|
```

**② 为什么叫"左深"（left-deep）？**

```
      Join_2
     /      \
  Join_1     c
 /      \
a        b
```

所有右孩子都是**叶子**（`SeqScan`），左孩子是累积的树。这跟"右深"（`a ⋈ (b ⋈ (c ⋈ d))`）和"浓密树"（bushy）是相对的。

**左深的优点**：中间结果可以在流水线里逐层处理，不需要物化多棵子树。
**本项目的实现**：`Join.execute()` 先把左边全部算完（`child.execute()`），再把右边全部算完（`right.execute()`），然后双重循环。**所以其实是全物化的嵌套循环，不是真正的左深流水线**。但对课程规模的数据量没影响。

**③ 聚合分支**

```java
// PlanBuilder.java:171
boolean hasAgg = hasAggregate(sel.items);
boolean hasGroup = sel.groupBy != null && !sel.groupBy.isEmpty();

if (hasAgg || hasGroup) {
    int[] groupIdx = groupIndices(sel.groupBy, csF);
    List<Aggregate.Item> aggItems = new ArrayList<>();
    List<String> names = new ArrayList<>();
    for (SelectItem it : sel.items) {
        if (it.isAgg()) {
            int argIdx = -1;
            FieldType argType = null;
            if (it.arg != null) {
                argIdx = csF.resolve(it.arg.qualifier, it.arg.name);
                argType = csF.typeAt(argIdx);
            }
            aggItems.add(new Aggregate.Item(true, -1, it.func, argIdx, it.arg == null, argType));
            names.add(it.outputName());
        } else {
            int gp = groupKeyPos(sel.groupBy, csF, it.col);   // 非聚合列在分组键里的位次
            aggItems.add(new Aggregate.Item(false, gp, null, -1, false, null));
            names.add(it.outputName());
        }
    }
    root = new Aggregate(root, groupIdx, aggItems);
    if (sel.orderBy != null) {
        int oi = aggOutputIndex(sel.items, sel.orderBy);
        if (oi < 0) throw new Error("SE-0004", "ORDER BY 列不存在: " + sel.orderBy);
        root = new Sort(root, oi, sel.orderDesc);              // 排序在 Aggregate 之上
    }
    return new Plan(root, names);
}
```

**注意 `Sort` 的位置变了**：聚合查询里 `Sort` 在 `Aggregate` **之上**（因为排序键是聚合输出列，必须先聚合出来才能排）。

对比非聚合路径：`Sort` 在 `Project` **之下**。**同一个算子在两棵树里位置相反**——这是数据依赖决定的，不能死记。

**④ 非聚合分支的排序位置**

```java
// PlanBuilder.java:217
if (sel.orderBy != null) {
    int oi = outputIndex(names, sel.orderBy);
    if (oi >= 0) {                       // 排序键就是输出列（别名/列名）
        root = new Project(root, projCols);
        root = new Sort(root, oi, sel.orderDesc);       // Project 在下面
    } else {                             // 排序键是源列，须在投影前排序
        root = new Sort(root, resolveOrder(csF, sel.orderBy), sel.orderDesc);
        root = new Project(root, projCols);             // Project 在上面
    }
}
```

**这里比单表路径聪明**：它**判断排序键是不是输出列**，如果是就投影后再排（数据更窄），否则只能排完再投影。

例：`select u.name as n from users u order by n` → 排序键 `n` 是别名，属于输出列 → `Sort(Project(...))`
例：`select u.name from users u order by u.age` → 排序键 `u.age` 不是输出列 → `Project(Sort(...))`

### ★★★ `indexScanFor`：物理计划选择（B+ 树索引的入口）

这是整个索引功能在规划器里的全部代码，**66 行**：

```java
// PlanBuilder.java:242
private Operator indexScanFor(Table table, Cond where) {
    if (where == null) return null;

    List<Cond> conjuncts = new ArrayList<>();
    collectConjuncts(where, conjuncts);                   // ① 提取合取项

    for (Cond c : conjuncts) {
        if (c.column2 != null) continue;                  // ② 列 vs 列，与索引无关
        if (!isRangeOp(c.op)) continue;                   // ③ <> 用不上 B+ 树的范围性
        Index idx = catalog.indexManager().findFor(table, c.column);
        if (idx == null) continue;                        // ④ 该列没建索引

        Object v = StorageEngine.cast(table.schema(), idx.colIndex(), c.value);
        Object lo = null, hi = null;
        boolean loInc = false, hiInc = false;
        switch (c.op) {                                   // ⑤ 运算符 → 扫描区间
            case "=":  lo = v; loInc = true;  hi = v; hiInc = true;  break;
            case ">":  lo = v; loInc = false;                        break;
            case ">=": lo = v; loInc = true;                         break;
            case "<":  hi = v; hiInc = false;                        break;
            case "<=": hi = v; hiInc = true;                         break;
            default:   continue;
        }
        return new IndexScan(se, table, idx.tree(), idx.colIndex(), lo, loInc, hi, hiInc);
    }
    return null;                                          // 没有可用索引 → 调用方回退 SeqScan
}
```

**① 提取合取项**

```java
// PlanBuilder.java:269
private void collectConjuncts(Cond cond, List<Cond> out) {
    if (cond == null) return;
    if (cond.kind == Cond.Kind.CMP) {
        out.add(cond);
    } else if (cond.kind == Cond.Kind.AND && cond.children != null) {
        for (Cond child : cond.children) collectConjuncts(child, out);
    }
    // 注意：没有 OR 分支 —— OR 整体被跳过
}
```

**"合取项"（conjunct）= AND 连接起来的每一个子条件。**

`where a = 1 and b > 2 and c < 3` 的合取项是 `[a=1, b>2, c<3]`。

`and` 会递归展开，`or` **完全不进入**。为什么？

**这就是索引能不能用的核心判断**：

| WHERE 子句 | 能用索引吗 | 为什么 |
|---|---|---|
| `age > 22` | ✅ | 顶层就是 CMP |
| `age > 22 and score > 90` | ✅ | 可以只用 `age > 22` 缩小范围，剩下的交给上层的 Filter |
| `age > 22 or score > 90` | ❌ | **不能用 `age` 的索引**——因为不满足 `age > 22` 的行只要 `score > 90` 也应该输出 |
| `not`（不支持） | — | — |

> ★ **这是索引使用上最容易错的地方，也是最值得在汇报里讲的点**：
>
> `where age > 22` 用索引取 `[22, +∞)` 的 RID —— 完全正确。
>
> 但 `where age > 22 or score > 90` 如果只用 `age` 的索引取 `[22, +∞)`，就会**漏掉**那些 `age <= 22` 但 `score > 90` 的行。**结果是错的**。
>
> 想用 OR 上索引需要"两边的索引结果做并集"（union），复杂度高得多。所以本项目**保守处理**：遇到 OR 整个退回全表扫描。

**③ `isRangeOp`**

```java
// PlanBuilder.java:279
private boolean isRangeOp(String op) {
    return op.equals("=") || op.equals("<") || op.equals(">")
            || op.equals("<=") || op.equals(">=");
}
```

**为什么 `<>` 不在里面？**

B+ 树的优势是"**有序性**"——能快速定位一个**连续区间**。`<>` 对应的集合是 `(-∞, v) ∪ (v, +∞)`，是**两个不相交的区间**。用它上索引要么扫两遍再合并，要么扫全表。不如直接全表扫。

**⑤ 运算符 → 区间的映射**

这是**整个索引功能里最需要理解的一张表**：

| WHERE 写法 | lo | loInc | hi | hiInc | 树上的区间 |
|---|---|---|---|---|---|
| `age = 30` | `30` | `true` | `30` | `true` | `[30, 30]` 一个点 |
| `age > 22` | `22` | `false` | `null` | — | `(22, +∞)` |
| `age >= 22` | `22` | `true` | `null` | — | `[22, +∞)` |
| `age < 30` | `null` | — | `30` | `false` | `(-∞, 30)` |
| `age <= 30` | `null` | — | `30` | `true` | `(-∞, 30]` |

**`lo = null` / `hi = null` 表示"这一侧无界"** —— `BPlusTree.range` 里用这个判断：

```java
// BPlusTree.range
while (!n.leaf) n = n.children.get(lo == null ? 0 : upperBound(n, lo));
```

`lo == null` 时直接从**最左叶子**开始 —— 这是全树最小键所在的叶子。

**⑥ 为什么还要 `StorageEngine.cast`？**

因为 WHERE 里的 `22` 是 `Long`（字面量），而 `age` 列是 `INT32`（Java 的 `Integer`）。索引树里存的是**已经 cast 过的 `Integer`**。如果不 cast 就比，`Compare.compare(Long(22), Integer(30))` 会走 `Number` 分支按 double 比——结果碰巧对，但类型语义混乱。

**cast 到列类型再比**，保证"树里的键"和"查询的键"是同一种 Java 类型。

### `buildShow`：三种 SHOW

```java
// PlanBuilder.java:336
private Plan buildShow(ShowStmt s) {
    if (s.indexes) {
        return new Plan(new ShowIndexes(catalog, s.tableName), Arrays.asList("index", "table", "column"));
    }
    if (s.list) {
        return new Plan(new ShowTables(catalog), Arrays.asList("table", "columns"));
    }
    return new Plan(new ShowTable(catalog, s.tableName), Arrays.asList("column", "type"));
}
```

`Arrays.asList("index", "table", "column")` 直接定义输出列名 —— 因为这三个算子不经过 `Project`，列名由 `Plan.columns` 提供。

## 4.8 第 4 章自问自答

**Q：为什么 `PlanBuilder` 是最长的类？**
A：因为它是"所有 SQL 特性汇聚成算子"的地方。每加一个特性（多表、聚合、索引），它都要加分支。这是**自然的复杂度中心**。

**Q：语法分析器和语义分析器的分工界限在哪？**
A：**语法**关心"句子的形状对不对"（括号配对、关键字顺序），**不查目录**。**语义**关心"名字存不存在、类型对不对"，**查目录**。

例子：`create table t ()` —— 空列列表。`Parser` 的 `do-while` 要求至少一列，所以这是**语法错误** `SY-0001`（`AnalyzerTest.createEmptyColumnIsSyntaxError` 专门测了这个，断言 `startsWith("SY")` 而不是 `SE`）。

**Q：`Analyzer` 为什么是幂等的？**
A：因为它只读 `Catalog`、只做 `StorageEngine.cast`（纯函数），不写任何东西。所以可以安全地重复调用。

---

# 第 5 章 数据库引擎（engine）

课程图片"③ 数据库引擎"。核心是"CreateTable / Insert / SeqScan / Filter / Project 五算子"（图片上的），实际做了 13 个。

## 5.1 表结构层（engine/table）

### `FieldType`：6 种类型 + 编解码

文件：[`engine/table/FieldType.java`](src/main/java/com/course/dbms/engine/table/FieldType.java)，75 行。

```java
public enum FieldType {
    INT32(1), INT64(2), FLOAT64(3), BOOL(4), STRING(5), DATETIME(6);
    private final byte tag;
    public byte tag() { return tag; }
    public boolean variable() { return this == STRING; }
    public String sqlName() { return name().toLowerCase(); }
}
```

**四种表示法，各有用途**：

| 表示 | 用途 | 例（STRING） |
|---|---|---|
| **枚举常量** | Java 代码里用 | `FieldType.STRING` |
| **`tag` (byte)** | 存进 `sys_columns` 表 | `5` |
| **`sqlName()`** | SQL 里写、`show table` 输出 | `"string"` |
| **Java 值类型** | 内存里实际存的对象 | `String` |

**Java 值类型对照表**（类注释里写明）：

| FieldType | tag | Java 类型 | 编码字节数 |
|---|---|---|---|
| `INT32` | 1 | `Integer` | 4 |
| `INT64` | 2 | `Long` | 8 |
| `FLOAT64` | 3 | `Double` | 8 |
| `BOOL` | 4 | `Boolean` | 1 |
| `STRING` | 5 | `String` | 变长（UTF-8） |
| `DATETIME` | 6 | `Long`（毫秒时间戳） | 8 |

**`encode` / `decode`**

```java
public byte[] encode(Object v) {
    switch (this) {
        case INT32:   return ByteBuffer.allocate(4).putInt((Integer) v).array();
        case INT64:   return ByteBuffer.allocate(8).putLong((Long) v).array();
        case FLOAT64: return ByteBuffer.allocate(8).putDouble((Double) v).array();
        case BOOL:    return new byte[] { (byte) ((Boolean) v ? 1 : 0) };
        case STRING:  return ((String) v).getBytes(StandardCharsets.UTF_8);
        case DATETIME:return ByteBuffer.allocate(8).putLong((Long) v).array();
        default: throw Error.st("unknown type encode: " + this);
    }
}

public Object decode(byte[] buf, int off, int len) {
    switch (this) {
        case INT32:   return ByteBuffer.wrap(buf, off, 4).getInt();
        case INT64:   return ByteBuffer.wrap(buf, off, 8).getLong();
        ...
        case STRING:  return new String(buf, off, len, StandardCharsets.UTF_8);
        ...
    }
}
```

**`decode` 的 `len` 参数**：固定长度类型**忽略**它（直接用类型自己的长度，比如 INT32 → 4），变长的 `STRING` 才用它。这解释了为什么 `StorageEngine.rowFromBytes` 必须先读出 `len` 再调用 `decode`。

**`ByteBuffer.wrap(buf, off, 4).getInt()` 的陷阱**：`wrap(byte[], offset, length)` 的 offset 是**相对于数组开头**的绝对偏移，`getInt()` 从那里读。而 `ByteBuffer` 默认**大端序**——跟 `Page` 的手写位运算一致。

（如果写成 `ByteBuffer.wrap(buf).position(off).getInt()` 效果一样，但多一次方法调用。）

**`fromName` / `fromTag`**

```java
public static FieldType fromName(String name) {
    for (FieldType t : values()) {
        if (t.name().equalsIgnoreCase(name)) return t;
    }
    throw new Error("SE-0002", "unknown type: " + name);
}

public static FieldType fromTag(byte tag) {
    for (FieldType t : values()) {
        if (t.tag == tag) return t;
    }
    throw new Error("SE-0003", "unknown field tag: " + tag);
}
```

`fromName` 在 `Parser.parseCreate` 里被调用（`FieldType.fromName(typeName)`），所以 `create table t (a int33)` 会在**语法分析阶段**就抛 `SE-0002`。这有点打破"语法层不查语义"的原则，但类型名确实是词法的一部分——可以接受。

### `Column` / `Schema` / `Row` / `Table`

这四个类都很小，是纯粹的数据载体。

**`Column`**（21 行）：`name` + `type`，实现 `Serializable`。`toString()` 返回 `"name type"`（如 `"age int32"`）。

**`Schema`**（41 行）：有序的 `List<Column>`。

```java
public Schema add(String name, FieldType type) { columns.add(new Column(name, type)); return this; }
public int columnCount() { return columns.size(); }
public Column column(int i) { return columns.get(i); }
public int indexOf(String name) {                                   // 大小写不敏感
    for (int i = 0; i < columns.size(); i++) {
        if (columns.get(i).name().equalsIgnoreCase(name)) return i;
    }
    return -1;
}
public FieldType typeOf(String name) { ... }
```

**`add` 返回 `this`** —— 这是**流式 API**（fluent API）：

```java
// Catalog.java:45
this.sysTables = new Table(SYS_TABLES_ID, "sys_tables", new Schema()
        .add("table_id", FieldType.INT32)
        .add("name", FieldType.STRING));
```

比连着 `s.add(...); s.add(...);` 紧凑得多。

**`indexOf` 返回 -1 表示不存在**（而不是抛异常）—— 因为调用方经常要**判断**而不是**假设**（比如 `Analyzer.requireColumn` 要自己抛 `SE-0004`、`IndexManager.createIndex` 要返回 null）。

**`Row`**（26 行）：

```java
public class Row {
    private final List<Object> values;
    public Row(List<Object> values) { this.values = new ArrayList<>(values); }   // ← 防御性拷贝
    public static Row of(Object... vs) { ... }
    public int size() { return values.size(); }
    public Object get(int i) { return values.get(i); }
    public List<Object> values() { return values; }
}
```

> ★ **`Row` 没有 RID（行标识）字段** —— 这是索引功能实现时的一个重要发现。
>
> 真正的数据库里 `Row` 通常带一个 RID（`{pageNo, slot}`），这样索引可以直接指向行。本项目的 `Row` 只装值，位置信息丢失了。
>
> 后果：建索引时没法从 `Row` 拿到 RID。**解决方案**：在 `StorageEngine` 里加一个 `Located` 载体类（`{pageNo, slot, row}`）和 `scanLocated` 方法。见 §5.2。
>
> **如果重来会怎么设计？** 让 `Row` 带一个可空的 `Rid` 字段。但那样会改动所有创建 `Row` 的地方（`Row.of(...)` 有几十处调用），风险大——所以选了"加一个并行载体类"的路。

**防御性拷贝 `new ArrayList<>(values)`**：防止调用方之后修改传入的 List 影响到 `Row`。

**`Table`**（22 行）：`tableId` + `name` + `schema`。类注释明确："数据本身由 `StorageEngine` 管理"。

### `CombinedSchema`：多表拼接的逻辑列集合

文件：[`engine/table/CombinedSchema.java`](src/main/java/com/course/dbms/engine/table/CombinedSchema.java)，76 行。

**要解决的问题**：`a join b` 之后，结果行是 `[a 的列..., b 的列...]`。要能回答两个问题：

1. 引用 `b.x` 对应到拼接行的第几个位置？
2. 引用裸名 `x`（不带限定符）合法吗？—— 只有当 `x` 在所有表里唯一时才合法

**数据结构**：

```java
private static final class Bound {
    final String qualifier;   // 可空
    final String name;
    final FieldType type;
}

private final List<Bound> cols = new ArrayList<>();
private final Map<String, Integer> byQualified = new HashMap<>();   // "u.id" → 下标
private final Map<String, Integer> bareCount = new HashMap<>();     // "id" → 出现次数
```

**`add` 维护三份结构**：

```java
// CombinedSchema.java:34
public int add(String qualifier, String name, FieldType type) {
    int idx = cols.size();
    cols.add(new Bound(qualifier, name, type));
    if (qualifier != null) {
        byQualified.put((qualifier.toLowerCase() + "." + name.toLowerCase()), idx);
    }
    String bare = name.toLowerCase();
    Integer c = bareCount.get(bare);
    bareCount.put(bare, c == null ? 1 : c + 1);
    return idx;
}
```

**`resolve` 处理歧义**：

```java
// CombinedSchema.java:47
public int resolve(String qualifier, String name) {
    if (qualifier != null) {
        Integer i = byQualified.get(qualifier.toLowerCase() + "." + name.toLowerCase());
        if (i == null) throw new Error("SE-0004", "列不存在: " + qualifier + "." + name);
        return i;
    }
    // 裸名
    String bare = name.toLowerCase();
    Integer cnt = bareCount.get(bare);
    if (cnt == null) throw new Error("SE-0004", "列不存在: " + name);
    if (cnt > 1) throw new Error("SE-0004", "列名有歧义: " + name + "（请用 表名.列名 限定）");
    for (int i = 0; i < cols.size(); i++) {
        if (cols.get(i).name.equalsIgnoreCase(name)) return i;
    }
    throw new Error("SE-0004", "列不存在: " + name);
}
```

**限定名走 HashMap（O(1)），裸名走线性扫描（O(n)）** —— 因为限定名可以预先建索引，裸名涉及"统计出现次数"（必须在全部 `add` 完之后才知道）。

**大小写处理**：`byQualified` 和 `bareCount` 的 key 都用 `toLowerCase()`，所以 `U.ID` 和 `u.id` 等价。**这符合 SQL 的惯例**（标识符大小写不敏感）。

**`toSchema()`：给 Cast 用**

```java
// CombinedSchema.java:69
public Schema toSchema() {
    Schema s = new Schema();
    for (Bound b : cols) {
        s.add(b.qualifier != null ? b.qualifier + "." + b.name : b.name, b.type);
    }
    return s;
}
```

**为什么需要它？** `CondEval.eval` 里要把 WHERE 字面量 cast 到列类型：

```java
Object lit = StorageEngine.cast(castSchema, i1, c.value);
```

`StorageEngine.cast(Schema, int colIndex, Object)` 需要的是 **`Schema`**（按位置取类型），不是 `CombinedSchema`。

所以把 `CombinedSchema` 转成一个 `Schema`——**列名用 `qualifier.name` 的形式保证唯一**（`CombinedSchema` 里可能有两个表都有 `id` 列，转成 `Schema` 时叫 `u.id` 和 `o.id` 就不冲突了）。

**注意 `cast` 只按位置取类型**，不看名字——名字唯一只是为了不违反 `Schema` 的隐含约定。

## 5.2 StorageEngine：行与页的映射

文件：[`engine/storage/StorageEngine.java`](src/main/java/com/course/dbms/engine/storage/StorageEngine.java)，214 行。

**职责**：把 `Row`（内存对象）变成字节数组放进页，以及反过来。这是课程图片"③.2 存储引擎：行与页映射、磁盘组织"的对应实现。

### 记录格式

```
一条记录的字节布局：
[4字节 len][值0 字节][4字节 len][值1 字节][4字节 len][值2 字节] ...

- 每列一个"长度前缀 + 值字节"的单元
- 固定类型（INT32/INT64/FLOAT64/BOOL/DATETIME）：len 是固定值（4/8/8/1/8）
- STRING：len 是 UTF-8 实际字节数（变长）
```

> ★ 这个格式叫 **TLV（Type-Length-Value）的变体**——只有 LV（长度+值），类型由外部 `Schema` 提供。
>
> **为什么记录里不存类型？** 因为 `Schema` 已经知道了（存在 `sys_columns` 里）。存了就是冗余，而且浪费空间。
>
> **代价**：解码时必须**按正确的 `Schema`** 解。用错的 Schema 读会读出垃圾（或抛 IO 异常）。这是"紧凑 vs 自描述"的经典取舍——数据库都选紧凑。

### `cast`：类型规整

```java
// StorageEngine.java:38
public static Object cast(Schema schema, int colIndex, Object v) {
    FieldType type = schema.column(colIndex).type();
    try {
        switch (type) {
            case INT32:   return ((Number) v).intValue();
            case INT64:   return ((Number) v).longValue();
            case FLOAT64: return ((Number) v).doubleValue();
            case DATETIME:
                if (v instanceof String) return Long.parseLong((String) v);
                return ((Number) v).longValue();
            case BOOL:
                if (v instanceof Boolean) return v;
                return Boolean.parseBoolean(String.valueOf(v));
            case STRING:  return String.valueOf(v);
            default:      throw new Error("SE-0004", "unsupported type " + type);
        }
    } catch (ClassCastException | NumberFormatException e) {
        throw new Error("SE-0005", "插入值类型与列 " + schema.column(colIndex).name() + " 不匹配", e);
    }
}
```

**这是一个"静态方法"**（`static`）—— 因为它是**纯函数**，不依赖 `PageManager` 之类的实例状态。所以 `Analyzer` 能在不持有 `StorageEngine` 实例的情况下调用它（`Analyzer` 只有 `catalog` 字段）。

**几个转换细节**：

- `INT32`: `((Number) v).intValue()` —— 传入 `"abc"` 会抛 `ClassCastException`（`String` 不是 `Number`），被捕获转成 `SE-0005`
- `DATETIME`: 允许从**字符串**解析（`'1700000000000'`），也允许数字。这是唯一一个"从字符串转数字"的类型
- `BOOL`: `Boolean.parseBoolean("true")` → true，其他任何字符串 → false（**不报错**）。这是个宽松的取舍
- `STRING`: `String.valueOf(v)` —— 任何值都能转成字符串，**永远不会失败**

**`catch (ClassCastException | NumberFormatException e)`** —— Java 7 的**多重捕获**语法，比写两个 catch 块简洁。

**异常包装的价值**：把底层的 `ClassCastException` 转成带**业务语义**的 `SE-0005`，并带上**列名**。用户看到的是

```
✗ [SE-0005] 插入值类型与列 age 不匹配
```

而不是

```
java.lang.ClassCastException: java.lang.String cannot be cast to java.lang.Number
```

### `rowToBytes` / `rowFromBytes`

```java
// StorageEngine.java:60
public byte[] rowToBytes(Schema schema, Row row) {
    if (row.size() != schema.columnCount()) {
        throw new Error("SE-0006", "行值个数与列数不一致: " + row.size() + "!=" + schema.columnCount());
    }
    ByteArrayOutputStream bos = new ByteArrayOutputStream();
    DataOutputStream out = new DataOutputStream(bos);
    for (int i = 0; i < schema.columnCount(); i++) {
        Object v = cast(schema, i, row.get(i));       // ← 再 cast 一次（防御性）
        byte[] enc = schema.column(i).type().encode(v);
        out.writeInt(enc.length);                     // 长度前缀（大端）
        out.write(enc);
    }
    out.flush();
    return bos.toByteArray();
}

// StorageEngine.java:81
public Row rowFromBytes(Schema schema, byte[] data) {
    DataInputStream in = new DataInputStream(new ByteArrayInputStream(data));
    List<Object> values = new ArrayList<>();
    for (int i = 0; i < schema.columnCount(); i++) {
        int len = in.readInt();                       // 读长度
        byte[] buf = new byte[len];
        in.readFully(buf);                            // 读内容
        values.add(schema.column(i).type().decode(buf, 0, len));
    }
    return new Row(values);
}
```

**`rowToBytes` 里的 cast 是**防御性的第二道关卡 —— `PlanBuilder.buildInsert` 其实已经 cast 过了：

```java
// PlanBuilder.java:90
List<Object> vals = new ArrayList<>();
for (int i = 0; i < schema.columnCount(); i++) {
    vals.add(StorageEngine.cast(schema, i, ins.values.get(i)));
}
Operator op = new Insert(se, table, new Row(vals), catalog.indexManager());
```

**为什么重复 cast？** 因为 `StorageEngine.insert` 是个**公开 API**，可能被绕过 `PlanBuilder` 直接调用（测试里就这么干）。在 API 边界做校验是稳妥的做法。cast 是幂等的（`cast(cast(x)) == cast(x)`），所以重复无害。

**注意 `rowFromBytes` 不检查 `schema.columnCount()` 和实际字节数是否吻合** —— 如果字节在中间被截断，`readFully` 会抛 `EOFException`（被包成 `SE-0008`）。**这是有意的**：不吻合说明数据损坏，应该报错而不是静默容忍。

### `insert`：找页、放记录

```java
// StorageEngine.java:98
public int[] insert(Table table, Row row) {
    int tableId = table.tableId();
    byte[] rec = rowToBytes(table.schema(), row);
    int n = pm.pageCount(tableId);
    for (int pageNo = 0; pageNo < n; pageNo++) {                    // ① 顺序找有空位的页
        Page page = pm.getPage(tableId, pageNo);
        if (page == null) continue;
        if (page.freeSpace() >= rec.length + Page.SLOT_SIZE) {
            int slot = page.addRecord(rec);
            if (slot >= 0) {
                pm.persist(page);                                   // ② 落盘（或进工作集）
                return new int[] { pageNo, slot };                  // ③ 返回 RID
            }
        }
    }
    int newPageNo = pm.newPage(tableId);                            // ④ 都满了，开新页
    Page newPage = pm.getPage(tableId, newPageNo);
    int slot = newPage.addRecord(rec);
    pm.persist(newPage);
    return new int[] { newPageNo, slot };
}
```

**这是"顺序扫描找空位"（first-fit）策略** —— O(页数)。真正的数据库会维护**空闲空间表**（free space map，像 PostgreSQL 的 FSM）来 O(1) 定位有用空间的页。

**但对本项目不是问题**：因为 `PageManager.getPage` 走缓存，扫过的页都在内存里，只是遍历一个数组。

**注意返回的是 `int[] {pageNo, slot}`** —— 这就是 **RID**。索引功能靠它把索引项指回具体位置。

**`page.freeSpace() >= rec.length + Page.SLOT_SIZE`** —— 别忘了加槽位的 4 字节（跟 `Page.addRecord` 里的判断呼应）。

**`if (slot >= 0)` 的双重保险**：`freeSpace` 说够，`addRecord` 又说不够？理论上不会，但**防御性编程**——如果真发生（比如 future 改了 `freeSpace` 的算法），也不会写入错误位置，而是继续找下一页。

### `scan`：全表扫描

```java
// StorageEngine.java:121
public List<Row> scan(Table table) {
    List<Row> rows = new ArrayList<>();
    int tableId = table.tableId();
    int n = pm.pageCount(tableId);
    for (int pageNo = 0; pageNo < n; pageNo++) {
        Page page = pm.getPage(tableId, pageNo);
        if (page == null) continue;
        for (byte[] rec : page.records()) {
            rows.add(rowFromBytes(table.schema(), rec));
        }
    }
    return rows;
}
```

两层循环：**页 → 槽位**。这跟 `Page` 的物理组织直接对应（所以叫"页式存储"）。

**`pageCount` 在循环外取一次** —— 因为事务内每次调用 `pageCount` 都要算 `disk.pageCount + t.newCount`，循环内反复算没必要。

### `fetch`：按 RID 取一行（索引回表用）

```java
// StorageEngine.java:156
public Row fetch(Table table, int pageNo, int slot) {
    Page page = pm.getPage(table.tableId(), pageNo);
    if (page == null) return null;
    byte[] rec = page.getRecord(slot);
    if (rec == null) return null;
    return rowFromBytes(table.schema(), rec);
}
```

**这就是"回表"（index lookup → table fetch）的实现** —— 索引找到 RID，再用 RID 精确定位到页的某个槽位。

**注释里强调的一点**：

> 走 `PageManager`，因此事务内取的是工作集里的深拷贝 —— 索引扫描同样能读到自己的未提交数据。

这是**正确性的关键**：索引扫描也必须走 `PageManager`（而不是直接读磁盘），否则事务内的 `select ... where 索引列 = ?` 就看不到自己刚插入的行了。

**两处 `return null`** —— 页没了、槽位空了都返回 null。`IndexScan` 会跳过：

```java
Row r = se.fetch(table, rid[0], rid[1]);
if (r != null) out.add(r);                   // 失效 RID 跳过
```

### `scanLocated`：带 RID 的全表扫描（建索引用）

```java
// StorageEngine.java:165
public List<Located> scanLocated(Table table) {
    List<Located> out = new ArrayList<>();
    int tableId = table.tableId();
    int n = pm.pageCount(tableId);
    for (int pageNo = 0; pageNo < n; pageNo++) {
        Page page = pm.getPage(tableId, pageNo);
        if (page == null) continue;
        int count = page.recordCount();
        for (int slot = 0; slot < count; slot++) {          // ← 手工数槽位，不用 page.records()
            byte[] rec = page.getRecord(slot);
            if (rec == null) continue;
            out.add(new Located(pageNo, slot, rowFromBytes(table.schema(), rec)));
        }
    }
    return out;
}
```

**为什么不用现成的 `page.records()`？** 因为 `records()` 返回 `Iterable<byte[]>`——**不给槽位下标**。而索引需要 `(pageNo, slot)` 组成的 RID。

**所以这里手工用 `for (int slot = 0; slot < count; slot++)` 循环**，牺牲了 `records()` 的优雅换取位置信息。

> ★ 这暴露了 `Page.records()` 接口设计的一个局限：它只够"读全部记录"，不够"读全部记录 + 位置"。
>
> **更好的设计**：`records()` 返回 `Iterable<Record>`，其中 `Record` 带 `slot` 字段。但那样要改 `Page` 的公开接口，影响 `scan` 和 `iterate`。所以选了"加一个并行方法"。
>
> 这就跟 `Row` 不带 RID 是同一个问题的两面。

### `iterate`：流式扫描（未被算子使用）

```java
// StorageEngine.java:183
public Iterable<Row> iterate(Table table) {
    return () -> new java.util.Iterator<Row>() {
        private final int n = pm.pageCount(table.tableId());
        private int pageNo = 0;
        private java.util.Iterator<byte[]> cur = null;
        private Row next = null;

        @Override public boolean hasNext() {
            if (next != null) return true;
            next = fetch();
            return next != null;
        }
        @Override public Row next() {
            if (!hasNext()) throw new java.util.NoSuchElementException();
            Row r = next; next = null; return r;
        }
        private Row fetch() { ... }
    };
}
```

**这是真正的流式接口**——一次只在内存里持有一行。但它**没有被任何算子使用**（`SeqScan` 用的是全物化的 `se.scan`）。

**为什么留着？**

1. **展示"另一种可能"**——汇报时可以说"我们实现了流式接口，但为了代码简洁，算子层选择了全物化"
2. **未来的扩展点**——如果要支持 `limit` 或大数据集，改 `SeqScan` 用 `iterate` 即可

**注意 `hasNext()` 的实现**：用一个 `next` 字段做**单元素缓冲**。

```java
if (next != null) return true;       // 已经预取了一行
next = fetch();                       // 否则预取
return next != null;                  // 取到就是有
```

`next()` 里 `hasNext()` 保证 `next != null`，然后取出并**清空缓冲**：

```java
Row r = next; next = null; return r;
```

这是 `Iterator` 的标准实现模式（Java 的 `Iterator` 契约要求 `next()` 不能在 `hasNext()` 返回 true 之前调用两次）。

## 5.3 算子体系：Operator 抽象基类

文件：[`engine/exec/op/Operator.java`](src/main/java/com/course/dbms/engine/exec/op/Operator.java)，33 行。

```java
public abstract class Operator {
    protected final Operator child;

    protected Operator(Operator child) { this.child = child; }

    public abstract List<Row> execute();
    public abstract String name();
    public Operator child() { return child; }

    @Override public String toString() {
        return name() + (child != null ? "(" + child + ")" : "");
    }
}
```

**这个 33 行的类是整个执行引擎的骨架**。设计要点：

**① `child` 是 `final`，构造时传入**

子算子在构造时就固定了——**算子是不可变的**。执行只是"拉取"（`execute()`），不改结构。这意味着同一个 `Plan` 可以被执行多次，结果相同。

**② `execute()` 返回 `List<Row>`（全物化）**

已经讨论过（§0.4）。这是"简单 vs 效率"的取舍。

**③ `toString()` 递归拼出整棵树的形状**

```java
name() + "(" + child + ")"
```

因为 `child` 的 `toString()` 又是它自己的 `name() + "(" + ... + ")"`，所以递归展开成：

```
Project[0, 1](Sort(col=0, asc)(Filter(age > 22)(IndexScan(users))))
```

> ★ **这个 `toString()` 是个非常好的调试工具**，也是汇报时的展示点。
> `PlanBuilderTest` 里就靠它断言计划形状：
>
> ```java
> assertTrue(plan.toString().contains("IndexScan(users)"));
> ```
>
> **加任何新算子，只要实现 `name()`，整棵树的打印就自动支持它**。这是多态的价值。

**④ `child()` 访问器**

`child` 是 `protected`，同包的算子能直接访问；`child()` 是给**外部**（测试、日志）用的。

### 算子的两种类型

| 类型 | 有 child 吗 | 例子 | 特点 |
|---|---|---|---|
| **流式算子** | 有 | `Filter`、`Project`、`Sort`、`Join`、`Aggregate` | 先 `child.execute()` 拿输入，处理后输出 |
| **叶子动作算子** | 无（`super(null)`） | `SeqScan`、`IndexScan`、`CreateTable`、`Insert`、`Show*` | 直接产生结果 |

**叶子算子也要传 `super(null)`** —— 因为它继承的是抽象类，必须调构造器。虽然没人会读这个 `child`。

## 5.4 逐个算子详解

### `SeqScan`：全表扫描

```java
public class SeqScan extends Operator {
    private final StorageEngine se;
    private final Table table;

    public SeqScan(StorageEngine se, Table table) { super(null); this.se = se; this.table = table; }

    @Override public List<Row> execute() { return se.scan(table); }
    @Override public String name() { return "SeqScan(" + table.name() + ")"; }
}
```

**最薄的算子**——就是 `StorageEngine.scan` 的一层包装。为什么要有这一层？

1. **计划树需要它**——`Plan` 的根必须是个 `Operator`
2. **统一接口**——`Filter` 不知道自己的输入是 `SeqScan` 还是 `IndexScan`（这就是多态的意义）

### `IndexScan`：索引扫描

（详细见第 6 章 §6.6，这里只讲算子层面的设计）

```java
@Override public List<Row> execute() {
    List<Row> out = new ArrayList<>();
    for (int[] rid : tree.range(lo, loInc, hi, hiInc)) {
        Row r = se.fetch(table, rid[0], rid[1]);     // 回表
        if (r != null) out.add(r);
    }
    return out;
}

@Override public String name() { return "IndexScan(" + table.name() + ")"; }
```

**注意也是叶子算子**（`super(null)`）—— 因为索引扫描不需要"子管线的行"，它自己就是数据源。

**`name()` 里不带索引名或范围** —— 所以 `IndexScan(users)` 只表明"走索引扫 users"，具体范围得看字段。这是刻意的简化（`PlanBuilderTest` 靠 `indexRangeBoundsFollowOperator` 单独断言范围字段）。

### `Filter`：条件过滤

```java
public class Filter extends Operator {
    private final Schema castSchema;
    private final CondEval.Resolver resolver;
    private final Cond cond;

    /** 单表过滤：列名按 schema 解析。 */
    public Filter(Operator child, Schema schema, Cond cond) {
        this(child, schema, (q, n) -> {
            int i = schema.indexOf(n);
            if (i < 0) throw new Error("SE-0004", "列不存在: " + n);
            return i;
        }, cond);
    }

    /** 多表过滤：列引用经 resolver（通常由 CombinedSchema）解析成行下标。 */
    public Filter(Operator child, Schema castSchema, CondEval.Resolver resolver, Cond cond) {
        super(child);
        this.castSchema = castSchema;
        this.resolver = resolver;
        this.cond = cond;
    }

    @Override public List<Row> execute() {
        List<Row> out = new ArrayList<>();
        for (Row r : child.execute()) {
            if (CondEval.eval(cond, castSchema, r, resolver)) out.add(r);
        }
        return out;
    }

    @Override public String name() { return "Filter(" + cond + ")"; }
}
```

**两个构造函数**：

- **2 参数版（`child, schema, cond`）** —— 单表路径用。内部把一个 **lambda** 当作 `Resolver` 传进去。lambda 体就是 `schema.indexOf(n)`。
- **4 参数版（`child, castSchema, resolver, cond`）** —— rich 路径用，`resolver` 来自 `CombinedSchema::resolve`。

**这个 lambda 写法值得看**：

```java
this(child, schema, (q, n) -> {
    int i = schema.indexOf(n);
    if (i < 0) throw new Error("SE-0004", "列不存在: " + n);
    return i;
}, cond);
```

`CondEval.Resolver` 是函数式接口（只有一个方法 `int resolve(String qualifier, String name)`），所以可以用 lambda 实现。

> ★ **`this(...)` 委托构造器 + lambda 组合，让"旧路径"一行代码就复用了"新路径"的逻辑**。
> 这是保证"加索引功能零回归"的关键手法之一：`Filter` 的旧构造函数签名不变，内部换成新实现。

**lambda 参数 `q` 被忽略** —— 因为单表查询里不会有 `u.id` 这种限定引用（`SelectStmt.isRich()` 会因为"限定列"返回 true 而走 rich 路径）。

**`name()` 用 `cond.toString()`** —— `Cond` 重写了 `toString()`：

```java
// Cond.java
if (kind == Kind.CMP) {
    String left = qualifier != null ? qualifier + "." + column : column;
    if (column2 != null) {
        String right = qualifier2 != null ? qualifier2 + "." + column2 : column2;
        return left + " " + op + " " + right;
    }
    return left + " " + op + " " + value;
}
```

所以计划树里能看到 `Filter(age > 22)`。

### `Project`：投影

```java
public class Project extends Operator {
    private final int[] cols;

    public Project(Operator child, int[] cols) { super(child); this.cols = cols; }

    @Override public List<Row> execute() {
        List<Row> out = new ArrayList<>();
        for (Row r : child.execute()) {
            List<Object> vals = new ArrayList<>(cols.length);
            for (int c : cols) vals.add(r.get(c));      // 按源列下标取
            out.add(new Row(vals));
        }
        return out;
    }

    public int[] columns() { return cols; }
    @Override public String name() { return "Project" + java.util.Arrays.toString(cols); }
}
```

**`cols` 是"源列下标数组"，不是列名**。

`select id, name from users`（users 的 schema 是 `[id, name, age, score]`）→ `cols = [0, 1]`。

**为什么用 `int[]` 不用 `String[]`？** 因为投影是**最热的循环**之一。用下标访问 `r.get(c)` 比按名字查找快得多（不用查 map）。列名到下标在 `PlanBuilder` 里已经解析完了。

**`name()` 输出 `Project[0, 1]`** —— 一目了然。

**注意 `Project` 一定在树的顶部**（单表路径里 `root = new Project(root, cols)` 是最后一步）。因为投影会**丢掉列**，丢掉之后就再也算不了别的东西了。

### `Sort`：排序

```java
public class Sort extends Operator {
    private final int col;
    private final boolean desc;

    @Override public List<Row> execute() {
        List<Row> rows = new ArrayList<>(child.execute());   // 拷贝一份（child 返回的可能是内部引用）
        rows.sort((a, b) -> {
            int c = Compare.compare(a.get(col), b.get(col));
            return desc ? -c : c;                            // ★ 降序取反
        });
        return rows;
    }

    @Override public String name() { return "Sort(col=" + col + ", " + (desc ? "desc" : "asc") + ")"; }
}
```

**降序的实现：`desc ? -c : c`**

一个"取反比较结果"就实现了降序，不需要写两套比较逻辑。

> ⚠️ **这里有个理论隐患值得知道**：`-c` 在 `c == Integer.MIN_VALUE` 时会溢出。
> 但 `Compare.compare` 返回的是 `Double.compare(...)` / `compareTo(...)` 的结果，这些方法保证返回 **-1 / 0 / 1**，绝不会是 `MIN_VALUE`。所以**这里是安全的**。
> （如果自己写 `a - b` 形式的比较器就有这个风险，Java 里推荐用 `Integer.compare(a, b)` 而不是 `a - b`。）

**`new ArrayList<>(child.execute())`** —— 为什么要拷一份？

`List.sort` 是**原地排序**（in-place），会修改列表。如果 `child.execute()` 返回的是内部持有的列表（比如 `Aggregate` 返回的 `out`），就地排序会改到它。

本项目的算子每次 `execute()` 都**新建** `ArrayList`，所以其实可以直接排。但拷贝一份是**防御性**的——防止未来某个算子返回了共享列表。

### `Join`：连接

```java
public class Join extends Operator {
    private final Operator right;
    private final Cond on;                    // 可空 = 笛卡尔积
    private final CombinedSchema cs;
    private final CondEval.Resolver resolver;
    private final boolean leftOuter;
    private final int leftWidth;
    private final int rightWidth;

    @Override public List<Row> execute() {
        List<Row> leftRows = child.execute();
        List<Row> rightRows = right.execute();
        Schema castSchema = cs.toSchema();
        List<Row> out = new ArrayList<>();

        for (Row lr : leftRows) {
            boolean matched = false;
            for (Row rr : rightRows) {
                Row joined = cat(lr, rr);
                if (on == null || CondEval.eval(on, castSchema, joined, resolver)) {
                    out.add(joined);
                    matched = true;
                }
            }
            if (!matched && leftOuter) {
                out.add(cat(lr, new Row(nulls(rightWidth))));   // ★ LEFT JOIN 补 null
            }
        }
        return out;
    }

    private Row cat(Row l, Row r) {
        List<Object> vals = new ArrayList<>(l.size() + r.size());
        vals.addAll(l.values());
        vals.addAll(r.values());
        return new Row(vals);
    }

    private List<Object> nulls(int n) {
        List<Object> l = new ArrayList<>();
        for (int i = 0; i < n; i++) l.add(null);
        return l;
    }

    @Override public String name() {
        return (leftOuter ? "LeftJoin" : "Join") + (on != null ? "(" + on + ")" : "");
    }
}
```

**这是"嵌套循环连接"（Nested Loop Join）**：

```
for each 左行:
    matched = false
    for each 右行:
        if ON 成立: 输出拼接行; matched = true
    if !matched && LEFT: 输出"左行 + 右列全 null"
```

**复杂度 O(左行数 × 右行数)**。真正的数据库会用 Hash Join（O(n+m)）或 Sort-Merge Join。

**为什么选嵌套循环？**

1. **最简单**——十几行代码
2. **通用**——支持任意 ON 条件（Hash Join 只支持等值连接）
3. **数据量小**——课程项目几十行数据，O(n×m) 完全够

**`leftOuter` 的逻辑只在"内层循环一次都没匹配上"时触发** —— 用 `matched` 标志跟踪。

**为什么需要 `leftWidth` / `rightWidth`？**

`cat(lr, new Row(nulls(rightWidth)))` —— 补 null 时要知道**右边有几列**。这个宽度在 `PlanBuilder.buildSelectRich` 里算出来传进来的（因为 `Join` 拿不到右表的 `Schema`——它只有 `Operator`）。

**`castSchema = cs.toSchema()` 在循环外算一次** —— 构造 `Schema` 对象有开销，不能放在内层循环里。

**`name()`**：`Join(u.id = o.uid)` 或 `LeftJoin(u.id = o.uid)` 或裸 `Join`（笛卡尔积）。一眼看出是什么连接、什么条件。

**连接顺序**：左行在外层、右行在内层。所以**输出行的顺序**是"按左行顺序、每个左行跟所有匹配的右行"。

`JoinAggTest` 里断言顺序就依赖这一点。

### `Aggregate`：聚合与分组

文件：[`engine/exec/op/Aggregate.java`](src/main/java/com/course/dbms/engine/exec/op/Aggregate.java)，158 行。**算子里的"重量级"**。

**核心数据结构：`Item`**（描述每个 SELECT 项怎么产出）

```java
public static final class Item {
    public final boolean isAgg;
    public final int groupPos;      // !isAgg：分组键在输出行中的位次
    public final String func;       // isAgg：count/sum/avg/min/max
    public final int argIdx;        // isAgg：参数列在子管线行中的下标（COUNT(*) 为 -1）
    public final boolean countStar; // isAgg 且 COUNT(*)
    public final FieldType argType; // isAgg 且 !countStar：参数列类型（决定 SUM 整型/浮点）
}
```

**这个类把"SELECT 项"编译成了"产出指令"**：

| SELECT 写法 | `Item` 各字段 |
|---|---|
| `age`（分组键） | `isAgg=false, groupPos=0`（在 GROUP BY 列表里是第 0 个） |
| `u.name`（分组键） | `isAgg=false, groupPos=0` |
| `count(*)` | `isAgg=true, func="count", countStar=true, argIdx=-1` |
| `count(id)` | `isAgg=true, func="count", countStar=false, argIdx=<id 的下标>` |
| `sum(score)` | `isAgg=true, func="sum", argIdx=<score 下标>, argType=FLOAT64` |
| `avg(amount)` | `isAgg=true, func="avg", argIdx=..., argType=FLOAT64` |

**累加器 `Acc`**

```java
private static final class Acc {
    long count;        // count 用 + avg 用（计数）
    long lsum;         // sum：整型列的累加
    double dsum;       // sum：浮点列的累加；avg：也是它
    Object min;
    Object max;
    boolean any;       // 是否遇到非 NULL 值（用于区分 SUM 无结果 = 空）
}
```

**`long lsum` 和 `double dsum` 分开** —— 这是为了**保留整数求和的精确性**：

```java
// Aggregate.java:113
case "sum":
    if (integral(it.argType)) a.lsum += n.longValue();      // INT32/INT64 → long 累加
    else a.dsum += n.doubleValue();                          // 其他 → double 累加
    break;
```

如果统一用 double，`sum` 一个大整数列会丢精度（double 只有 53 位尾数）。**用 `long` 累加整数是正确性的要求**。

**`any` 标志的作用**：

```java
case "sum":
    if (!a.any) return null;        // 一行都没喂到（或全是 NULL）→ SUM 返回 NULL
    return integral(it.argType) ? Long.valueOf(a.lsum) : Double.valueOf(a.dsum);
```

SQL 标准规定：**`SUM` 在没有任何非 NULL 值时返回 `NULL`，不是 0**。

但 `COUNT` 不同：

```java
case "count": return Long.valueOf(a.count);      // 一行没有 → 0，不是 NULL
```

**这是 SQL 的语义细节，容易搞错**：`count(*)` 空集 → 0；`sum(x)` 空集 → NULL。

**为什么 `COUNT` 是 `Long` 而不是 `Integer`？** 因为 SQL 标准里 COUNT 返回 BIGINT，而且一张表可能有超过 21 亿行（`int` 上限）。用 `Long` 是标准的做法。

**`execute()` 的主体**

```java
// Aggregate.java:60
@Override public List<Row> execute() {
    List<Row> rows = child.execute();
    LinkedHashMap<List<Object>, Acc[]> groups = new LinkedHashMap<>();

    if (groupIdx.length == 0) {
        groups.put(Arrays.asList(), new Acc[items.length]);   // ★ 全局聚合：空输入也产出一行
    }

    for (Row r : rows) {
        List<Object> key = key(r);                            // 按分组键造 key
        Acc[] accs = groups.get(key);
        if (accs == null) { accs = new Acc[items.length]; groups.put(key, accs); }
        feed(accs, r);
    }

    List<Row> out = new ArrayList<>();
    for (Map.Entry<List<Object>, Acc[]> e : groups.entrySet()) {
        Acc[] accs = e.getValue();
        List<Object> vals = new ArrayList<>(items.length);
        for (int i = 0; i < items.length; i++) {
            Item it = items[i];
            if (!it.isAgg) {
                vals.add(e.getKey().get(it.groupPos));        // 非聚合项：直接从分组键取
            } else {
                Acc a = accs[i];
                if (a == null) a = new Acc();                 // 未喂到行（空输入）
                vals.add(result(a, it));
            }
        }
        out.add(new Row(vals));
    }
    return out;
}
```

**几个关键设计**：

**① `LinkedHashMap` 而不是 `HashMap`** —— **保证输出顺序是分组首次出现的顺序**。如果用 `HashMap`，输出顺序不确定（取决于 hash），测试就没法断言了。

例：`select age, avg(score) from users group by age` —— 输出顺序是 age 值第一次出现的顺序。

**② 全局聚合的"空输入也产出一行"**

```java
if (groupIdx.length == 0) {
    groups.put(Arrays.asList(), new Acc[items.length]);
}
```

`select count(*) from 空表` 应该返回**一行**，值是 0。如果不预置这个空组，循环一次都不进，`groups` 是空的，就会返回 0 行——**错了**。

注意预置时 `Acc[]` 里的元素是 `null`（没 `new Acc()`），所以后面：

```java
Acc a = accs[i];
if (a == null) a = new Acc();     // ← 这里补上
```

**③ 分组聚合的空输入返回空**

`select age, count(*) from users group by age` 在空表上返回 **0 行**（没有分组就没有输出行）。这跟全局聚合不同——符合 SQL 标准。

**④ `key(r)` 用 `List<Object>` 做分组键**

```java
private List<Object> key(Row r) {
    List<Object> k = new ArrayList<>(groupIdx.length);
    for (int g : groupIdx) k.add(r.get(g));
    return k;
}
```

**为什么 `List` 能当 HashMap 的 key？** 因为 `List` 实现了 `equals` / `hashCode`——**按内容比较**。所以 `[23]` 和另一个 `[23]` 是同一个 key。

这正是支持**多列 GROUP BY** 的关键：`group by a, b` 的 key 是 `[a值, b值]`。

**⑤ 非聚合项直接从分组键取**

```java
vals.add(e.getKey().get(it.groupPos));
```

**这就是为什么 `Analyzer` 要强制"非聚合列必须在 GROUP BY 里"** —— 因为 `Aggregate` 只能从分组键里取值，如果某个非聚合列不在分组键里，它根本不知道该输出哪个值。

`groupPos` 是在 `PlanBuilder` 里算的（`groupKeyPos`），标明了"这个列在 GROUP BY 列表里排第几"。

**`accumulate` 的 NULL 处理**

```java
// Aggregate.java:101
private void accumulate(Acc a, Item it, Row r) {
    if (it.func.equals("count")) {
        if (it.countStar) { a.count++; return; }          // COUNT(*)：数所有行，包括 NULL 行
        Object v = r.get(it.argIdx);
        if (v != null) { a.any = true; a.count++; }        // COUNT(列)：只数非 NULL
        return;
    }
    Object v = r.get(it.argIdx);
    if (v == null) return;                                 // 聚合忽略 NULL
    a.any = true;
    Number n = (Number) v;
    ...
}
```

**`COUNT(*)` 和 `COUNT(列)` 的区别**：

```sql
select count(*) from users            -- 所有行数（包括所有列都是 NULL 的行，虽然本系统不允许）
select count(score) from users        -- score 非 NULL 的行数
```

这个差异在 `JoinAggTest.aggregateOverLeftJoinCountsNullsAsZero` 里被验证——LEFT JOIN 产生的右列全 null 的行，`count(*)` 算它，`count(o.amount)` 不算它。

**`min` / `max` 用 `Compare.compare`**

```java
case "min":
    if (a.min == null || Compare.compare(v, a.min) < 0) a.min = v;
    break;
case "max":
    if (a.max == null || Compare.compare(v, a.max) > 0) a.max = v;
    break;
```

**复用统一的比较规则** —— 所以 `min`/`max` 能正确处理字符串、数字、布尔。**`a.min == null` 同时充当"还没遇到过值"的判断**（因为 `v` 已经保证非 null）。

**`avg` 为什么用 `dsum / count`**

```java
case "avg":
    a.dsum += n.doubleValue();
    a.count++;
    break;
...
case "avg":
    if (!a.any) return null;
    return Double.valueOf(a.dsum / a.count);
```

**注意 `avg` 用的是 `count`**（不是 `lsum`）——所以 `avg` 和 `count` 共享了 `count` 字段。这在同一个 SELECT 里同时有 `count(*)` 和 `avg(x)` 时会**冲突**吗？

```sql
select count(*), avg(score) from users
```

**不会冲突** —— 因为 `Acc` 是**按 SELECT 项分开的**（`Acc[items.length]`），`count(*)` 用 `accs[0]`、`avg(score)` 用 `accs[1]`。每个 Item 有自己的累加器。

### `CreateTable` / `CreateIndex`：DDL 叶子

```java
public class CreateTable extends Operator {
    @Override public List<Row> execute() {
        catalog.createTable(name, schema);
        return Collections.singletonList(Row.of("table '" + name + "' created"));
    }
    @Override public String name() { return "CreateTable(" + name + ")"; }
}

public class CreateIndex extends Operator {
    @Override public List<Row> execute() {
        catalog.createIndex(indexName, tableName, columnName);
        return Collections.singletonList(Row.of("index '" + indexName + "' created"));
    }
    @Override public String name() { return "CreateIndex(" + indexName + ")"; }
}
```

**模式完全一致**：调用 `Catalog` 完成实际工作，返回一行"成功消息"。

`Collections.singletonList(...)` —— 单元素不可变列表，比 `new ArrayList<>()` 再 `add` 省事且省内存。

**注意 `catalog.createTable` 会抛 `CT-0002`（表已存在）** —— Analyzer 其实没查这个（见 §4.6）。所以"表名重复"的错误是**在算子执行时**才发现的。

**这就是"约束在权威写入点检查"的体现**（复习 §4.6）。

### `Insert`：插入 + 索引维护钩子

```java
public class Insert extends Operator {
    private final StorageEngine se;
    private final Table table;
    private final Row row;
    private final IndexManager indexes;   // 可为 null（无索引环境）

    public Insert(StorageEngine se, Table table, Row row) {
        this(se, table, row, null);                  // 旧签名保留，委托给新签名
    }

    public Insert(StorageEngine se, Table table, Row row, IndexManager indexes) {
        super(null);
        this.se = se;
        this.table = table;
        this.row = row;
        this.indexes = indexes;
    }

    @Override public List<Row> execute() {
        int[] rid = se.insert(table, row);           // 返回 (页号,槽位)
        if (rid != null && indexes != null) {
            indexes.onInsert(table, row, rid[0], rid[1]);   // ★ 索引维护钩子
        }
        return Collections.singletonList(Row.of(rid != null ? 1 : 0));
    }
}
```

> ★ **这是一处非常优雅的设计**：整个索引维护（对所有索引、所有行）**只有一个钩子**。
>
> 为什么一个钩子就够？因为：
> 1. 系统**没有 DELETE / UPDATE** —— `StorageEngine.insert` 是唯一的写路径
> 2. `Insert` 算子是唯一的写入口
>
> 于是"每插入一行，把它加到该表的所有索引上"这一句话，在这个位置实现就**完备**了。
>
> **如果将来加了 DELETE**？那就需要在 `Delete` 算子里也加一个 `onDelete` 钩子，并且 `IndexManager` 要给 B+ 树加删除操作（那是个复杂的话题——B+ 树的删除涉及合并和重分配）。

**`indexes` 可为 null** —— 保留了 3 参数构造器（委托 `null`），所以旧测试代码 `new Insert(se, table, row)` 还能用。这是"零回归"的又一体现。

**`rid != null ? 1 : 0`** —— `se.insert` 正常总是返回非 null（它会开新页），所以永远是 1。写成三元是**防御性**的。

### `ShowTables` / `ShowTable` / `ShowIndexes`：三个 SHOW 叶子

```java
public class ShowTables extends Operator {
    @Override public List<Row> execute() {
        List<Row> out = new ArrayList<>();
        for (Table t : catalog.listTables()) out.add(Row.of(t.name(), t.schema().columnCount()));
        return out;
    }
    @Override public String name() { return "ShowTables"; }
}

public class ShowTable extends Operator {
    @Override public List<Row> execute() {
        Table table = catalog.getTable(name);   // 表不存在抛 TB-0001
        List<Row> out = new ArrayList<>();
        for (int i = 0; i < table.schema().columnCount(); i++) {
            Column c = table.schema().column(i);
            out.add(Row.of(c.name(), c.type().sqlName()));
        }
        return out;
    }
    @Override public String name() { return "ShowTable(" + name + ")"; }
}

public class ShowIndexes extends Operator {
    @Override public List<Row> execute() {
        List<Row> out = new ArrayList<>();
        Integer onlyTable = null;
        if (tableName != null) {
            Table t = catalog.getTable(tableName);              // 表不存在抛 TB-0001
            onlyTable = Integer.valueOf(t.tableId());
        }
        for (Index idx : catalog.indexes()) {
            if (onlyTable != null && idx.tableId() != onlyTable.intValue()) continue;   // 过滤
            out.add(Row.of(idx.name(), idx.tableName(), idx.columnName()));
        }
        return out;
    }
    @Override public String name() { return "ShowIndexes"; }
}
```

**三个都是"读 Catalog 然后拼行"** —— 因为它们的数据源是元数据，不是表数据。

**`ShowTables` 列出的是 `catalog.listTables()`** —— 而 `Catalog.tables` 这个 map 里**只装用户表**（`sys_*` 是单独字段 `sysTables`/`sysColumns`/`sysIndexes`，不进 map）。所以 `show tables` 不会露出系统表。**这是刻意的**。

**`ShowIndexes` 的 `Integer` vs `int` 比较陷阱**：

```java
Integer onlyTable = Integer.valueOf(t.tableId());   // 必须显式 valueOf
...
if (onlyTable != null && idx.tableId() != onlyTable.intValue()) continue;
```

**注意必须用 `.intValue()` 拆箱比较，不能写 `idx.tableId() != onlyTable`** —— 因为那会变成"`int` 和 `Integer` 比较"，Java 会把 `Integer` **拆箱**，其实也对。但写成 `onlyTable != Integer.valueOf(...)`（两个 Integer 用 `!=`）就会变成**引用比较**——`Integer` 缓存只覆盖 -128~127，表 id 从 10 开始所以碰巧在缓存内能对，但这是**靠不住的**。显式 `.intValue()` 才是正确的写法。

### `CondEval` 和 `Compare`：条件求值工具

**`Compare`**（54 行）：

```java
public static int compare(Object a, Object b) {
    if (a == null && b == null) return 0;
    if (a == null) return 1;                        // NULL 排最后
    if (b == null) return -1;
    if (a instanceof Number && b instanceof Number) {
        return Double.compare(((Number) a).doubleValue(), ((Number) b).doubleValue());
    }
    if (a instanceof Boolean && b instanceof Boolean) {
        return ((Boolean) a).compareTo((Boolean) b);
    }
    return String.valueOf(a).compareTo(String.valueOf(b));    // 兜底：字符串比较
}

public static boolean apply(String op, Object a, Object b) {
    if (a == null || b == null) return false;       // SQL 三值逻辑：UNKNOWN 不保留
    int c = compare(a, b);
    switch (op) {
        case "=":  return c == 0;
        case "<>": return c != 0;
        case "<":  return c < 0;
        case ">":  return c > 0;
        case "<=": return c <= 0;
        case ">=": return c >= 0;
        default: throw new Error("EX-0001", "unknown operator: " + op);
    }
}
```

**这个类是全项目"值比较"的唯一真相来源**。`Filter`、`Join`、`Sort`、`Aggregate`（min/max）、`BPlusTree` 全部用它。只有一个类，却要同时满足**两组互相矛盾**的需求——这是本类设计上最值得讲的一点：

| 调用方 | 语义要求 | NULL 该怎么办 |
|---|---|---|
| `Sort`、`Aggregate` 的 min/max、`BPlusTree` 定位 | **全序**：任意两个值必须能分出大小，否则排序/二分无从下手 | 必须给一个**确定位置** |
| `Filter`、`Join` 的 ON | **三值逻辑**：`NULL = NULL` 是 UNKNOWN，不是 TRUE | 一律**判不成立** |

所以 `compare` 与 `apply` 拆开，各管一头：

- **`compare` —— NULLS LAST**。NULL 视为最大，升序垫底。`Sort` 降序时取反比较结果，NULL 自然翻到最前，恰好就是 PostgreSQL 的 `NULLS FIRST`，与真实数据库一致。
- **`apply` —— 先判 NULL 直接返回 false**。这就是 SQL 的 UNKNOWN：`WHERE`/`ON` 只保留 TRUE，所以与 NULL 的任何比较都不保留该行（含 `<>`，含 `x = x` 这种自比）。

> **历史缺陷（已修）**：早期版本 `apply` 直接复用 `compare` 的结果，NULL 走到兜底分支后变成字符串 `"null"` 参与比较，而 `'n'` 的 ASCII 是 110，**大于所有数字字符（48–57）**，于是 NULL 被当成了"比任何数都大"——`where o.amount > 50` 会把 NULL 行错误地放行。
>
> 修法就利用了上表：调用方本来就已经干净地分成"要全序的"和"要三值逻辑的"两组（`compare` 的调用点只有 `Aggregate`/`Sort`/`BPlusTree`，`apply` 的调用点只有 `CondEval` 两处），所以只需在 `apply` 开头加一行守卫，不动任何调用方。
>
> 回归防线是 `NullSemanticsTest`（8 个用例，见 §10.2）。它覆盖六个运算符、列列比较、自比、升降序 NULL 位置、以及聚合忽略 NULL 不被破坏。**这组用例经过反向验证**：临时把守卫改成 `if (false)` 后跑，`NullSemanticsTest` 出现 3 个失败且差异全是 `was:<2>`（NULL 行泄漏进结果），证明它确实咬得住这个 bug，不是摆设。

**"数值统一按 double 比较"的取舍**：

```java
Double.compare(((Number) a).doubleValue(), ((Number) b).doubleValue())
```

`int 5` 和 `long 5` 和 `double 5.0` 比较结果都是"相等"。**这很方便**（不同类型不会出错）。

**代价**：大整数精度。`Long.MAX_VALUE` 和 `Long.MAX_VALUE - 1` 转成 double 后**可能相等**（double 只有 53 位尾数，`Long.MAX_VALUE` 是 63 位）。所以：

```sql
select * from t where bigint_col = 9223372036854775806
```

在极端情况下可能匹配到错误的行。

**但对本项目可接受**：INT64 主要用来存时间戳（毫秒级，约 1.7×10¹²，远在 double 精度范围内）。

**兜底用字符串比较** —— `String.valueOf(a).compareTo(...)`。类型不同（如字符串 vs 数字）时不抛异常，按字典序给出一个确定结果。

注意兜底分支里**已经不可能出现 `null`** 了：`compare` 开头就把 NULL 拦截返回，`apply` 开头更是直接返回 false。这道拦截必须放在兜底**之前**，否则 `String.valueOf(null)` 会生成字符串 `"null"`，而 `'n'` 的 ASCII 值 110 大于所有数字字符，NULL 就会被错误地当成"最大"——这正是上文"历史缺陷"里那个已修复的 bug。

**`CondEval`**（47 行）：已经详细讲过了（§4.4 的 `Cond` 部分）。核心：

```java
public static boolean eval(Cond c, Schema castSchema, Row row, Resolver r) {
    switch (c.kind) {
        case CMP:  ... Compare.apply(c.op, v1, lit);
        case AND:  for (Cond child : c.children) if (!eval(child, castSchema, row, r)) return false;
                   return true;                                      // 短路求值
        case OR:   for (Cond child : c.children) if (eval(child, castSchema, row, r)) return true;
                   return false;                                     // 短路求值
        default:   return false;
    }
}
```

**`AND` / `OR` 都是短路求值** —— `AND` 遇到第一个 false 立即返回 false，`OR` 遇到第一个 true 立即返回 true。跟 Java 的 `&&`/`||` 语义一致。

**`Resolver` 接口**（函数式接口）：

```java
public interface Resolver {
    int resolve(String qualifier, String name);
}
```

**这样一个接口统一了单表和单表两种解析方式**：

- 单表：`(q, n) -> schema.indexOf(n)`（忽略 qualifier）
- 多表：`cs::resolve`（用 `CombinedSchema` 处理限定名与歧义）

## 5.5 `Result`：查询结果

文件：[`engine/Result.java`](src/main/java/com/course/dbms/engine/Result.java)，43 行。

```java
public class Result {
    public final List<String> columns;
    public final List<Row> rows;

    public static Result message(String msg) {
        List<String> cols = new ArrayList<>(); cols.add("result");
        List<Row> rs = new ArrayList<>();      rs.add(Row.of(msg));
        return new Result(cols, rs);
    }

    public static Result affected(int n) {
        List<String> cols = new ArrayList<>(); cols.add("affected");
        List<Row> rs = new ArrayList<>();      rs.add(Row.of(n));
        return new Result(cols, rs);
    }

    public int rowCount() { return rows.size(); }
}
```

**类注释里点明了它的地位**："这是客户端/服务器端契约的核心数据结构。"

**服务端和客户端都依赖它**：

- 服务端：`Executor` 产出 `Result` → `Encoder.encodeResult(r)` 序列化
- 客户端：`Decoder.decodeResult(payload)` 还原 → `Renderer.render(r)` 渲染

**两个静态工厂**：
- `Result.message("transaction committed")` → 一列 `result`、一行字符串
- `Result.affected(1)` → 一列 `affected`、一行数字

**为什么 `columns` 和 `rows` 是 `public final` 而不是 getter？** 因为它是纯数据载体（DTO），没有行为。加 getter 只会让所有调用点变长（`r.columns.size()` vs `r.columns().size()`）。

## 5.6 `Catalog`：系统目录（元数据即数据）

文件：[`engine/table/Catalog.java`](src/main/java/com/course/dbms/engine/table/Catalog.java)，188 行。

> ★ **这是整个项目最有"数据库味道"的设计**：**元数据用普通表存储**。

### 三张系统表

```java
public static final int SYS_TABLES_ID = 1;
public static final int SYS_COLUMNS_ID = 2;
public static final int SYS_INDEXES_ID = 3;
private static final int FIRST_USER_ID = 10;

this.sysTables  = new Table(SYS_TABLES_ID, "sys_tables", new Schema()
        .add("table_id", FieldType.INT32)
        .add("name", FieldType.STRING));

this.sysColumns = new Table(SYS_COLUMNS_ID, "sys_columns", new Schema()
        .add("table_id", FieldType.INT32)
        .add("cid", FieldType.INT32)
        .add("name", FieldType.STRING)
        .add("type", FieldType.INT32));

this.sysIndexes = new Table(SYS_INDEXES_ID, "sys_indexes", new Schema()
        .add("table_id", FieldType.INT32)
        .add("name", FieldType.STRING)
        .add("column_name", FieldType.STRING)
        .add("cid", FieldType.INT32));
```

**它们就是三张普通的 `Table`** —— 用同一个 `StorageEngine` 读写，落盘成 `table-1.db` / `table-2.db` / `table-3.db`。

**内容示例**（建了 `users` 表和 `idx_age` 索引后）：

`sys_tables`：
```
| table_id | name  |
| 10       | users |
```

`sys_columns`：
```
| table_id | cid | name  | type |
| 10       | 0   | id    | 1    |   ← 1 = INT32 的 tag
| 10       | 1   | name  | 5    |   ← 5 = STRING
| 10       | 2   | age   | 1    |
| 10       | 3   | score | 3    |   ← 3 = FLOAT64
```

`sys_indexes`：
```
| table_id | name    | column_name | cid |
| 10       | idx_age | age         | 2   |
```

> ★ **这就是课程图片上"系统目录：维护元数据，作为特殊表存储"的完整实现**。
>
> **为什么要这样设计？**
>
> 朴素做法：元数据写成一个 JSON / properties 文件。问题：
> - **N 种格式**（元数据一种、数据一种）→ N 套读写代码、N 套持久化保证
> - **元数据不支持事务**——建表回滚了但元数据文件没回滚
> - **不支持查询**——想查"哪些表有 age 列"得自己写解析
>
> **用普通表存**：
> - 元数据和数据**走完全相同的存储/缓存/事务/WAL 路径**——零新增代码
> - `create table` 自动获得**事务性**（元数据写在影子页上，回滚就没了）
> - 重启恢复自动工作（`se.scan(sysTables)` 读出来就是）
> - 未来可以 `select * from sys_tables` 查询（虽然本项目没暴露）
>
> **代价**：自举（bootstrap）问题——第一次启动时 `sys_tables` 本身还不存在。本项目的处理：**`Catalog` 构造函数里硬编码这三张表的 Schema**（不查目录），所以它们"总是存在"。用户表从 id 10 开始，1/2/3 让给系统表。

### `reload()`：重启恢复 + 回滚撤销的统一入口

```java
// Catalog.java:67
public synchronized void reload() {
    tables.clear();
    for (Row r : se.scan(sysTables)) {
        int tid = (Integer) r.get(0);
        String name = (String) r.get(1);
        tables.put(name.toLowerCase(), new Table(tid, name, schemaOf(tid)));
    }
    rebuildIndexes();
}

private Schema schemaOf(int tableId) {
    TreeMap<Integer, Column> byCid = new TreeMap<>();        // ★ 按 cid 排序
    for (Row r : se.scan(sysColumns)) {
        int tid = (Integer) r.get(0);
        if (tid != tableId) continue;
        int cid = (Integer) r.get(1);
        String name = (String) r.get(2);
        FieldType type = FieldType.fromTag((byte) (int) (Integer) r.get(3));
        byCid.put(cid, new Column(name, type));
    }
    Schema s = new Schema();
    for (Column c : byCid.values()) s.add(c.name(), c.type());
    return s;
}
```

**`TreeMap<Integer, Column>`** —— 必须用**有序**的 map！因为 `sys_columns` 的行是按插入顺序物理存储的，理论上可能与 `cid` 顺序一致，但**不能依赖物理顺序**（比如将来重写了页）。用 `TreeMap` 按 `cid` 排序是**显式保证列顺序正确**。

**列顺序极其重要** —— 因为 `StorageEngine.rowFromBytes` 是**按位置**解码的。如果列顺序变了，所有数据都会读错。

**`synchronized`** —— 多连接共享同一个 `Catalog`，`reload` 会 `tables.clear()`，必须串行。

**`FieldType.fromTag((byte) (int) (Integer) r.get(3))`** —— 三层转换，值得解释：

1. `(Integer) r.get(3)` —— 从 `Row` 取出，是 `Integer`
2. `(int) ...` —— 拆箱成 `int`
3. `(byte) ...` —— 截断成 `byte`（因为 `fromTag` 收 `byte`）

**为什么不直接存 `byte`？** 因为 `FieldType.encode` 没有 `byte` 类型的路径，而 `Row` 里的值必须是对象（`Integer` 会装箱）。存 `int` 是自然的选择。

### `createTable`：登记两张系统表

```java
// Catalog.java:100
public Table createTable(String name, Schema schema) {
    String key = name.toLowerCase();
    if (tables.containsKey(key)) {
        throw new Error("CT-0002", "table already exists: " + name);
    }
    int tid = nextTableId();
    se.insert(sysTables, Row.of(tid, name));                    // ① 登记表
    int cid = 0;
    for (Column c : schema.columns()) {
        se.insert(sysColumns, Row.of(tid, cid++, c.name(), (int) c.type().tag()));   // ② 登记每列
    }
    Table t = new Table(tid, name, schema);
    tables.put(key, t);                                          // ③ 更新内存缓存
    return t;
}

private int nextTableId() {
    int max = FIRST_USER_ID - 1;                                // 至少从 9 开始
    for (Row r : se.scan(sysTables)) {
        max = Math.max(max, (Integer) r.get(0));
    }
    return max + 1;
}
```

**`nextTableId` 是"扫全表求最大值 + 1"** —— O(n)。为什么不维护一个计数器？因为计数器也要持久化（又多一份元数据），而且**重启后会失效**。扫一遍 `sys_tables`（通常只有几十行）是最简单且永远正确的做法。

**`tid` 递增不复用** —— 即使删表也不复用 id（本项目没有 DROP，所以更不存在这个问题）。

**关键：`se.insert(sysTables, ...)` 是在事务内还是外？**

如果在事务内（`create table` 写在 BEGIN...ROLLBACK 之间），`se.insert` → `PageManager.persist` → 走**事务路径** → 只进工作集。

**结果**：`sys_tables` 的新行只在工作集里，磁盘上没变，`tables` map 里**有**这张表（内存登记了）。

ROLLBACK 时：

```java
Txn.clearCurrent();            // 脱离事务
db.pages().rollbackTxn(cur2);  // 丢弃工作集（sys_tables 的新行没了）
db.catalog().reload();         // 重新读 sys_tables（此时读的是磁盘，没有新行）
                               // → tables map 被重建，新建的表"消失"了
```

**这就是"回滚撤销建表"的完整机制**——`reload()` 是天然的回滚钩子。**零专用代码**。

### `createIndex`：索引元数据 + 建树

```java
// Catalog.java:120
public Index createIndex(String name, String tableName, String columnName) {
    Table t = getTable(tableName);                                    // TB-0001 if missing
    int cid = t.schema().indexOf(columnName);
    if (cid < 0) {
        throw new Error("SE-0004", "列不存在: " + columnName + "（表 " + t.name() + "）");
    }
    if (hasIndex(name)) {
        throw new Error("SE-0007", "索引名已存在: " + name);
    }
    se.insert(sysIndexes, Row.of(t.tableId(), name, t.schema().column(cid).name(), cid));  // ① 元数据
    return indexManager.createIndex(name, t, cid);                                          // ② 建树
}
```

**两步**：

1. **元数据写进 `sys_indexes`** —— 落盘、随事务提交/回滚
2. **内存里建 B+ 树** —— 扫一遍基表填充

**重复了一次 `Analyzer` 的检查**（列存在、索引名不重复）—— 又是"约束在权威写入点检查"的原则。

### `rebuildIndexes`：重建全部索引

```java
// Catalog.java:78
private void rebuildIndexes() {
    indexManager.clear();
    for (Row r : se.scan(sysIndexes)) {
        int tid = (Integer) r.get(0);
        String name = (String) r.get(1);
        String column = (String) r.get(2);
        int cid = (Integer) r.get(3);
        Table t = tableById(tid);
        if (t == null) continue;                                    // 表已被删（防御性）
        if (cid < 0 || cid >= t.schema().columnCount()) continue;   // 列数不对（防御性）
        indexManager.createIndex(name, t, cid);
    }
}
```

**在 `reload()` 末尾调用，所以两类场景自动生效**：

| 场景 | 触发 |
|---|---|
| **进程重启** | `Catalog` 构造 → `reload()` → `rebuildIndexes()` |
| **事务回滚** | `Session.handleTx(ROLLBACK)` → `catalog.reload()` → `rebuildIndexes()` |

**回滚场景的推演**：

```
begin;
create index idx_age on users(age);     -- ① sys_indexes 新行进工作集；② 内存里 B+ 树建好了
rollback;                                -- ③ 工作集丢弃；④ reload → indexManager.clear() 后重读 sys_indexes
                                            （读磁盘，没有新行）→ 树没被重建 → 索引"消失"
```

**这就是"回滚撤销 CREATE INDEX"的机制**——**跟回滚撤销建表完全同构**。

**两处防御性检查**（`t == null` / `cid` 越界）—— 因为 `sys_indexes` 是磁盘上的数据，可能有损坏或不一致。跳过而不是崩溃，是稳健的做法。

### `tableById`：线性查找

```java
private Table tableById(int tableId) {
    for (Table t : tables.values()) {
        if (t.tableId() == tableId) return t;
    }
    return null;
}
```

**O(n) 扫描** —— 因为 `tables` 是 `Map<String, Table>`（按名字索引），没有按 id 的索引。

数据量小（几十张表），可以接受。**如果要优化**，再加一个 `Map<Integer, Table>` 即可。

## 5.7 第 5 章自问自答

**Q：为什么不把系统表和用户表放在同一个 map 里？**
A：因为 `show tables` 应该只列用户表。分开存（`sysTables` 等是独立字段）就不用每次过滤了。

**Q：`Catalog` 里的 `tables` 为什么用 `ConcurrentHashMap`？**
A：服务端多连接共享同一个 `Catalog`。`getTable` 是只读的（`ConcurrentHashMap` 的读操作无锁），并发安全。而 `reload()` 会修改它，所以加了 `synchronized`。

**Q：`StorageEngine` 里为什么要同时有 `scan` 和 `scanLocated`？**
A：`scan` 的接口更干净（`List<Row>`），但缺位置信息。`scanLocated` 补上位置，代价是调用方要处理 `Located` 包装。**不给 `scan` 加参数**（比如 `scan(table, boolean withRid)`），因为那样所有现有调用点都要改。

**Q：Delete 和 Update 为什么没实现？**
A：需要：
1. `Parser` 加语法（`delete from t where ...`）——简单
2. 算子里要按 RID 标记槽位为空（`Page` 已有"空槽位"的机制，但需要 `deleteRecord(int slot)` 方法）
3. 索引要支持删除（B+ 树的删除比插入复杂得多：要处理下溢、合并、重分配）
4. 空间回收策略

工作量不小，且课程图片没要求。**索引功能只做 insert 维护**就是这个原因。

---

# 第 6 章 B+ 树索引与 IndexScan

这是课程图片底部"高级功能③ 复杂索引"的实现，也是**规划器第一次做真正的物理计划选择**。

## 6.1 为什么需要索引

**没有索引时**，`select id from users where age = 30` 的执行是：

```
SeqScan(users) → 逐页读、逐行解码 → Filter 逐行判 age == 30
```

读 1 亿行找 3 行，**读的全是白读**。

**有索引时**：

```
IndexScan(users) → B+ 树里定位 age=30 的 RID → 只回表取那几行
```

**B+ 树为什么适合做索引？**

| 特性 | 为什么重要 |
|---|---|
| **有序** | 支持范围查询（`> 22`、`between`），不只是等值 |
| **树矮** | 阶数高（一个节点存几百个键），1 亿行只要 3~4 层，几次页读就能定位 |
| **叶子链表** | 范围查询只需找到起点，然后沿链表右扫，不用回溯内部节点 |
| **数据只在叶子** | 内部节点只存键，能塞下更多键 → 树更矮 |

> **本项目的简化**：`MAX_KEYS = 4`（阶数 5），而不是真实数据库的几百。为什么？
> - 小数据量就能触发**分裂**，便于演示和测试（插 5 个键就分裂了）
> - 树高增长快（插 100 个键能到 3~4 层），`height()` 有东西可展示
> - 代码调试时打印节点内容看得清
>
> **真实数据库**：页 8KB，键 8 字节 + 指针 6 字节 → 一个内部节点存约 580 个键。1 亿行（叶子能放约 1000 个条目/页）→ 4 层。

## 6.2 `BPlusTree`：内存 B+ 树

文件：[`engine/index/BPlusTree.java`](src/main/java/com/course/dbms/engine/index/BPlusTree.java)，166 行。

### 结构定义

```java
public class BPlusTree {
    private static final int MAX_KEYS = 4;      // 阶数 K = 5（最多 4 个键）

    private static class Node {
        boolean leaf;
        List<Object> keys = new ArrayList<>();
        List<Node> children = new ArrayList<>();   // 非叶：子节点
        List<int[]> rids = new ArrayList<>();      // 叶：RID {pageNo, slot}
        Node next;                                 // 叶：下一个叶子的链表指针
    }

    private static final class Split {
        final Object key;      // 上提到父节点的分隔键
        final Node right;      // 分裂出的右半部分
        Split(Object key, Node right) { this.key = key; this.right = right; }
    }

    private Node root;
    private int size;                          // 索引项总数
}
```

**一张图说清结构**（MAX_KEYS=4，插入了 `1..7` 之后的可能形态）：

```
                    ┌─────────────┐
                    │  根（内部）  │
                    │  [3, 5]     │
                    └──┬───┬───┬──┘
              <3       │   │       >=5
         ┌─────────────┘   └────────────┐
         ▼               ▼              ▼
    ┌─────────┐    ┌─────────┐    ┌─────────┐
    │ 叶 [1,2]│───▶│ 叶 [3,4]│───▶│ 叶 [5,6]│───▶ ... ──▶ [7]
    └─────────┘    └─────────┘    └─────────┘
     叶链表：按 key 升序串起来（range 查询沿它右扫）
```

**关键不变量**：

1. **所有键都有序**：内部节点 `keys[0] < keys[1] < ...`
2. **子树区间**：`subtree(children[i]) ⊆ [keys[i-1], keys[i])` —— 即 `keys[i]` 是**右子树的下界**
3. **数据只在叶子**：内部节点只存分隔键
4. **叶子之间用 `next` 串联**，按 key 升序

> ★ 不变量 2 决定了查找逻辑：**相等的键往右走**。所以找 `4` 时，在节点 `[3, 5]` 里 `upperBound(4)` 返回 `2`（因为 `keys[0]=3 <= 4`、`keys[1]=5 > 4`）→ 走 `children[2]`。
> 如果实现成"相等的键往左走"，那么 `search(5)` 会走到 `[3,4]` 叶子，找不到 5 —— **错**。这个方向的正确性必须严格保证。

**数据结构选型的取舍** —— 为什么用 `List<Object>` 而不是 `Object[]`？

- `ArrayList` 的插入/删除是 `System.arraycopy` 实现，很快
- 但本项目 MAX_KEYS=4，`List` 的开销（每个元素一个引用）完全无所谓
- **`List` 的 API 更好用**（`add(int, E)` 插入、`subList` 取子区间），代码更短

### `insert`：插入 + 分裂

```java
// BPlusTree.java
public void insert(Object key, int pageNo, int slot) {
    if (root == null) {
        root = new Node(true);
        root.keys.add(key);
        root.rids.add(new int[] { pageNo, slot });
        size = 1;
        return;
    }
    Split s = insertInto(root, key, new int[] { pageNo, slot });
    if (s != null) {                       // ★ 根分裂 → 树高 +1
        Node newRoot = new Node(false);
        newRoot.keys.add(s.key);
        newRoot.children.add(root);
        newRoot.children.add(s.right);
        root = newRoot;
    }
    size++;
}
```

**空树特判**：直接建一个叶根，放一个键。所以 `height()` 对空树返回 0、只有叶根返回 1。

**根分裂**是"树长高"的唯一方式：

```
分裂前：        [1,2,3,4,5]（叶根，超了 MAX_KEYS=4）

分裂后：            [3]           ← 新根（内部节点）
                  /     \
             [1,2]       [3,4,5]
               
              树高从 1 变成 2
```

> ⚠️ **注意**：分裂**不是独立方法**，而是内联在 `insertInto` 里——因为插入和分裂共享同一个 `upperBound` 结果 `i`（插入位置），拆成两个方法反而要重算一遍。

**完整的 `insertInto`（源码逐字）**：

```java
/** 递归下沉插入；返回非 null 表示本节点分裂，需要父节点接纳 (分隔键, 右兄弟)。 */
private Split insertInto(Node n, Object key, int[] rid) {
    int i = upperBound(n, key);                        // ★ 插入位置 / 选孩子的下标
    if (n.leaf) {
        n.keys.add(i, key);                            // 插在 i 处
        n.rids.add(i, rid);
        if (n.keys.size() <= MAX_KEYS) return null;    // 没超限，不分裂
        int mid = n.keys.size() / 2;                   // 叶子：复制上提，mid 处的键仍留在右叶子
        Node right = new Node(true);
        right.keys.addAll(new ArrayList<>(n.keys.subList(mid, n.keys.size())));
        right.rids.addAll(new ArrayList<>(n.rids.subList(mid, n.rids.size())));
        n.keys.subList(mid, n.keys.size()).clear();    // 从 n 里删掉搬走的
        n.rids.subList(mid, n.rids.size()).clear();
        right.next = n.next;                           // 维护叶子链：n → right → 原 n.next
        n.next = right;
        return new Split(right.keys.get(0), right);    // "复制上提"：分隔键在叶子和父节点各一份
    }
    Split s = insertInto(n.children.get(i), key, rid); // ★ 递归下沉
    if (s == null) return null;                        // 孩子没分裂
    n.keys.add(i, s.key);                              // 把孩子上提的分隔键插进来
    n.children.add(i + 1, s.right);                    // 右兄弟挂在孩子右边
    if (n.keys.size() <= MAX_KEYS) return null;
    int mid = n.keys.size() / 2;                       // 内部节点：移出上提，mid 处的键不再留在本节点
    Object up = n.keys.get(mid);
    Node right = new Node(false);
    right.keys.addAll(new ArrayList<>(n.keys.subList(mid + 1, n.keys.size())));
    right.children.addAll(new ArrayList<>(n.children.subList(mid + 1, n.children.size())));
    n.keys.subList(mid, n.keys.size()).clear();        // 从 mid 开始删（包括 up 自己）
    n.children.subList(mid + 1, n.children.size()).clear();
    return new Split(up, right);
}
```

**递归的骨架**：

```
insertInto(节点, key, rid):
    i = upperBound(节点, key)              ← 选位置/选孩子
    if 叶子:
        插入 key 和 rid 到位置 i
        if 没超限: return null             ← 分裂信号到此为止
        else: 分裂，return Split(分隔键, 右兄弟)
    else:
        s = insertInto(孩子[i], key, rid)   ← ★ 先递归
        if s == null: return null           ← 孩子没分裂，我什么都不用做
        ★ 孩子分裂了：把分隔键和右兄弟插到我这里
        if 没超限: return null
        else: 我也分裂，return Split(...)   ← 信号继续往上冒
```

**返回值是 `Split` 或 `null`** —— `null` 表示"没分裂，不用管"。这是**递归向上传递分裂信号**的经典写法（跟 B 树教材里一致）。

**三个关键细节**：

**① 叶链表维护**

```java
right.next = n.next;    // 顺序不能反！
n.next = right;
```

如果写成 `n.next = right; right.next = n.next;` —— 那 `right.next` 就指向自己了，**叶链表断掉**（range 查询会死循环）。这是典型的"链表插入顺序"陷阱。

**② 叶子"复制上提" vs 内部节点"移出上提"**

对比两处 `mid` 的处理：

| | 右侧取的范围 | 左侧删的范围 | 分隔键 |
|---|---|---|---|
| **叶子** | `[mid, size)` | `[mid, size)` | `right.keys.get(0)` —— **仍在右叶子里** |
| **内部** | `[mid+1, size)` | `[mid, size)` | `n.keys.get(mid)` —— **被移走了** |

**为什么不同？** 因为**叶子里的每一个键都必须保留**（每个键都对应真实数据）。如果像内部节点那样"移走"，这个键的数据就丢了。

**内部节点的键只是"路标"，不是数据** —— 所以 `up` 从 `n` 里删掉（`n.keys.subList(mid, ...).clear()` 从 `mid` 开始删，包括 `up` 自己）。

**这就是"叶子复制提升、内部节点移动提升"的区别**，也是 B+ 树和 B 树的关键差异之一。

**③ `new ArrayList<>(...subList(...))` 的拷贝**

```java
right.keys.addAll(new ArrayList<>(n.keys.subList(mid, n.keys.size())));
```

**这里必须先用 `new ArrayList<>` 拷贝一份**。如果直接 `right.keys.addAll(n.keys.subList(...))` 会怎样？`addAll` 会**边遍历边添加**——而源是 `n.keys` 的**视图**，下一行 `n.keys.subList(...).clear()` 会把它清空…… 实际上 `addAll` 在 `clear` 之前就完成了，所以**碰巧也能工作**。但显式拷贝**消除了这个隐蔽的顺序依赖**，是更稳的写法（代价是临时多一份列表）。

**④ `size++` 的位置**

`insert` 里 `size++` 在**最末尾**（根分裂处理之后），而空树分支是 `size = 1; return;` —— 两个分支各管一次，不会重复计数。

### `upperBound`：最核心的查找原语

```java
/** 第一个键【大于】key 的位置。内部节点用它选孩子，叶子节点用它选插入点。 */
private static int upperBound(Node n, Object key) {
    int i = 0;
    while (i < n.keys.size() && Compare.compare(n.keys.get(i), key) <= 0) i++;
    return i;
}
```

**这 4 行是整棵树的枢纽**：

- 在**内部节点**：`children[upperBound(n, key)]` 是要走的子树
- 在**叶子节点**：`upperBound` 是插入位置（**放在所有相等键之后**）

**验证正确性**：

| n.keys | key | upperBound | 含义 |
|---|---|---|---|
| `[3, 5]` | `1` | `0` | 1 < 3，走最左子树 |
| `[3, 5]` | `3` | `1` | 3 <= 3，**相等往右走** |
| `[3, 5]` | `4` | `1` | 走 `children[1]` |
| `[3, 5]` | `5` | `2` | 5 <= 5，继续往右 |
| `[3, 5]` | `7` | `2` | 7 > 5，走最右子树 |

**为什么用 `<=` 而不是 `<`？** 因为"相等的键往右走"（与不变量 2 一致）。如果改成 `<`，重复键会落到左边子树，破坏"`keys[i]` 是右子树下界"的不变量。

**复杂度**：O(节点键数) = O(MAX_KEYS) = O(4) = **常数**。真实数据库会用二分查找（节点有几百个键），本项目线性扫足够。

### `search` 和 `range`

```java
public List<int[]> search(Object key) {
    return range(key, true, key, true);           // 点查 = 闭区间的范围查
}

public List<int[]> range(Object lo, boolean loInc, Object hi, boolean hiInc) {
    List<int[]> out = new ArrayList<>();
    if (root == null) return out;                                    // 空树

    // ① 下降到"可能包含 lo 的最左叶子"
    Node n = root;
    while (!n.leaf) {
        n = n.children.get(lo == null ? 0 : upperBound(n, lo));
    }

    // ② 沿叶链表右扫，边扫边过滤
    while (n != null) {
        for (int j = 0; j < n.keys.size(); j++) {
            Object k = n.keys.get(j);
            if (lo != null) {
                int c = Compare.compare(k, lo);
                if (c < 0 || (c == 0 && !loInc)) continue;            // 低于下界 → 跳过
            }
            if (hi != null) {
                int c = Compare.compare(k, hi);
                if (c > 0 || (c == 0 && !hiInc)) return out;          // ★ 超过上界 → 直接返回
            }
            out.add(n.rids.get(j));
        }
        n = n.next;
    }
    return out;
}
```

**`search` 就是 `range` 的特例** —— 复用代码，不用写两套查找。

**① 下降的目标是"可能包含 lo 的最左叶子"**

```java
n = n.children.get(lo == null ? 0 : upperBound(n, lo));
```

**为什么这个下降是安全的？** 因为无论 `lo` 落在哪里，**包含 `lo` 的条目（如果存在）一定在从这棵叶子开始的右侧**。

- 如果 `lo` 落在 `[keys[i-1], keys[i])` 区间，`upperBound` 走 `children[i]` —— `lo` 对应的条目就在这棵子树里
- 如果 `lo` 在某棵叶子的键之间，也会落到正确的叶子
- `lo == null`（无下界）→ 从**最左叶子**开始（`children[0]`）

**下降过程中不需要回退** —— 这是 B+ 树相比二叉搜索树/普通 B 树的一大优势（普通 B 树的内部节点也可能存数据，下降时要检查沿途每个节点）。

**② `hi` 的提前返回（`return out`）**

```java
if (c > 0 || (c == 0 && !hiInc)) return out;
```

**注意是 `return` 不是 `continue`** —— 因为叶子是按 key 升序的，一旦遇到超过上界的键，后面**全部**都超过，可以直接结束。

**这就是 B+ 树有序性的价值**：范围查询不需要扫描全表，甚至可以提前终止。

**③ `out` 天然有序**

因为遍历顺序是"叶子从小到大、叶子内部从前往后"，所以 `out` 里的 RID 是**按 key 升序**的。

**`IndexScan` 的输出因此也是有序的**（注释里也说了："输出按【索引键有序】"）。

> ⚠️ **但 SQL 不保证无序查询的行序！** 所以：
> - 上层**仍然会叠 `Sort`**（如果 SQL 里有 ORDER BY）
> - 上层**仍然会叠 `Filter`**（正确性保证）
>
> **不能因为"索引输出有序"就省略 `Sort`** —— 因为：
> 1. 只有走索引的那一条路径有序，走 `SeqScan` 时无序 → **同一句 SQL 的结果顺序会随索引有无而变化**，这是不可接受的
> 2. `where age > 22 order by name` —— 索引在 `age` 上，输出按 `age` 有序，不是按 `name`

这是个**很容易想当然出错的地方**，值得在汇报时强调。

### `height()` 和 `clear()`

```java
public int size() { return size; }

public int height() { ... }   // 空树 0，只有叶子 1

public void clear() { root = null; size = 0; }
```

**`height()` 存在的意义**：**让"树在长高"这个抽象概念变得可验证**。

`BPlusTreeTest.insertManySplitsAndGrowsTree` 就断言了：

```java
// 插 100 个键之后
assertEquals(100, tree.size());
assertTrue("树应多次分裂并长高", tree.height() >= 3);
for (int k = 0; k < 100; k++) {
    assertFalse(tree.search(k).isEmpty());     // 每个键都能找到
}
```

> ★ **给数据结构加"可观测指标"（`size`/`height`）是个好习惯** —— 否则你没法用测试验证"分裂发生了"，只能靠人眼看。汇报时也可以现场展示"插 100 个键，树高 3 层"。

## 6.3 `Index`：索引元数据 + 树

文件：[`engine/index/Index.java`](src/main/java/com/course/dbms/engine/index/Index.java)，58 行。

```java
public class Index {
    private final String name;
    private final String tableName;
    private final int tableId;
    private final String columnName;
    private final int colIndex;       // 被索引列在表结构里的下标
    private final FieldType type;
    private final BPlusTree tree = new BPlusTree();    // 每个 Index 自带一棵树

    public void insert(Object key, int pageNo, int slot) { tree.insert(key, pageNo, slot); }
    public List<int[]> search(Object key) { return tree.search(key); }
    public int size() { return tree.size(); }
    public int height() { return tree.height(); }
}
```

**类注释点明了核心设计**：

> 设计取舍：索引是【派生数据】——完全可以从基表重建，因此只把元数据持久化到系统目录表 `sys_indexes`，树本身放在内存里，进程启动（`Catalog.reload`）时重扫基表重建。
> 这样索引与 WAL / 影子页事务解耦：提交后重建即可，回滚后重建即撤销。

> ★★ **这是整个索引功能最重要的一个设计决策**：**树不持久化，只重建**。
>
> **如果持久化树会怎样？**
> - B+ 树的页要跟数据页一样走 `PageManager`、缓存、影子页、WAL
> - 每次 `insert` 要么重写整棵树的页（分裂的连锁反应），要么做"写时复制"
> - 事务回滚时，树的改动也要跟着回滚 —— 需要在 WAL 里记录树的页
> - **复杂度爆炸**（这就是为什么真实数据库里索引持久化是个大工程）
>
> **重建的代价**：
> - 启动时扫全表建索引（本项目几十行，毫秒级）
> - 回滚时重建（只有建索引的事务回滚才触发）
>
> **收益**：索引代码完全不碰存储层，**逻辑隔离干净**。
>
> **什么时候重建会出问题？** 表很大（重建要几十秒）。真实数据库不允许这样，所以它们持久化索引。本项目是教学规模。

**`colIndex` 的作用**：`IndexManager.onInsert` 用它从 `Row` 里取值：

```java
idx.insert(row.get(idx.colIndex()), pageNo, slot);
```

**为什么不每次按列名查？** 因为 `onInsert` 是**每插入一行都要走**的热路径，按名字查（`schema.indexOf`）是 O(列数) 的字符串比较。预存下标省掉这部分开销。

## 6.4 `IndexManager`：索引注册表 + 维护

文件：[`engine/index/IndexManager.java`](src/main/java/com/course/dbms/engine/index/IndexManager.java)，99 行。

```java
public class IndexManager {
    private final StorageEngine se;
    private final Map<String, Index> byName = new LinkedHashMap<>();      // 索引名(小写) → 索引
    private final Map<Integer, List<Index>> byTable = new HashMap<>();    // 表 id → 该表上的索引
}
```

**两张索引表，支持两种查询**：

- `byName`：`findByName` / `hasIndex` —— 判重、`show indexes`
- `byTable`：`indexesOf(tableId)` / `onInsert` —— 插入时找"这个表上有哪些索引"

**一个 `Index` 同时出现在两张表里**（同一个对象，两个引用）。

**类注释明确了职责边界**："目录里的元数据由 `Catalog` 负责，本类只管树。"

### `createIndex`：建索引

```java
// IndexManager.java:33
public Index createIndex(String name, Table table, String columnName) {
    int colIndex = table.schema().indexOf(columnName);
    if (colIndex < 0) return null;                    // 列名不存在（由调用方先做语义校验）
    return createIndex(name, table, colIndex);
}

// IndexManager.java:40
public Index createIndex(String name, Table table, int colIndex) {
    Column col = table.schema().column(colIndex);
    Index idx = new Index(name, table.name(), table.tableId(), col.name(), colIndex, col.type());
    for (StorageEngine.Located loc : se.scanLocated(table)) {          // ★ 全表扫描建树
        idx.insert(loc.row().get(colIndex), loc.pageNo(), loc.slot());
    }
    register(idx);
    return idx;
}

private void register(Index idx) {
    byName.put(idx.name().toLowerCase(), idx);
    List<Index> list = byTable.get(idx.tableId());
    if (list == null) {
        list = new ArrayList<>();
        byTable.put(idx.tableId(), list);
    }
    list.add(idx);
}
```

**两个重载版本**：

- **按列名**（`String columnName`）—— 用户在 SQL 里写的
- **按列下标**（`int colIndex`）—— **重建索引时用**（`sys_indexes` 里存的就是 `cid`，不用再按名字找一遍）

**`LinkedHashMap` 给 `byName`** —— 保持插入顺序，所以 `show indexes` 的输出**顺序稳定**（按创建顺序）。用 `HashMap` 的话顺序不确定，测试没法断言。

**`scanLocated` 的使用** —— 这里就是 `Located` 存在的原因（需要 RID 才能建索引）。

**注意 `byTable` 用的是 `HashMap`** —— 它的遍历顺序不影响任何东西（`indexesOf` 返回的是一个 list，`onInsert` 会对每个索引都插一遍）。

### `onInsert`：唯一的索引维护钩子

```java
// IndexManager.java:63
public void onInsert(Table table, Row row, int pageNo, int slot) {
    List<Index> list = byTable.get(table.tableId());
    if (list == null) return;                          // 该表没索引，直接返回
    for (Index idx : list) idx.insert(row.get(idx.colIndex()), pageNo, slot);
}
```

**每一行插入时，把它加到该表的每一个索引上**。

- `row.get(idx.colIndex())` —— 从行里取出被索引列的值
- `idx.insert(值, pageNo, slot)` —— 记进 B+ 树

**`if (list == null) return;`** —— 快速路径：**没建索引的表插入零额外开销**。这保证了"不用索引时行为与改造前完全一致"。

**为什么不需要"按列名找索引"再匹配？** 因为**一次插入要维护该表的所有索引**，不能只维护 WHERE 里用到的那个。

例：`users` 表上有 `idx_age`（age 列）和 `idx_name`（name 列）。插一行时**两个索引都要更新**。

### 三个查询方法

```java
/** 某张表上的所有索引（无则空列表）。 */
public List<Index> indexesOf(int tableId) {
    List<Index> list = byTable.get(tableId);
    return list == null ? new ArrayList<>() : list;    // ★ 永远不返回 null
}

/** 找某表某列上的索引；没有则 null。规划器据此决定走 IndexScan 还是 SeqScan。 */
public Index findFor(Table table, String columnName) {
    for (Index idx : indexesOf(table.tableId())) {
        if (idx.columnName().equalsIgnoreCase(columnName)) return idx;
    }
    return null;
}

public boolean hasIndex(String name) { return byName.containsKey(name.toLowerCase()); }
public List<Index> all() { return new ArrayList<>(byName.values()); }
public void clear() { byName.clear(); byTable.clear(); }
```

**`indexesOf` 永远不返回 null** —— 这条"永远不会 null"的约定让调用方（`findFor`、`onInsert`）不用判空。**这是很好的 API 设计习惯**（JDK 的 `Collections.emptyList()` 就是这个思路）。

**`hasIndex` 和 `byName` 都用小写** —— 索引名大小写不敏感。`create index IDX on ...` 和 `create index idx on ...` 是同一个名字。

**`all()` 返回拷贝** —— 防止调用方修改内部 map 的 values 视图。

## 6.5 持久化与事务（索引的完整生命周期）

这一节把前面的片段串起来，讲清**索引在整个系统里是怎么活下来的**。

### 建索引的完整流程

```
SQL: create index idx_age on users(age);

① Parser
   parseCreate() 里 match(INDEX) → parseCreateIndex()
   → CreateIndexStmt{indexName="idx_age", tableName="users", columnName="age"}

② Analyzer.analyzeCreateIndex
   - catalog.getTable("users")        → 表存在（否则 TB-0001）
   - schema.indexOf("age") >= 0       → 列存在（否则 SE-0004）
   - !catalog.hasIndex("idx_age")     → 名字不重复（否则 SE-0007）

③ PlanBuilder.buildCreateIndex
   → Plan{root=CreateIndex(catalog, "idx_age", "users", "age"), columns=["result"]}

④ Executor
   → CreateIndex.execute()
     → catalog.createIndex("idx_age", "users", "age")

⑤ Catalog.createIndex
   - se.insert(sysIndexes, Row.of(10, "idx_age", "age", 2))     ← 元数据落盘/进工作集
   - indexManager.createIndex("idx_age", users, 2)

⑥ IndexManager.createIndex
   - new Index(...)
   - for (Located loc : se.scanLocated(users))                   ← 全表扫描
       idx.insert(loc.row().get(2), loc.pageNo(), loc.slot())     ← 灌进 B+ 树
   - register(idx)                                                ← 登记到 byName / byTable

⑦ 返回 Row.of("index 'idx_age' created")
```

### 持久化的"两级"设计

| 数据 | 存在哪 | 怎么恢复 |
|---|---|---|
| **索引元数据**（名字、表、列、cid） | `sys_indexes` **表**（落盘） | `Catalog.reload()` 读出来 |
| **索引内容**（B+ 树） | **内存** | `rebuildIndexes()` 扫基表重建 |

**两级设计的连接点**：

```java
// Catalog.reload() 末尾
rebuildIndexes();

// Catalog.rebuildIndexes()
indexManager.clear();
for (Row r : se.scan(sysIndexes)) {              // ← 读元数据（一级）
    ...
    indexManager.createIndex(name, t, cid);       // ← 重建树（二级）
}
```

### 四种场景的正确性推演

**场景 A：进程重启**

```
启动 → new Database(dir) → new Catalog(se) → reload() → rebuildIndexes()
     → 读 sys_indexes 拿到 idx_age → 扫 users 表 → 树建好了
```

✅ 索引自动恢复。`IndexTest.indexSurvivesReopen` 验证了这一点。

**场景 B：事务内建索引然后回滚**

```
begin;
create index idx_age on users(age);
  → sys_indexes 的新行进了**工作集**（影子页），磁盘没变
  → indexManager 里**有** idx_age（内存登记了）
rollback;
  → Txn.clearCurrent()          脱离事务
  → rollbackTxn(t)              丢弃工作集（sys_indexes 的新行没了）
  → catalog.reload()
      → tables.clear() + 重读 sys_tables
      → rebuildIndexes()
          → indexManager.clear()          ← 清空所有索引！
          → 重读 sys_indexes（读磁盘，只有旧索引）
          → 只重建旧的
  → idx_age 消失了
```

✅ 回滚撤销建索引。`IndexTest.indexRolledBackWithTransaction` 验证。

**场景 C：事务内建索引然后提交**

```
begin;
create index idx_age on users(age);
commit;
  → log.writeCommit(txnId, 工作集)     ← WAL 里记下 sys_indexes 的页
  → 写数据页（sys_indexes 的页落盘）
  → 清工作集
  → 锁释放
  → indexManager 里 idx_age 还在（不需要重建）
```

✅ 提交后索引保留。`IndexTest.indexCommittedWithTransaction` 验证。

**场景 D：建索引后插入新行**

```
create index idx_age on users(age);          -- 树里有 4 行的 RID
insert into users values (4, 'dave', 30, 99.0);
  → Insert.execute()
      → se.insert(...) 返回 rid = [0, 4]
      → indexes.onInsert(users, row, 0, 4)    ← ★ 钩子
          → 对 users 的每个索引：idx.insert(30, 0, 4)
  → 树里有 5 行的 RID

select id from users where age = 30;
  → IndexScan 用树查 age=30 → 返回 [1,2] 和 [0,4] 的 RID
  → 回表取出 3 行：bob(30), dave(30)... 
```

✅ 新行能被索引查到。`IndexTest.indexIsMaintainedOnLaterInserts` 验证，`E2ETest` 里也通过 DEMO.sql 验证了（"索引扫描应查到建索引后插入的 dave"）。

### 并发安全（为什么索引不需要加锁）

**核心论证**：`IndexManager` 的 `byName` / `byTable` 不是线程安全的（`LinkedHashMap`/`HashMap`），**但索引的访问被表级锁串行化了**：

| 操作 | 加的锁 | 为什么够 |
|---|---|---|
| `insert`（维护索引） | 目标表 **X 锁** | X 锁互斥，所以同一时刻只有一个线程在改这棵树的 same 表 |
| `create index`（建树） | 目标表 **X 锁** | 建树时扫全表，X 锁挡住并发写（否则扫描期间插入的行会漏进索引） |
| `select` 走 IndexScan（读树） | 目标表 **S 锁** | S 锁与 X 互斥，所以读树时没有人在改树 |

**关键的一行代码在 `Session.lockRequest`**：

```java
} else if (stmt instanceof CreateIndexStmt) {
    // 建索引要扫全表，必须挡住并发写（否则扫描期间插入的行会漏进索引）
    CreateIndexStmt ci = (CreateIndexStmt) stmt;
    Table t = db.catalog().getTable(ci.tableName);
    req.add(new int[]{SYS_TABLES, 0});
    req.add(new int[]{SYS_INDEXES, 1});      // ← sys_indexes 加 X 锁
    req.add(new int[]{t.tableId(), 1});      // ← 目标表加 X 锁
}
```

> ★ **这条注释点出了一个真实的并发陷阱**：如果不加目标表的 X 锁，会有这个场景：
>
> ```
> 时刻  A: create index idx on users(age);    ← 开始扫全表建树
>  t1       扫到第 3 页
>  t2   B: insert into users values (5,...);  ← 并发插入
>  t3       B 的 onInsert 发现 users 上还没有索引（A 还没 register）
>  t4       → 这一行没有进索引
>  t5   A: 扫完（没扫到新插入的行）→ register(idx) → 树里少了 B 插入的那行
> ```
>
> **结果：索引与实际数据不一致**。`select ... where age = <B插入的值>` 会漏行。
>
> 加了 X 锁后，B 必须等 A 建完索引才能插入，而 B 的 `onInsert` 就会发现索引已存在，正常维护。
>
> **汇报时这是个很好的"并发正确性"案例。**

**`select` 走索引时加 S 锁**（`lockRequest` 的 `SelectStmt` 分支无条件加目标表的 S 锁）—— 挡住并发的写事务，所以读树时树不会被改。

**代价**：`show indexes` 也要对 `SYS_INDEXES` 加 S 锁（见 `lockRequest` 的 `ShowStmt` 分支），这会跟 `create index` 的 X 锁竞争。**但这是正确的**——`show indexes` 读 `indexManager.all()`，如果并发有 CREATE INDEX 在改，可能读到中间状态。

## 6.6 `IndexScan` 算子与规划器规则

算子的实现细节见 §5.4，这里重点讲**规划规则**——即"什么时候能用索引"。

### 规划规则总表

| 规则 | 判定 | 例子 |
|---|---|---|
| **① 只看 WHERE** | 没有 WHERE 就不选索引 | `select * from users` → SeqScan |
| **② 只在合取项里找** | 顶层 CMP 或 AND 树下的 CMP | `where a>1 and b<2` → 可以只看 `a>1` |
| **③ 不深入 OR** | OR 整体放弃 | `where a>1 or b<2` → SeqScan |
| **④ 只认 `列 op 字面量`** | `column2 != null`（列 vs 列）跳过 | `where a = b` → SeqScan |
| **⑤ 只认 5 个运算符** | `= < > <= >=`；`<>` 不适用 | `where a <> 1` → SeqScan |
| **⑥ 列上必须有索引** | `indexManager.findFor(table, col)` | 没建索引 → SeqScan |
| **⑦ 只取第一个命中的** | `return`（不是收集所有） | `where a>1 and b<2` 都有索引 → 用 `a` 的 |
| **⑧ Filter 永远保留** | `PlanBuilder` 里 Filter 无条件叠在 IndexScan 之上 | 见下 |

### ★ 规则⑧：为什么 Filter 一定要保留

```java
// PlanBuilder.java:108
Operator root = indexScanFor(table, sel.where);
if (root == null) root = new SeqScan(se, table);
if (sel.where != null) root = new Filter(root, schema, sel.where);   // ← 无条件
```

**这是"索引是纯优化，不改变语义"的保证**。三个层面：

**① 范围精度问题**

`where age > 22 and score > 90` → 索引只用 `age`（因为规则⑦只取第一个）→ 扫描结果是"age > 22 的所有行"，**比 SQL 要求的多了**。Filter 会把它过滤到正确。

**② 边界处理问题**

即使只有 `where age > 22`，索引扫出来的行理论上就是正确的。但万一 `range` 的边界实现有 bug（比如 `loInc` 搞反了），Filter 会**兜底**。

**③ 类型/cast 的一致性**

索引里的键是建索引时 cast 过的；Filter 里又 cast 一次。**两处独立计算**，互为校验。

> ★ **这个"多一层保险"的设计让索引功能的回归风险降到接近零**：
> - 索引的边界算错了 → Filter 纠正
> - 索引漏了行 → **不能纠正**（数据少了，Filter 无能为力）⬅ 这是唯一的风险点
>
> 所以 `IndexTest` 里最重要的一组测试是**"有索引和无索引的结果完全相同"**：
>
> ```java
> @Test public void indexGivesSameRowsAsSeqScan() {
>     Result without = s.execute("select id, name from users where age > 22 order by id");
>     s.execute("create index idx_age on users(age)");
>     Result with = s.execute("select id, name from users where age > 22 order by id");
>     assertEquals(col(without, 0), col(with, 0));
>     assertEquals(col(without, 1), col(with, 1));
> }
> ```
>
> 这是**差分测试（differential testing）** —— 用"未经优化的路径"当正确答案的参照。**这是验证优化正确性最有力的手段**，值得在汇报里讲。

### 规划器的递归下降：`collectConjuncts`

```java
// PlanBuilder.java:269
private void collectConjuncts(Cond cond, List<Cond> out) {
    if (cond == null) return;
    if (cond.kind == Cond.Kind.CMP) {
        out.add(cond);
    } else if (cond.kind == Cond.Kind.AND && cond.children != null) {
        for (Cond child : cond.children) collectConjuncts(child, out);
    }
}
```

**为什么 `OR` 没有分支？** 因为它**不递归进去**，所以 OR 的子树被完全忽略。

**举几个例子**：

| WHERE AST | 收集到的合取项 |
|---|---|
| `CMP(age>22)` | `[age>22]` |
| `AND[CMP(age>22), CMP(score>90)]` | `[age>22, score>90]` |
| `AND[CMP(a=1), AND[CMP(b=2), CMP(c=3)]]` | `[a=1, b=2, c=3]`（递归展开嵌套 AND） |
| `OR[CMP(a=1), CMP(b=2)]` | `[]`（**空**） |
| `OR[AND[CMP(a=1),CMP(b=2)], CMP(c=3)]` | `[]`（空——OR 在顶层） |
| `AND[CMP(a=1), OR[CMP(b=2),CMP(c=3)]]` | `[a=1]`（OR 子树被跳过，但 AND 的其他分支保留） |

**最后一行是关键**：`where a=1 and (b=2 or c=3)` 依然可以用 `a` 的索引！

因为 `a=1` 是合取项——满足整个 WHERE 的行**必然满足** `a=1`。所以用 `a` 的索引缩小范围是**安全的**（不会漏行）。

**这就是"合取项"这个概念的价值**：从 AND 树里挑出一个能缩小范围的子条件，剩下的交给 Filter。

### 计划树对比（汇报演示的核心）

**建索引前**：

```java
Database db = ...;
Plan p = ...;   // 内部状态
// toString → "Project[0, 1](Filter(age > 22)(SeqScan(users)))"
```

**建索引后**：

```java
// toString → "Project[0, 1](Filter(age > 22)(IndexScan(users)))"
```

**同一条 SQL，计划形状从 `SeqScan` 变成 `IndexScan`，结果行完全一致。**

`PlanBuilderTest` 里就是这么断言的：

```java
@Test public void indexReplacesSeqScanWhenAvailable() {
    // 断言 Project → Filter → IndexScan
}

@Test public void seqScanWhenNoIndex() {
    // 没建索引时仍是 Project → Filter → SeqScan
}
```

### 规则覆盖测试

`PlanBuilderTest` 里有一整套"规则边界"测试，对应上面的规则③④：

| 测试 | 规则 | 断言 |
|---|---|---|
| `indexUsedForConjunctInsideAnd` | ② | `a>1 and b>2` → 走 `a` 的索引 |
| `orConditionFallsBackToSeqScan` | ③ | `a>1 or b>2` → SeqScan |
| `notEqualFallsBackToSeqScan` | ⑤ | `a <> 1` → SeqScan |
| `indexIgnoredWhenPredicateIsOnOtherColumn` | ⑥ | 索引在 `age` 上，`where name='x'` → SeqScan |
| `indexRangeBoundsFollowOperator` | ⑤ | 验证 `=`/`>`/`>=`/`<`/`<=` 各自的 lo/hi/含端点 |

**最后一条值得细看** —— 它逐个验证了运算符到区间的映射：

```java
// 示意
assertEquals(30, ((IndexScan) op).lower());
assertTrue(((IndexScan) op).lowerInclusive());     // >= 是闭
```

## 6.7 ★ 关键 API 在索引改造中的演进

这部分讲"为了加索引，哪些既有 API 做了妥协"——**这是汇报时体现工程思维的地方**。

| 需求 | 原有 API 的问题 | 解决方案 | 是否破坏兼容 |
|---|---|---|---|
| 索引需要行的位置 | `Row` 没有 RID | 新增 `Located` 载体类 + `scanLocated` | ✅ 不破坏（新增方法） |
| 索引要按 RID 取行 | `scan` 只有"全表" | 新增 `fetch(table, pageNo, slot)` | ✅ 不破坏 |
| `Insert` 要维护索引 | 构造器只有 3 个参数 | 加 4 参构造器，**保留** 3 参委托 null | ✅ 不破坏（旧测试仍能用） |
| `Filter` 要支持多表 | 只接受 `Schema` | 加 4 参构造器（收 `Resolver`），2 参委托 lambda | ✅ 不破坏 |
| `ShowStmt` 要支持 `show indexes` | 只有 `list`/`tableName` | 加 `indexes` 字段，**保留** 2 参构造器 | ✅ 不破坏 |
| `Page.records()` 不给槽位号 | — | 不改造，`scanLocated` 自己数槽位 | ✅ 不破坏 |

> ★ **统一的模式：加新方法/新构造器，旧的保留并委托**。
>
> 好处：**130 个测试一个都不用改**（除了新增的），"93 个旧测试全绿"这句话才有说服力。
>
> 代价：类里多了一些看起来冗余的构造器。但这是**演进式设计的正常成本**。

## 6.8 相关错误码

| 错误码 | 场景 | 在哪抛 |
|---|---|---|
| `SE-0004` | 索引的列不存在 | `Analyzer.analyzeCreateIndex` / `Catalog.createIndex` |
| `SE-0007` | 索引名已存在 | `Analyzer.analyzeCreateIndex` / `Catalog.createIndex` |
| `TB-0001` | 索引/`show indexes` 的表不存在 | `Catalog.getTable` |
| `LX-0001` | 用了 `--` 注释（词法不支持） | `Lexer` |
| `SY-0001` | `create index` 语法不完整 | `Parser.parseCreateIndex` |

**注意 `SE-0007` 是本次新增的错误码** —— 原来的 `Error` 类注释里 `SE` 是"语义"前缀，`SE-0007` 之前没用过（`SE-0007` 在 `StorageEngine.rowToBytes` 里也用作"序列化失败"，是个**冲突**——不过那是个几乎不可能触发的路径）。

> ⚠️ 如果老师问"你的错误码有冲突吗"，可以坦率说：`SE-0007` 在 `StorageEngine.rowToBytes` 的 `catch (IOException)` 里被用作"序列化失败"，在 `Analyzer`/`Catalog` 里被用作"索引名已存在"。这是因为两个场景都很罕见（一个是 IO 异常，一个是 DDL 冲突），且都在不同模块。**这是个可以改进的点**——严格的错误码表应该集中定义。

## 6.9 第 6 章自问自答

**Q：为什么 MAX_KEYS = 4？**
A：便于演示。插 5 个键就分裂、插 100 个键树高到 3~4 层，能直观展示"分裂"和"长高"。真实数据库用几百。

**Q：叶子为什么允许重复键？**
A：因为被索引的列（如 `age`）**本来就有重复值**。如果假设唯一，`age=30` 只能返回一行——**错**。叶子存**有序的 `(key, RID)` 条目**，`search` 返回**全部**相等键的 RID。

`BPlusTreeTest.searchReturnsAllRidsForDuplicateKeys` 和 `IndexTest.equalityScanFindsDuplicates` 都验证了这一点。

**Q：`IndexScan` 的输出有序，为什么不省掉 `Sort`？**
A：因为：
1. 只是"按索引键有序"，不是"按 ORDER BY 的列有序"
2. 有索引/无索引两条路径的输出顺序必须一致，否则同一句 SQL 的结果顺序会随索引有无而变化

**Q：索引的 `range` 为什么从"可能包含 lo 的最左叶子"开始就够了？**
A：因为 B+ 树**所有数据都在叶子**且叶子按 key 升序串联。从可能包含 `lo` 的叶子开始向右扫，不会漏掉任何 `>= lo` 的条目。

**Q：如果一条 SQL 的 WHERE 里有两个列都有索引，用哪个？**
A：用 `collectConjuncts` 遍历时**第一个命中的**（`return` 而不是继续）。顺序取决于 WHERE 的书写顺序和 AND 树的展平顺序。

**可以改进**：选择"选择性更高"的索引（估算哪个能过滤掉更多行），或者做**索引合并**。这是真正的查询优化器做的事。

**Q：索引能加速 JOIN 吗？**
A：**目前不能**。因为 `buildSelectRich` 里 Join 的两侧都是硬编码的 `SeqScan`：

```java
Operator root = new SeqScan(se, tables.get(0));
for (int k = 1; k < tables.size(); k++) {
    root = new Join(root, new SeqScan(se, tables.get(k)), ...);
}
```

**索引嵌套循环连接**（Index Nested Loop Join）就是在这里改造：右表的 `SeqScan` 换成 `IndexScan`，用左行当前的连接列值作为索引的查询键——**每次左行不同的键查一次索引**。这是"索引加速 JOIN"的标准做法，也是本项目明确的**扩展方向**。

**Q：为什么索引不做删除维护？**
A：因为系统没有 DELETE/UPDATE。`StorageEngine.insert` 是唯一写路径（见 §2.6 的 `Insert` 算子分析）。**加 DELETE 时的改动清单**：`Parser` + `DeleteStmt` + `Analyzer` + `PlanBuilder` + `Delete` 算子 + `Page.deleteRecord(slot)` + `BPlusTree.delete`（复杂！）。

---

# 第 7 章 客户端 / 服务器 通信

课程图片"客户端与服务器端交互"。这一层的关键词：**协议、帧、编码**。

## 7.1 协议设计：为什么需要"帧"

TCP 是**字节流**协议，不保留消息边界。如果你连续 `write("abc")` 和 `write("def")`，接收方可能一次 `read` 到 `"abcdef"`（粘包），也可能分两次读到 `"ab"` 和 `"cdef"`（拆包）。

**解决方案：长度前缀（length-prefixed framing）** —— 每条消息前面写一个 4 字节的长度，接收方先读长度再读那么多个字节。

### `Package`：一个消息

文件：[`protocol/Package.java`](src/main/java/com/course/dbms/protocol/Package.java)，16 行。

```java
public class Package {
    public final boolean error;
    public final byte[] payload;

    public Package(boolean error, byte[] payload) { ... }
}
```

**双向都用这个类**：

| 方向 | `error` | `payload` |
|---|---|---|
| 客户端 → 服务端 | 恒为 `false` | SQL 文本（UTF-8 字节） |
| 服务端 → 客户端 | `false` = 成功 | `Result` 的编码 |
| 服务端 → 客户端 | `true` = 出错 | 错误信息文本（`Error.toString()`） |

### `Packager`：帧的读写

文件：[`protocol/Packager.java`](src/main/java/com/course/dbms/protocol/Packager.java)，31 行。

**帧格式**：

```
[4字节 payload_len][1字节 err_flag][payload_len 字节的 payload]
     ↑ 大端             ↑ 0=成功 1=错误
```

```java
public static void write(DataOutputStream out, Package pkg) throws IOException {
    out.writeInt(pkg.payload.length);
    out.writeByte(pkg.error ? 1 : 0);
    out.write(pkg.payload);
    out.flush();                                   // ★ 立即刷新！
}

public static Package read(DataInputStream in) throws IOException {
    int len = in.readInt();
    byte err = in.readByte();
    byte[] payload = new byte[len];
    in.readFully(payload);                         // ★ 保证读满
    return new Package(err == 1, payload);
}
```

**两个关键点**：

**① `readFully` 而不是 `read`**

```java
byte[] payload = new byte[len];
in.readFully(payload);
```

`InputStream.read(byte[])` **不保证读满**——可能只读了一部分就返回（返回实际读到的字节数）。`DataInputStream.readFully` 会**循环读直到读满或 EOF**。

**这是网络编程最常见的 bug 之一**：用 `read` 会导致"大部分时候正常、偶尔截断"的诡异问题。

**② `flush()` 在 `write` 里**

因为 `ConnectionHandler` 用的是 `BufferedOutputStream`（缓冲写，提高性能）。**不 flush 就发不出去**。

项目里没有用 `try-with-resources` 自动 flush，而是**在 `Packager.write` 里显式 flush** —— 保证每次写完消息立即发出。

### `Encoder` / `Decoder`：结果的序列化

文件：[`protocol/Encoder.java`](src/main/java/com/course/dbms/protocol/Encoder.java)（48 行）、[`Decoder.java`](src/main/java/com/course/dbms/protocol/Decoder.java)（39 行）。

**`Result` 的二进制布局**：

```
[int 列数 nc]
  { [int 长度][utf8 列名] } × nc
[int 行数 nr]
  { [int 长度][utf8 值文本] } × (nr × nc)
```

```java
public static byte[] encodeResult(Result r) throws IOException {
    ByteArrayOutputStream bos = new ByteArrayOutputStream();
    DataOutputStream out = new DataOutputStream(bos);
    out.writeInt(r.columns.size());
    for (String c : r.columns) writeString(out, c);
    out.writeInt(r.rows.size());
    for (Row row : r.rows) {
        for (int i = 0; i < r.columns.size(); i++) {
            writeString(out, String.valueOf(row.get(i)));      // ★ 一切转文本
        }
    }
    out.flush();
    return bos.toByteArray();
}

private static void writeString(DataOutputStream out, String s) throws IOException {
    byte[] b = s.getBytes(StandardCharsets.UTF_8);
    out.writeInt(b.length);
    out.write(b);
}
```

> ★ **关键设计：所有值都转成字符串传输**。
>
> `String.valueOf(row.get(i))` —— 不管值是 `Integer`、`Double` 还是 `String`，一律转文本。
>
> **为什么？**
> - **简单**：不用为 6 种类型各写一套编解码
> - **解耦**：客户端**完全不需要知道类型**，只负责显示
> - **够用**：这个协议只用于"展示结果"，不是用于数据交换
>
> **代价**：
> - 类型信息丢失（客户端拿到的是字符串）
> - 传输体积略大（`1234` 是 4 字节文本 vs 4 字节 int，但 `1` 是 1 字节文本 vs 4 字节 int——其实**文本常常更小**！）
>
> **这不是一个通用的序列化协议**（比如不能用它做备份/复制）。但对"客户端展示"这个用途是**恰好合适**的。

**`Decoder` 严格镜像 `Encoder`**：

```java
public static Result decodeResult(byte[] payload) throws IOException {
    DataInputStream in = new DataInputStream(new ByteArrayInputStream(payload));
    int nc = in.readInt();
    List<String> cols = new ArrayList<>();
    for (int i = 0; i < nc; i++) cols.add(readString(in));
    int nr = in.readInt();
    List<Row> rows = new ArrayList<>();
    for (int i = 0; i < nr; i++) {
        List<Object> vals = new ArrayList<>();
        for (int j = 0; j < nc; j++) vals.add(readString(in));    // ★ 值也是 String
        rows.add(new Row(vals));
    }
    return new Result(cols, rows);
}
```

**注意：客户端拿到的值也是 `String`**（不是原始类型）。所以 `Renderer.cell(v)` 里的 `String.valueOf(v)` 对客户端来说是 `String.valueOf(String)` —— 无害。

**`ProtocolTest` 验证了这个往返**：

```java
@Test public void resultRoundTrip() { /* 编码 → 解码 → 内容相同 */ }
@Test public void emptyResultRoundTrip() { /* 0 列 0 行也不出错 */ }
@Test public void packagerFrameRoundTrip() { /* 帧格式往返 */ }
```

## 7.2 服务端

### `Server`：监听 + 每连接一个线程

文件：[`server/Server.java`](src/main/java/com/course/dbms/server/Server.java)，59 行。

```java
public class Server implements Runnable, Closeable {
    private final Database db;
    private final ServerSocket ss;
    private final List<Thread> handlers = new ArrayList<>();

    public Server(Database db, int port) throws IOException {
        this.db = db;
        this.ss = new ServerSocket(port);
    }

    public int port() { return ss.getLocalPort(); }        // ★ 端口传 0 时返回实际分配的端口

    public void start() {
        Thread t = new Thread(this, "dbms-server");
        t.setDaemon(true);                                  // 守护线程
        t.start();
    }

    @Override public void run() {
        Log.info("[SERVER] listening on " + ss.getInetAddress() + ":" + ss.getLocalPort());
        try {
            while (!ss.isClosed()) {
                Socket s = ss.accept();                     // 阻塞等连接
                Log.info("[SERVER] client connected: " + s.getRemoteSocketAddress());
                Thread th = new Thread(new ConnectionHandler(db, s), "dbms-client");
                th.setDaemon(true);
                th.start();                                 // ★ 每个连接一个线程
                handlers.add(th);
            }
        } catch (SocketException ignored) {
            // 关闭引起的
        } catch (IOException e) {
            Log.error("[SERVER] accept error: " + e.getMessage());
        }
    }

    @Override public void close() throws IOException {
        ss.close();                                          // 关闭监听 → accept 抛 SocketException → 循环退出
    }
}
```

**几个设计要点**：

**① `port()` 返回实际端口**

```java
public int port() { return ss.getLocalPort(); }
```

**这是给测试用的**：`new Server(db, 0)` 让 OS 分配一个空闲端口，然后用 `server.port()` 拿到。这样**多个测试可以并行跑**，不会抢 9999 端口。

`ServerClientTest` 和 `E2ETest` 都用了这个技巧。

**② 每个连接一个线程（thread-per-connection）**

```
主线程: accept() → 起线程 → accept() → 起线程 → accept() ...
线程1: ConnectionHandler(连接A)
线程2: ConnectionHandler(连接B)
```

**优点**：简单、直观，每个连接的代码是顺序执行的（不用写回调/状态机）。

**缺点**：连接数多时线程开销大（每线程约 1MB 栈）。生产级用 NIO + 事件循环（Netty）或虚拟线程（Java 21）。

**本项目选 thread-per-connection**：因为要演示"并发 + 锁"，多线程模型最直观。

**③ `setDaemon(true)`**

守护线程 —— JVM 退出时不等待它们结束。**测试里很重要**：`E2ETest` 跑完如果不关服务端，非守护线程会让 JVM 挂住。

**④ `close()` 的技巧**

```java
ss.close();
```

关闭 `ServerSocket` 会让阻塞在 `accept()` 的线程抛 `SocketException`，被 `catch (SocketException ignored)` 吞掉，循环条件 `!ss.isClosed()` 变 false，线程退出。**一个 `close()` 就优雅停止了整个 accept 循环。**

**⑤ `handlers` 列表被收集但从不清除**

```java
private final List<Thread> handlers = new ArrayList<>();
...
handlers.add(th);
```

**这是个小的内存泄漏**（连接多了会一直增长）。如果要修，可以在 `ConnectionHandler` 结束时从列表里移除。**但对短生命周期的演示无害**。

### `ConnectionHandler`：一个连接的处理循环

文件：[`server/ConnectionHandler.java`](src/main/java/com/course/dbms/server/ConnectionHandler.java)，64 行。

```java
@Override public void run() {
    try (Socket s = socket;
         DataInputStream in = new DataInputStream(new BufferedInputStream(s.getInputStream()));
         DataOutputStream out = new DataOutputStream(new BufferedOutputStream(s.getOutputStream()))) {

        Session session = new Session(db);                  // ★ 一个连接一个会话
        while (true) {
            Package pkg;
            try {
                pkg = Packager.read(in);
            } catch (EOFException e) {
                break;                                      // 对端关闭
            }
            String sql = new String(pkg.payload, StandardCharsets.UTF_8);
            Log.info("[client " + s.getRemoteSocketAddress() + "] " + sql);
            try {
                Result r = session.execute(sql);
                Packager.write(out, new Package(false, Encoder.encodeResult(r)));
            } catch (Error e) {
                Log.warn("[client " + s.getRemoteSocketAddress() + "] " + e.toString());
                Packager.write(out, new Package(true, Encoder.encodeError(e)));
            }
        }
        Log.info("[client " + s.getRemoteSocketAddress() + "] disconnected");
    } catch (IOException e) {
        Log.error("[conn] " + e.getMessage());
    }
}
```

**几个关键点**：

**① `try-with-resources`**

```java
try (Socket s = socket;
     DataInputStream in = ...;
     DataOutputStream out = ...) { ... }
```

三个资源自动关闭。**注意 `Socket s = socket` 而不是 `Socket s = new Socket(...)`** —— 因为 socket 是构造时传入的，这里只是"接管所有权"。

**② `Session session = new Session(db)` 在循环外**

**一个连接一个 Session，整个连接生命周期内复用**。这一点很重要：

- `Session` 持有 `Database` 引用和 `LockManager` 引用
- 事务状态（`Txn.current()`）是 **ThreadLocal**，而这个线程处理一个连接，所以"一个连接 = 一个线程 = 一个事务上下文"

**这就是整个并发模型的基础**：连接 → 线程 → ThreadLocal 隔离。

**③ `catch (EOFException e) break;`**

`Packager.read` 的第一行是 `in.readInt()`。如果对端关闭了连接，会抛 `EOFException`。**这是"客户端断开"的正常信号**，不是错误——所以 `break` 退出循环而不是报错。

**④ 内层的 `try/catch (Error e)`**

**这是全项目唯一的错误捕获点**（除了 `Lexer`/`Parser` 内部的细节）：

```java
try {
    Result r = session.execute(sql);
    Packager.write(out, new Package(false, Encoder.encodeResult(r)));
} catch (Error e) {
    Packager.write(out, new Package(true, Encoder.encodeError(e)));
}
```

**为什么这个位置？** 因为要保证**每个请求必有一个响应**（请求-响应协议的要求）。失败时回一个 `error=true` 的包，客户端就知道出错了。

**注意 `catch (Error e)` 只捕获业务错误** —— 如果是 `NullPointerException` 之类的代码 bug，**不捕获**，会冒到外层（`catch (IOException)` 也接不住，会导致线程死亡）。这是**有意的**：

- 业务错误（表不存在、类型不匹配）→ 回错误包，客户端可以继续
- 代码 bug → 让线程死掉，暴露问题

**⑤ 循环继续**

```java
while (true) { ... }        // 出错后继续读下一个请求
```

**一个请求出错不会断开连接**。客户端可以继续发下一条 SQL。这是交互式客户端的必要条件（否则打错一个表名就要重连）。

## 7.3 客户端

### `ClientLauncher`：入口 + 参数解析

文件：[`client/ClientLauncher.java`](src/main/java/com/course/dbms/client/ClientLauncher.java)，41 行。

```
用法：client [-e SQL] [-f 文件] [host] [port]
  -e SQL   直接执行一条 SQL 后退出
  -f 文件  批量执行文件中的语句后退出
  缺省 host=127.0.0.1 port=9999
```

```java
for (int i = 0; i < args.length; i++) {
    switch (args[i]) {
        case "-e": sql = args[++i]; break;
        case "-f": file = args[++i]; break;
        default:
            if (host == null) host = args[i];
            else port = Integer.parseInt(args[i]);
    }
}
```

**"第一个非选项参数是 host，第二个是 port"** —— 手写的参数解析（零依赖）。`args[++i]` 先自增再取值，跳过选项的参数。

```java
try (Socket socket = new Socket(host, port)) {
    Shell shell = new Shell(socket);
    if (sql != null) { shell.execute(sql); return; }        // -e 模式
    if (file != null) { shell.executeFile(file); return; }   // -f 模式
    shell.run();                                             // 交互模式
}
```

**三种模式**：

| 模式 | 用途 |
|---|---|
| `-e "select ..."` | 单条 SQL，适合脚本/测试 |
| `-f DEMO.sql` | 批量执行文件，**演示用** |
| 无参数 | 交互式 REPL |

### `Shell`：交互 shell

文件：[`client/Shell.java`](src/main/java/com/course/dbms/client/Shell.java)，133 行。

**① 交互模式的元命令**

```java
if (t.equals("\\q") || t.equals("\\quit") || t.equals("exit")) { ... break; }
if (t.equals("\\dt")) { send("show tables"); continue; }                  // 列出表
if (t.startsWith("\\d ")) { send("show table " + t.substring(3).trim()); continue; }   // 看表结构
if (t.startsWith("\\")) { System.out.println("unknown command: " + t); continue; }
```

**`\dt` / `\d <表>` 是 psql 的风格** —— 它们**转换成 SQL 发给服务端**（`show tables` / `show table x`），而不是在客户端本地处理。

这是个好设计：**元命令不需要新的协议消息类型**，复用现有的 SQL 通道。

**② 分号判定的状态机**

```java
/** 找第一个"不在单引号字符串内"的分号下标；无则 -1。 */
private int topLevelSemicolon(String s) {
    boolean inQuote = false;
    for (int i = 0; i < s.length(); i++) {
        char c = s.charAt(i);
        if (c == '\'') inQuote = !inQuote;
        else if (c == ';' && !inQuote) return i;
    }
    return -1;
}
```

**必须处理引号内的分号**：

```sql
insert into t values ('a;b');      -- 这个分号在字符串里，不是语句结束
```

用状态机扫一遍（`inQuote` 取反）就对了。

**③ 累积缓冲 + drain**

```java
private final StringBuilder buf = new StringBuilder();

public void run() throws IOException {
    while (true) {
        System.out.print(pending() ? "     " : "dbms> ");      // 续行提示符
        String line = console.readLine();
        if (line == null) break;                                // EOF（Ctrl+D）
        String t = line.trim();
        if (!pending()) { /* 处理元命令... */ }
        buf.append(line).append('\n');                          // 累积
        drain();                                                // 抽出发送
    }
}
```

**`drain()` 抽出所有完整的语句**：

```java
private void drain() throws IOException {
    String text = buf.toString();
    int idx = topLevelSemicolon(text);
    while (idx >= 0) {
        String stmt = text.substring(0, idx + 1).trim();
        text = text.substring(idx + 1);
        buf.setLength(0);
        buf.append(text);
        if (!stmt.isEmpty()) send(stmt);
        idx = topLevelSemicolon(buf.toString());                 // ← 用更新后的 buf 继续找
    }
    if (buf.toString().trim().isEmpty()) buf.setLength(0);       // 清掉纯空白残留
}
```

**为什么用 `while` 而不是 `if`？** 因为一次输入可能有多条语句：

```sql
select 1; select 2;
```

**最后那行清空很重要** —— 否则累积了一个换行符，`pending()` 返回 true（`" \n".trim()` 是空但原字符串非空），提示符就变成续行缩进的 `"     "` 了。注释里也点明了这个坑。

**④ `executeFile`：批量执行**

```java
public void executeFile(String path) throws IOException {
    String content = new String(Files.readAllBytes(Paths.get(path)), StandardCharsets.UTF_8);
    buf.setLength(0);
    buf.append(content).append('\n');
    drain();
    if (buf.toString().trim().length() > 0) {                    // 末尾没有分号的残留
        send(buf.toString().trim());
        buf.setLength(0);
    }
}
```

**整个文件一次性读进来**，交给 `drain()` 按分号切分。最后如果还有残留（文件末尾没有分号），也当作一条语句发送。

**这就是 `DEMO.sql` 的运行方式**（`E2ETest` 就是这么测的）。

**⑤ `send`：往返**

```java
private void send(String sql) throws IOException {
    Packager.write(out, new Package(false, sql.getBytes(StandardCharsets.UTF_8)));
    Package pkg = Packager.read(in);                              // ★ 同步等待响应
    if (pkg.error) {
        System.out.println(new String(pkg.payload, StandardCharsets.UTF_8));
    } else {
        Renderer.render(Decoder.decodeResult(pkg.payload));
    }
}
```

**这是一个同步的请求-响应**（阻塞等待）。整个客户端是单线程的顺序流程。

**错误时直接打印 payload**（因为 `encodeError` 存的就是 `Error.toString()` 的字节），成功时解码 + 渲染。

### `Renderer`：ASCII 表格渲染

文件：[`client/Renderer.java`](src/main/java/com/course/dbms/client/Renderer.java)，68 行。

```java
public static void render(Result r) {
    if (r.columns.isEmpty()) return;

    // ① 算每列的宽度 = max(列头长度, 所有该列单元格的长度)
    int n = r.columns.size();
    int[] w = new int[n];
    for (int i = 0; i < n; i++) w[i] = r.columns.get(i).length();
    for (Row row : r.rows) {
        for (int i = 0; i < n; i++) {
            if (i < row.size()) w[i] = Math.max(w[i], cell(row.get(i)).length());
        }
    }

    border(w); header(r, w); border(w);                    // 上边框、表头、分隔线
    for (Row row : r.rows) { ... }                          // 数据行
    border(w);                                             // 下边框
    System.out.println(r.rows.size() + " row(s)");
}
```

**两趟**：先算宽度（要看所有行），再打印。这样表格是对齐的。

**`cell` 的 null 处理**：

```java
/** NULL 渲染为空串（LEFT JOIN 的右列常为 NULL，避免出现字面 "null"）。 */
private static String cell(Object v) {
    return v == null ? "" : String.valueOf(v);
}
```

**为什么重要？** LEFT JOIN 的输出里右列常常是 null。如果渲染成字面 `"null"` 字符串，表格里会出现 `null` 这个词，跟数据里的字符串 `'null'` 混淆。**渲染成空串更清晰**。

输出样例：

```
+----+-------+
| id | name  |
+----+-------+
| 1  | alice |
| 2  | bob   |
| 3  | carol |
+----+-------+
3 row(s)
```

**`System.out.println(r.rows.size() + " row(s)")`** —— `E2ETest` 就靠断言 `out.contains("1 row(s)")` 来验证行数。

## 7.4 一次真实的往返（时序图）

```
客户端                                                服务端
  │                                                     │
  │  console.readLine() → "select id from users;"        │
  │  buf.append(line); drain()                          │
  │  topLevelSemicolon 找到 ';' → stmt = "select ..."   │
  │                                                     │
  │  ── Packager.write(out, Package(false, sqlBytes)) ──▶│  accept 时已起线程
  │     [4B len][1B 0][SQL bytes]  + flush              │  Packager.read(in)
  │                                                     │    len = in.readInt()
  │                                                     │    err = in.readByte()
  │                                                     │    readFully(payload)
  │                                                     │  new String(payload, UTF_8) → sql
  │                                                     │  Log.info("[client ...] select...")
  │                                                     │
  │                                                     │  session.execute(sql)
  │                                                     │    ├ parse → Stmt
  │                                                     │    ├ 不是 TxnStmt → runData
  │                                                     │    ├ locks.acquire(SYS_TABLES, S)
  │                                                     │    ├ locks.acquire(users, S)
  │                                                     │    ├ db.executeStmt(stmt)
  │                                                     │    │   ├ analyzer.analyze
  │                                                     │    │   ├ planner.build → Plan
  │                                                     │    │   └ executor.execute → Result
  │                                                     │    └ finally: locks.releaseTxn
  │                                                     │
  │                                                     │  Encoder.encodeResult(r)
  │  ◀── Packager.write(out, Package(false, resultBytes))│
  │      [4B len][1B 0][Result bytes] + flush           │
  │  Packager.read(in)                                  │
  │  Decoder.decodeResult(payload) → Result             │
  │  Renderer.render(r)                                 │
  │    打印 ASCII 表格 + "3 row(s)"                     │
  │                                                     │
```

## 7.5 第 7 章自问自答

**Q：为什么客户端不直接调用 `Database`？**
A：因为要展示"客户端/服务器端交互"这个课程要求。而且真实数据库都是 C/S 架构的（进程隔离、多客户端、权限控制）。

**Q：协议里为什么没有"请求 ID"？**
A：因为客户端是**同步阻塞**的——发一个等一个，不存在"响应乱序"的问题。如果要支持流水线（pipeline）或异步，就需要请求 ID 来匹配响应。

**Q：为什么错误信息直接传文本，不传错误码？**
A：`Encoder.encodeError` 存的是 `e.toString()`，格式是 `✗ [SE-0004] 列不存在: xxx`。**错误码在文本里**，客户端直接打印。这是"人看的协议"，不是"机器解析的协议"。

**如果要做得更好**：把错误码单独作为一个字段传，客户端可以据此做本地化/分类处理。

**Q：`Session` 是每个连接一个，那 `Database` 呢？**
A：**整个服务端共享一个**（构造 `Server` 时传入）。所以 `Database` 里的 `Catalog`、`PageManager`、`LockManager` 都是共享的。这就是为什么这些类里有那么多 `synchronized`。

**Q：多个客户端同时建表会怎样？**
A：`acquireLocks` 会对 `SYS_TABLES` 加 X 锁（`CreateStmt` 分支），所以是串行的。第二个建表请求要等第一个完成。

---

# 第 8 章 顶层门面：Database 与 Session

## 8.1 `Database`：门面模式

文件：[`db/Database.java`](src/main/java/com/course/dbms/db/Database.java)，79 行。

```java
public class Database {
    private final PageManager pm;
    private final StorageEngine se;
    private final Catalog catalog;
    private final Analyzer analyzer;
    private final PlanBuilder planner;
    private final Executor executor;
    private final LockManager lockManager;   // 服务器所有连接共享

    public Database(String dir) {
        this(new PageManager(new DiskManager(dir), new LruCache(64)));
    }

    /** 允许外部注入自定义缓存策略（如切换 FIFO）用于演示。 */
    public Database(PageManager pm) {
        this.pm = pm;
        this.se = new StorageEngine(pm);
        this.catalog = new Catalog(se);
        this.analyzer = new Analyzer(catalog);
        this.planner = new PlanBuilder(catalog, se);
        this.executor = new Executor();
        this.lockManager = new LockManager(Consts.LOCK_TIMEOUT_MS);
    }
}
```

> ★ **门面模式（Facade）**：把 7 个组件组装起来，对外只暴露 `execute(sql)`。
>
> **依赖注入的体现**：两个构造函数——
> - `Database(String dir)` —— 正常用，"给我一个目录路径"
> - `Database(PageManager pm)` —— **允许注入自定义的存储层**，用来演示"切换 LRU/FIFO"
>
> 演示时可以：
>
> ```java
> Database fifo = new Database(new PageManager(new DiskManager("data/db"), new FifoCache(64)));
> Database lru  = new Database(new PageManager(new DiskManager("data/db2"), new LruCache(64)));
> // 跑同样的负载，对比 cache().stats() 的命中率
> ```

**组装顺序（有依赖关系，不能乱）**：

```
DiskManager(dir)                                  ← 先有磁盘
    ↓
PageManager(disk, cache)                          ← 页管理器（构造时做崩溃恢复！）
    ↓
StorageEngine(pm)                                 ← 行↔页映射
    ↓
Catalog(se)                                       ← 系统目录（构造时 reload()）
    ↓
Analyzer(catalog)                                 ← 语义分析要查目录
PlanBuilder(catalog, se)                          ← 计划生成要查目录 + 读数据
    ↓
LockManager(LOCK_TIMEOUT_MS)                      ← 独立
```

**注意 `Catalog` 构造时就会 `reload()`** —— 所以"打开数据库"这个动作本身就包含了"加载全部元数据 + 重建全部索引"。

**三个执行入口**：

```java
/** 只做词法+语法解析，返回 AST。由 Session 先拿到语句类型以决定是否走事务/加锁。 */
public Stmt parse(String sql) { return new Parser(sql).parse(); }

/** 执行一条【可执行语句】。是否走事务影子路径由 Txn.current() 决定。 */
public Result executeStmt(Stmt stmt) {
    analyzer.analyze(stmt);
    Plan plan = planner.build(stmt);
    return executor.execute(plan);
}

/** 执行一条 SQL（无事务上下文；供测试裸调用与自动提交复用）。 */
public Result execute(String sql) { return executeStmt(parse(sql)); }
```

**`execute` 和 `executeStmt` 分开的价值**：

- `Session` 用 `parse` + `executeStmt` —— **解析一次，复用 AST**
- 测试用 `execute` —— 一步到位

**如果只有一个 `execute(String)`**：`Session` 就要么解析两次（浪费），要么自己拼 AST（更糟）。

**`executeStmt` 的注释说"是否走事务影子路径由 `Txn.current()` 决定"** —— 这个细节很重要：`Database` **自己不知道有没有事务**，是深层存储层查 `ThreadLocal` 判断的。**零侵入的体现**。

**访问器**：

```java
public Catalog catalog() { return catalog; }
public StorageEngine storage() { return se; }
public PageManager pages() { return pm; }
public Cache cache() { return pm.cache(); }
public LockManager locks() { return lockManager; }

public void close() { pm.close(); }
```

**`cache()` 委托给 `pm.cache()`** —— 方便演示时打印命中率（`db.cache().stats()`）。

## 8.2 `Session`：每个连接一张表

（`Session` 的核心逻辑已在 §3.6 讲过了，这里补充**加锁请求表**的完整设计。）

### 完整的 `lockRequest`

```java
// Session.java:128
private List<int[]> lockRequest(Stmt stmt) {
    List<int[]> req = new ArrayList<>();

    if (stmt instanceof SelectStmt) {
        req.add(new int[]{SYS_TABLES, 0});                       // sys_tables: S
        SelectStmt sel = (SelectStmt) stmt;
        if (sel.isRich()) {
            // 多表联查：对 FROM 里的每一张表都加共享读锁
            for (TableRef tr : sel.from) {
                Table t = db.catalog().getTable(tr.name);
                req.add(new int[]{t.tableId(), 0});
            }
        } else {
            Table t = db.catalog().getTable(sel.tableName);
            req.add(new int[]{t.tableId(), 0});
        }
    } else if (stmt instanceof InsertStmt) {
        Table t = db.catalog().getTable(((InsertStmt) stmt).tableName);
        req.add(new int[]{SYS_TABLES, 0});                       // sys_tables: S
        req.add(new int[]{t.tableId(), 1});                      // 目标表: X
    } else if (stmt instanceof CreateStmt) {
        req.add(new int[]{SYS_TABLES, 1});                       // sys_tables: X
        req.add(new int[]{SYS_COLUMNS, 1});                      // sys_columns: X
    } else if (stmt instanceof CreateIndexStmt) {
        CreateIndexStmt ci = (CreateIndexStmt) stmt;
        Table t = db.catalog().getTable(ci.tableName);
        req.add(new int[]{SYS_TABLES, 0});                       // sys_tables: S
        req.add(new int[]{SYS_INDEXES, 1});                      // sys_indexes: X
        req.add(new int[]{t.tableId(), 1});                      // 目标表: X
    } else if (stmt instanceof ShowStmt) {
        ShowStmt s = (ShowStmt) stmt;
        if (s.indexes) {
            req.add(new int[]{SYS_INDEXES, 0});                  // sys_indexes: S
            if (s.tableName != null) {
                Table t = db.catalog().getTable(s.tableName);
                req.add(new int[]{t.tableId(), 0});
            }
        } else {
            req.add(new int[]{SYS_TABLES, 0});                   // sys_tables: S
            if (!s.list) {
                Table t = db.catalog().getTable(s.tableName);
                req.add(new int[]{t.tableId(), 0});
            }
        }
    }

    // 按 tableId 升序：谁都在同一全局顺序上抢锁，谁也不会互相等成环
    req.sort((a, b) -> Integer.compare(a[0], b[0]));
    return req;
}
```

`modeInt`：**0 = SHARED(S)，1 = EXCLUSIVE(X)**。

### 加锁清单总表（★ 汇报重点）

| 语句 | 加锁 |
|---|---|
| `select ... from t` | `sys_tables` **S**、`t` **S** |
| `select ... from a join b` | `sys_tables` **S**、`a` **S**、`b` **S** |
| `insert into t ...` | `sys_tables` **S**、`t` **X** |
| `create table t ...` | `sys_tables` **X**、`sys_columns` **X** |
| `create index i on t(c)` | `sys_tables` **S**、`sys_indexes` **X**、`t` **X** |
| `show tables` | `sys_tables` **S** |
| `show table t` | `sys_tables` **S**、`t` **S** |
| `show indexes [on t]` | `sys_indexes` **S**（+ `t` **S** 如果有 `on t`） |

**几条设计规律**：

**① 所有语句默认对 `sys_tables` 加 S 锁**

注释里写明了原因：

> 统一对 `sys_tables` 加 S：串行化对目录页的读写，避免并发 DDL 破坏目录一致性。

**为什么？** 因为 `Catalog.getTable` 会读内存 map（`tables`），而 DDL 会 `tables.put()`。如果 DDL 正在改 map，同时有 SELECT 在读，可能读到中间状态。

**加了 S 锁后**：`create table`（要 X 锁）与 `select`（要 S 锁）互斥——**DDL 期间不会有查询在跑**。

**代价**：所有语句都要先抢 `sys_tables` 的锁，这是**全局的锁竞争点**。数据量大、并发高时这是瓶颈。**真实数据库用更细粒度的目录锁**（或者 MVCC 读快照）。

**② `insert` 只需要 `sys_tables` 的 S 锁，不需要 `sys_columns`**

因为 INSERT 要读 schema（通过 `Catalog.getTable` → `tables` map），但不改元数据。而 `sys_tables` 的 S 锁已经保证"不会同时有 DDL 在改 map"。

**③ `create index` 加的三把锁各有用处**

- `sys_tables` **S** —— 读表结构（要 `getTable`）
- `sys_indexes` **X** —— 写索引元数据
- `t` **X** —— **扫全表建树期间挡住并发写**（见 §6.5 的并发陷阱分析）

**④ 排序的那一行**

```java
req.sort((a, b) -> Integer.compare(a[0], b[0]));
```

**这是防死锁的全部实现**（配合 `acquire` 的超时兜底）。详见 §3.3。

**注意用的是 `Integer.compare` 而不是 `a[0] - b[0]`** —— 后者在极端值下会 int 溢出。**这是 Java 里写比较器的标准做法**。

### 为什么 `lockRequest` 在 `Session` 而不是别处？

因为它需要**语句类型**（`instanceof` 分派）和 **`Catalog`**（把表名转成 tableId）。`Session` 是"语句 + 数据库"交汇的地方。

**另一个候选位置**：给每个算子一个 `locks()` 方法。但那样锁的逻辑就分散到 13 个算子里了，**看不到全局**。集中在 `Session` 一处，**一眼能列出所有语句的加锁策略**（就是上面那张表）。

## 8.3 第 8 章自问自答

**Q：`Session` 为什么要 `parse` 而不直接 `Database.execute`？**
A：因为要先知道语句类型才能决定加什么锁。而 `execute` 内部会再解析一次——**解析两次浪费**。所以拆成 `parse` + `executeStmt`。

**Q：`Session` 里的事务状态存在哪？**
A：**ThreadLocal**（`Txn.CURRENT`），不在 `Session` 对象里。`Session` 只提供"编排"（调 `Txn.setCurrent` / `clearCurrent`）。

**为什么要这样？** 因为 `Session` 是"连接级"的，而事务是"线程级"的。在 thread-per-connection 模型下两者一一对应——但用 ThreadLocal 让深层代码（`PageManager`）能零侵入地拿到事务。

**Q：`Session` 加了 `sys_tables` 的 S 锁，那 `Catalog.reload()` 呢？**
A：`reload()` 在 `Catalog` 内部是 `synchronized` 的（对象锁），跟 `LockManager` 的表锁是**两套不同的锁**。

**为什么需要两套？**
- **表锁**：跨连接的业务语义隔离（"建表时不许查询"）
- **对象锁**：单进程内的数据结构保护（`tables` map 不能被并发改）

一个是**逻辑并发控制**，一个是**物理线程安全**。都要有。

---

# 第 9 章 崩溃恢复完整推演

前面各章零散讲过 WAL，这里做**四个场景的完整推演**，确保你真的想通了。

## 9.1 前置：提交协议的时序

```java
// PageManager.commitTxn
log.writeCommit(t.id(), t.pages());       // ① 写 BEGIN + 所有 PUT + COMMIT，然后 fsync
for (entry : t.pages()) {                 // ② 把每页写到数据文件
    disk.writePage(...);                  //    （可能中途崩溃）
    cache.put(...);
}
t.reset();                                // ③ 清工作集
```

**关键：①发生在②之前，且①的最后有 fsync。**

## 9.2 场景 A：提交后、落页前崩溃

```
① WAL: BEGIN txn5 / PUT(users#0, 后像) / PUT(users#1, 后像) / COMMIT txn5
   → fsync 完成 ✅
② disk.writePage(users, 0)  ✅
② disk.writePage(users, 1)  ❌ ← 断电！
```

**磁盘状态**：`users#0` 是新的、`users#1` 是旧的、WAL 有完整记录。

**重启恢复**：

```java
byte[] all = readAll();                       // 读到完整日志
// 第一遍：committed = {5}, puts = [(5, users, 0, img0), (5, users, 1, img1)]
// 第二遍：
for (PutRec p : puts) {
    if (committed.contains(p.txnId)) {        // 5 ∈ {5} ✅
        disk.writePage(p.tableId, p.pageNo, p.image);   // 重放页 0 和页 1
    }
}
disk.force();                                 // 刷盘
truncate();                                   // 清空 WAL
```

**结果**：两个页都变成新值 ✅ **提交被持久化**。

`CrashRecoveryTest.committedTxnSurvivesCrash` 验证。

> ★ **这就是"先写日志"的意义**：只要 COMMIT 记录落盘了，即使数据页半个都没写，也能从日志完全恢复。

## 9.3 场景 B：提交前崩溃（未提交）

```
① WAL: BEGIN txn6 / PUT(users#0, 后像)      ← 还没写 COMMIT
   ❌ 断电！
```

或者更简单的：事务还在 ACTIVE 状态（工作集在内存里），进程被 kill：

```
WAL: （空的，或者只有别的已提交事务的记录）
内存: 工作集里有一堆修改   ← 全丢了
```

**磁盘状态**：`users` 表文件完全没变（因为影子页从未落盘）。

**重启恢复**：

```java
// 情况 1（有 PUT 无 COMMIT）：
// committed = {}（没有 COMMIT 记录）
// puts = [(6, users, 0, img)]
for (PutRec p : puts) {
    if (committed.contains(p.txnId)) { ... }   // 6 ∉ {} → 跳过
}
```

**结果**：这个事务的所有修改被丢弃 ✅ **未提交被回滚**。

`CrashRecoveryTest.uncommittedTxnRolledBackOnCrash` 和 `LogTest.discardsUncommittedTxnOnRecover` 验证。

> ★ **这就是影子页的价值**：未提交的修改**从未碰过数据文件**，所以"回滚"不需要做任何事——它本来就没生效。

## 9.4 场景 C：写 WAL 的过程中崩溃（撕裂尾部）

```
WAL 文件:
[BEGIN txn7 记录完整]
[PUT txn7 记录：长度字段已写 = 4108，但页数据只写了 2000 字节]  ← 断电！
```

**恢复时**：

```java
while (in.available() >= 4) {
    int len = in.readInt();                        // len = 4108
    if (in.available() < len) break;               // ★ 实际只剩 2000 < 4108 → 停止解析
    ...
}
```

**结果**：半截记录被丢弃，前面的完整记录正常处理 ✅ **优雅停止**。

`LogTest.tornTailStopsGracefully` 验证。

> ★ **为什么长度前缀能解决这个问题？** 因为"声明的长度"和"实际剩余字节"一比较就知道记录完不完整。而且**必须用 `available()` 判断而不是 `catch (EOFException)`** —— 后者也能工作，但 `available()` 让意图更明确。（代码里两个都做了：`available` 判断 + `catch (EOFException)` 兜底。）

## 9.5 场景 D：恢复过程中再次崩溃

```
第一次恢复：
  重放页 0 ✅
  重放页 1 ✅
  ❌ 断电！（还没 disk.force()，还没 truncate()）
```

**WAL 还在**（没被 truncate）。

**第二次恢复**：

```java
// 日志完整 → committed = {5}，puts 还是那两条
// 重放页 0（内容与上次相同）、重放页 1（内容与上次相同）
```

**结果**：与第一次相同 ✅ **幂等**。

`LogTest.replayIsIdempotent` 验证。

> ★ **幂等性为什么是必须的？** 因为恢复本身不是原子的——它也可能被打断。如果重放不是幂等的（比如"把页 0 的某个计数 +1"），第二次恢复就会出错。
>
> **PUT 是整页覆盖写**（`disk.writePage(tableId, pageNo, image)`），写两次和写一次结果一样。**这就是幂等的来源**。

## 9.6 一个边界问题：新页的乱序写

考虑事务提交一个包含**新页**的工作集：

```
users 表原本 3 页（0,1,2）
事务插了很多行，新建了页 3、页 4
工作集 = {页0'、页1'、页2'、页3(新)、页4(新)}
```

提交时：

```java
for (entry : t.pages().entrySet()) { ... }    // HashMap 的遍历顺序不确定！
disk.writePage(users, 3, page3Bytes);          // 如果先写页 3
```

**问题**：文件当前只有 3 页（长度 12288）。写到 `pageNo * PAGE_SIZE = 12288` 处——**正好是文件末尾**。`RandomAccessFile.write` 会**自动扩展文件**到 `12288 + 4096 = 16384`（4 页）。

然后写页 4 → 扩展到 20480（5 页）。

**结果正确** ✅。

**但如果只写页 4 没写页 3 呢？**（比如崩溃在中间）—— 文件长度会是 5 页，页 3 是**全零字节**。

恢复时重放页 3（从 WAL）→ 内容正确 ✅。

**所以 WAL 又一次救了场**。

**如果事务提交成功但页 3 从没被写过？**（不可能——`for` 循环会遍历到所有页。）**除非崩溃**——那就是恢复要处理的场景。

> ⚠️ **已知的理论缺口**：如果**恢复之后**日志被 truncate 了，但页 3 因为某种原因是全零的…… 这在本项目的流程里不会发生（恢复会重放所有 POST 记录）。

**代码注释里也承认了这个设计**：

```java
// 超出 EOF 处写会自动扩展文件 → 新页即使乱序写也能正确落盘
```

## 9.7 第 9 章自问自答

**Q：为什么只做 redo 不做 undo？**
A：因为**未提交的事务从未写到数据文件**（影子页保证）。所以没有"需要撤销的已落盘修改"。undo log 是为"原地更新（in-place update）"架构准备的——那种架构下修改直接改在原页上，所以回滚需要逆操作。

**Q：如果 WAL 文件损坏了（比如全变乱码）？**
A：`recover` 会尽力解析，遇到不完整记录就停止。如果第一条记录就损坏，`committed` 为空，**所有 PUT 都被丢弃**——数据文件保持现状（可能是撕裂态）。这是**极端情况下的数据损失**，真实数据库会做校验和（checksum）+ 双份日志。本项目没有，是一个已知限制。

**Q：为什么恢复后要 `truncate()`？**
A：因为已提交的数据已经落到数据文件了。留着日志：
1. 下次恢复会重复重放（浪费）
2. 日志文件无限增长

**truncate 的时机很关键**：必须在 `disk.force()` **之后**——保证数据真的落盘了再清日志。顺序反了就丢数据。

**Q：`truncate()` 用 `raf()` 而不是新建文件？**
A：`raf()` 是惰性打开的（如果没写过日志，`raf` 是 null）。`recover` 里调用 `truncate()` 会通过 `raf()` 创建文件句柄，然后 `setLength(0)`。所以**恢复后日志文件是存在的但长度为 0**。

**Q：事务提交时为什么要同时更新 `cache`？**

```java
disk.writePage(pg.tableId(), pg.pageNo(), pg.data());
cache.put(k, pg);
```

因为**提交后别的会话要能立即看到新数据**。如果不更新缓存：
- 缓存里可能还留着**旧版本**的页（事务开始前 `getPage` 放进去的）
- 别的会话读到这页 → 拿到旧数据 → **违反持久性**

`cache.put(k, pg)` 用提交后的页对象**覆盖**缓存里的旧版本。

---

# 第 10 章 测试体系

## 10.1 怎么跑

```bash
cd dbms
JAVA_HOME="D:\Java\jdk1.8.0_331" mvn -o test
```

**为什么要指定 `JAVA_HOME`？** Maven 需要 JDK 8（项目 `pom.xml` 设置了 `source/target = 1.8`）。如果系统默认是更高版本的 JDK，编译时会出现"不支持 source 1.8"之类的错误。

**`-o`（offline）** —— 用本地仓库，不出网。因为没有第三方运行时依赖，本地仓库里只需要 JUnit。

**期望输出**：

```
Tests run: 138, Failures: 0, Errors: 0, Skipped: 0
BUILD SUCCESS
```

## 10.2 测试分布（19 个类，138 个用例）

| 测试类 | 用例数 | 测什么 |
|---|---|---|
| `db/DatabaseTest` | 5 | 端到端 SQL：建表/插入/查询/持久化/分号容忍/错误传播 |
| `db/TxnTest` | 7 | 事务：回滚、提交、read-your-writes、回滚撤销建表、嵌套拒绝、隔离阻塞 |
| `db/CrashRecoveryTest` | 2 | 崩溃恢复：已提交存活、未提交丢弃 |
| `db/JoinAggTest` | 7 | 多表联查与聚合 |
| `db/NullSemanticsTest` | 8 | **NULL 三值逻辑**（修复 `Compare` 缺陷时新增，见 §5.4） |
| `db/IndexTest` | 13 | **索引端到端** |
| `compiler/ParserTest` | 18 | 语法分析（含 3 个索引用例） |
| `compiler/AnalyzerTest` | 20 | 语义分析（含 5 个索引用例） |
| `compiler/PlanBuilderTest` | 19 | 执行计划（含 9 个索引用例） |
| `engine/StorageTest` | 3 | 行↔页映射、跨页、持久化 |
| `engine/CatalogTest` | 4 | 系统目录 |
| `engine/index/BPlusTreeTest` | 7 | **B+ 树单元测试** |
| `storage/PageTest` | 7 | 页结构、磁盘往返、LRU 淘汰、页管理、缓存统计 |
| `storage/LogTest` | 4 | WAL 恢复的四个场景 |
| `txn/LockManagerTest` | 6 | 锁：共存、冲突超时、阻塞释放、重入、升级、批量释放 |
| `protocol/ProtocolTest` | 3 | 编解码往返、帧往返 |
| `server/ServerClientTest` | 2 | 真实 Socket 往返、错误标志 |
| `server/ConcurrencyTest` | 2 | 并发：写事务阻塞读者、并发插入无丢失 |
| `client/E2ETest` | 1 | **通过 `ClientLauncher -f DEMO.sql` 的完整端到端** |

## 10.3 测试分层策略

```
        ┌─────────────────────────────────┐
   ①    │  E2ETest（1）                    │  真实 Socket + 客户端入口 + DEMO.sql
        │  端到端：进程级全链路             │
        └─────────────────────────────────┘
        ┌─────────────────────────────────┐
   ②    │  ServerClientTest / ConcurrencyTest │  真实 Socket + 多线程
        │  集成：服务端并发                 │
        └─────────────────────────────────┘
        ┌─────────────────────────────────┐
   ③    │  DatabaseTest / TxnTest / IndexTest  │  走 db.execute 完整管线
        │  集成：SQL 语义（无网络）          │      （解析→语义→计划→执行→存储）
        └─────────────────────────────────┘
        ┌─────────────────────────────────┐
   ④    │  ParserTest / AnalyzerTest / PlanBuilderTest │  单层（只测编译器一层）
        │  单元：编译器各阶段独立            │
        └─────────────────────────────────┘
        ┌─────────────────────────────────┐
   ⑤    │  BPlusTreeTest / PageTest / LogTest / LockManagerTest │
        │  单元：数据结构与存储原语          │
        └─────────────────────────────────┘
```

**每层的测试策略不同**：

| 层 | 隔离方式 | 断言风格 |
|---|---|---|
| ⑤ 数据结构 | 无依赖，直接 `new` | 精确断言（`assertEquals(100, tree.size())`） |
| ④ 编译器 | `Catalog` 用临时目录，直接构造 | `ok()` / `bad()` 辅助方法，断言错误码前缀 |
| ③ SQL 集成 | 临时目录 + `Database` | 断言结果行/列 |
| ② 网络 | 临时目录 + `Server(db, 0)` 随机端口 | 断言往返值 |
| ① 端到端 | 临时目录 + 真实客户端 + 拦截 stdout | 断言输出文本包含关键字 |

**临时目录约定**：每个测试类用自己的目录（`target/tmp-xxx`），`@Before` 里 wipe：

```java
private final String dir = "target/tmp-analyzer";

@Before public void setup() {
    wipe(dir);
    ...
}

private static void wipe(String d) {
    File f = new File(d);
    File[] fs = f.listFiles();
    if (fs != null) for (File x : fs) x.delete();
}
```

**为什么用 `target/` 下？** 因为 `mvn clean` 会清掉，不会污染源码目录。

**为什么每次 wipe？** 测试之间**互不影响**（否则第二次跑会因为"表已存在"失败）。这是 `@Before` 而不是 `@BeforeClass` 的原因。

## 10.4 重点测试解读

### `DatabaseTest.persistenceAcrossReopen`：重启持久化

```java
@Test public void persistenceAcrossReopen() {
    Database db1 = new Database(dir);
    db1.execute("create table t (a int32, b string)");
    db1.execute("insert into t values (1, 'x')");
    db1.close();

    Database db2 = new Database(dir);                 // 重新打开
    Result r = db2.execute("select a, b from t");
    assertEquals(1, r.rowCount());
    db2.close();
}
```

**这是"元数据即数据"设计的验证**——`Catalog` 构造时读 `sys_tables`/`sys_columns`，表就"回来了"。

### `IndexTest.indexSurvivesReopen`：索引重启存活

```java
// 建索引 → 插入 → close → reopen → 查索引还在不在、数据对不对
```

**验证的是完整链路**：`sys_indexes` 落盘 → 重启 → `reload()` → `rebuildIndexes()` → 扫基表重建树。

### `LockManagerTest.upgradeBlockedByOtherReader`

```java
@Test public void upgradeBlockedByOtherReader() throws Exception {
    // txn1 拿 S，txn2 拿 S，txn1 尝试升级 X → 应该失败（有人还在读）
}
```

**验证 `canGrant` 里的升级规则**（§3.3）——只有自己是唯一 S 持有者才能升级。

### `ConcurrencyTest.concurrentAutocommitInsertsNoLostUpdate`

```java
// N 个线程同时 insert，最后断言行数正确
```

**验证的是"自动提交的短命锁"是对的**——如果锁有 bug，会出现"两个线程读同一页、都以为有空位、都往同一个槽位写"的丢失更新。

### `IndexTest.filterStillAppliesAboveIndexScan`

```java
// where age > 22 and score > 90，索引只在 age 上
// 验证结果与"不用索引"完全一致（Filter 兜住了 score 条件）
```

**验证的是"Filter 必须保留在 IndexScan 之上"这条规则**（§6.6 规则⑧）。

## 10.5 `ok()` / `bad()` 辅助方法模式

`AnalyzerTest` 里的这个模式很值得学：

```java
private void ok(String sql) {
    Stmt s = new Parser(sql).parse();
    analyzer.analyze(s);                     // 不抛即通过
}

private Error bad(String sql) {
    try {
        Stmt s = new Parser(sql).parse();
        analyzer.analyze(s);
        fail("should throw: " + sql);
        return null;
    } catch (Error e) {
        return e;
    }
}
```

**用法**：

```java
@Test public void validSelectColumns() {
    ok("select * from users");
    ok("select id, name, age, score from users");
    ok("select name from users where age > 24 and score >= 90 order by score desc");
}

@Test public void createIndexOnUnknownColumn() {
    Error e = bad("create index idx_x on users(nosuch)");
    assertEquals("SE-0004", e.code());
}
```

**为什么这个模式好？**

1. **`ok()` 一行一个用例** —— 可以堆很多正例，代码非常紧凑
2. **`bad()` 返回 `Error`** —— 可以断言具体的错误码，而不只是"抛了异常"
3. **`fail()` 在 `bad()` 里** —— 如果没抛异常，测试失败并打印出问题的 SQL

**一个细节**：`bad()` 里的 `return null` 在 `fail()` 之后是**为了编译通过**（`fail()` 返回 `void`，编译器不知道它一定会抛，所以需要 return 语句）。

### `PlanBuilderTest` 的计划形状断言

```java
@Test public void indexReplacesSeqScanWhenAvailable() {
    // 建索引
    db.execute("create index idx_age on users(age)");
    Plan plan = planner.build(new Parser("select id from users where age = 30").parse());
    // 断言计划树的形状
}
```

**依赖 `Operator.toString()` 的递归打印** —— 这又一次体现了那个设计（§5.3）的价值。

## 10.6 第 10 章自问自答

**Q：138 个测试里最能说明问题的是哪几个？**

按重要性排：

1. **`IndexTest.indexGivesSameRowsAsSeqScan`** —— 差分测试，用未优化路径当基准验证优化正确性
2. **`CrashRecoveryTest`（2 个）** —— 崩溃恢复的正确性（这个功能最难验证，因为没有"正常路径"可对照）
3. **`NullSemanticsTest`（8 个）** —— NULL 语义。它特别的地方在于**反向验证过**：把修复临时撤掉，用例确实会红，证明它不是"写完就绿"的摆设（见 §5.4）
4. **`ConcurrencyTest.concurrentAutocommitInsertsNoLostUpdate`** —— 并发正确性（也不容易测）
5. **`E2ETest`** —— 全链路（真实 Socket + 真实客户端 + 真实 SQL 文件）
6. **`PlanBuilderTest.orConditionFallsBackToSeqScan`** —— 边界规则（最容易写错的地方）

**Q：测试覆盖率有多高？**

没有跑过覆盖率工具（会增加依赖）。但从结构上看：

- **核心数据结构**（`Page`、`BPlusTree`）—— 覆盖充分（各有 7 个专门用例）
- **存储层**（`PageManager`、`Log`、`DiskManager`）—— 覆盖主要路径 + 异常路径
- **编译器四阶段** —— 每阶段独立测试
- **事务/锁** —— 覆盖主要场景
- **未覆盖的**：`Renderer` 的对齐逻辑、`Shell` 的续行提示符、`Server.close()` 的优雅关闭

**Q：为什么没有 mock 框架？**

零依赖约束。而且这个项目里**依赖都是真实的**（真实的文件、真实的 Socket），不太需要 mock。测试用"临时目录"和"随机端口"来隔离。

**Q：测试跑得快吗？**

138 个用例大约几秒。慢的主要是 Socket 相关的（`ServerClientTest`、`ConcurrencyTest`、`E2ETest`）和涉及超时的（`LockManagerTest.exclusiveConflictTimesOut` 要等 5 秒超时——不过它可能用了更短的 timeout 参数）。

---

# 第 11 章 汇报：演示脚本与答辩要点

## 11.1 一页纸摘要（开场用）

> 这是一个用 **Java 8 从零实现**的关系型数据库，**零第三方运行时依赖**。
>
> 完整实现了课程要求的三大模块：**SQL 编译器**（词法→语法→语义→计划）、**存储系统**（页式存储 + LRU/FIFO 缓存 + 磁盘管理）、**数据库引擎**（13 个算子的执行引擎 + 系统目录）。
>
> 在此之上完成了五个高级功能：**客户端/服务器通信**（自定协议 + 多线程服务端）、**事务与并发控制**（表级 S/X 锁 + 影子页隔离）、**WAL 崩溃恢复**（redo-only 预写日志）、**多表联查与聚合**（嵌套循环 JOIN + 5 种聚合函数）、**B+ 树索引**（含规划器的物理计划选择）。
>
> **74 个源文件、约 5200 行主代码、138 个测试用例全部通过。**

## 11.2 演示脚本（10 分钟版）

### 第 1 步：启动（1 分钟）

```powershell
.\run.ps1 create data\db      # 终端 1：新建库并启动服务端
```

```powershell
.\run.ps1 client              # 终端 2：交互客户端
```

**展示点**：服务端日志里有 `[SERVER] listening on ...` 和 `[OPEN] database dir = ...`。

### 第 2 步：基础 SQL（1 分钟）

```sql
create table users (id int32, name string, age int32, score float64);
insert into users values (1, 'alice', 23, 95.5);
insert into users values (2, 'bob', 30, 88.0);
insert into users values (3, 'carol', 22, 91.5);
show tables;
show table users;                          -- \d users 也行
select * from users where age > 22 order by score desc;
```

**讲什么**：客户端打印的 ASCII 表格是 `Renderer` 渲染的，服务端返回的是二进制 `Result`。**点出这是客户端/服务器架构**。

### 第 3 步：多表联查与聚合（2 分钟）

```sql
create table orders (oid int32, uid int32, amount float64);
insert into orders values (10, 1, 100.0);
insert into orders values (11, 2, 50.0);
insert into orders values (12, 1, 75.0);

select u.name, o.amount from users u join orders o on u.id = o.uid order by o.amount;
select u.id, o.oid from users u left join orders o on u.id = o.uid order by u.id;
select u.name, count(*), avg(o.amount) from users u join orders o on u.id = o.uid group by u.name;
```

**讲什么**：
- 第一条：内连接。`alice` 有两条订单，所以 alice 出现两次
- 第二条：左外连接。**`carol` 没有订单，但仍然出现，右列是空**（`Renderer.cell` 把 null 渲染成空串）
- 第三条：**在 JOIN 的结果上聚合**。alice 的 `count=2`、`avg=(75+100)/2=87.5`

**如果被问"JOIN 怎么实现的"**：嵌套循环，左边每行跟右边所有行配对，`ON` 条件用 `CondEval.eval` 逐对判定。全物化。

### 第 4 步：B+ 树索引（3 分钟）★ 重点演示

**先看不走索引的计划**：

```sql
select id, name from users where age > 22 order by id;
```

**然后建索引**：

```sql
create index idx_age on users(age);
show indexes;
```

**输出**：

```
+---------+-------+--------+
| index   | table | column |
+---------+-------+--------+
| idx_age | users | age    |
+---------+-------+--------+
1 row(s)
```

**再跑同一条查询**：

```sql
select id, name from users where age > 22 order by id;
```

**然后插入一行新数据，验证索引被维护**：

```sql
insert into users values (4, 'dave', 30, 99.0);
select id, name from users where age = 30 order by id;
```

**应该返回 bob 和 dave 两行** —— 证明"建索引后插入的行也被索引覆盖了"。

**讲什么（这是最好的展示点）**：

1. **同一条 SQL，建索引前后结果完全一致** —— 索引是纯优化
2. **计划树的形状变了** —— 可以临时在客户端加一行打印，或者用 `PlanBuilderTest` 的断言展示：

   ```
   Project[0, 1](Filter(age > 22)(SeqScan(users)))      ← 建索引前
   Project[0, 1](Filter(age > 22)(IndexScan(users)))    ← 建索引后
   ```

3. **Filter 没有消失** —— 说明索引只做"缩小扫描范围"，正确性由 Filter 保证

**如果被问"索引为什么是内存的"**：答"索引是派生数据，可以从基表重建，所以只持久化元数据到 `sys_indexes` 表，树在启动和回滚时重建。这样索引代码完全不碰存储层，逻辑解耦。"

### 第 5 步：事务与回滚（1 分钟）

```sql
begin;
insert into users values (9, 'zoe', 25, 80.0);
select * from users;                -- ★ 能看到 zoe（read-your-writes）
rollback;
select * from users;                -- ★ zoe 没了
```

**讲什么**：
- **在事务内能看到自己插入的行**（read-your-writes）
- 回滚后数据完全消失，**且从未落盘**（影子页机制）

**高级演示**（如果时间够）：

```sql
begin;
create table tmp (a int32);
rollback;
show tables;                        -- tmp 不见了
```

**讲什么**：**DDL 也能回滚**。机制是 `session` 的 ROLLBACK 分支调 `catalog.reload()`，而 `reload()` 重读磁盘上的 `sys_tables`。**零专用回滚代码**。

### 第 6 步：崩溃恢复（1 分钟）

```sql
begin;
insert into users values (100, 'ghost', 99, 1.0);
```

**然后直接杀掉服务端进程**（Ctrl+C 或 `taskkill //F //IM java.exe`）。

**重启**：

```powershell
.\run.ps1 open data\db
```

```sql
select * from users where id = 100;      -- 0 行
```

**再演示已提交的**：

```sql
begin;
insert into users values (101, 'keeper', 99, 1.0);
commit;
```

**杀掉进程 → 重启 → 查询**：

```sql
select * from users where id = 101;      -- 1 行，keeper 还在
```

**讲什么**：提交时 `commitTxn` 先写 WAL 并 `fsync`，再写数据页。所以提交后崩溃，重启时 `Log.recover` 能从 WAL 重放。

**如果被问"为什么只做 redo"**："因为未提交的事务从未写到数据文件——影子页保证了这一点。所以没有需要撤销的已落盘修改。"

### 第 7 步：`DEMO.sql` 一键演示（可选，1 分钟）

```powershell
.\run.ps1 demo
```

**讲什么**：11 条语句覆盖了全部特性，从建表到索引。这是"一键验收"。

## 11.3 高频问题与答案

### 关于架构

**Q：为什么不直接读文件，要搞"页"这么复杂？**
A：两个理由。①**性能**：磁盘的最小 IO 单位是块（512B~4KB），按字节读会 99% 时间花在系统调用上。②**原子性**：写一页的过程中断电，页是撕裂的——WAL 就是针对这一点设计的。见 §2.1。

**Q：为什么算子都是全物化的（返回 `List<Row>`）？**
A：简单、可调试。教科书式的 Volcano 模型（`open/next/close`）能早停、省内存，但代码量翻倍。本项目数据量小，全物化完全够用。见 §0.4。

**Q：系统目录为什么要用表存？**
A：因为这样元数据和数据**走完全相同的存储/缓存/事务/WAL 路径**——零新增代码，还自动获得了"建表回滚"、"重启恢复"这些能力。见 §5.6。

### 关于事务

**Q：隔离级别是什么？**
A：可串行化。表级 X 锁持有到提交，写-读、写-写全互斥。见 §3.8。

**Q：为什么不用 undo log 而是影子页？**
A：影子页让回滚变成"丢掉一张 map"（一行代码），而且天然与 WAL 解耦。代价是事务内改页要深拷贝、大事务占内存。见 §2.5。

**Q：死锁怎么办？**
A：**预防 + 兜底**。预防：所有会话按 tableId 升序加锁（资源有序分配法，死锁不可能成环）。兜底：5 秒超时抛 `TX-0003`。见 §3.3。

**Q：`Session` 里 `ROLLBACK` 那三行的顺序为什么重要？**
A：`Txn.clearCurrent()` 必须在 `catalog.reload()` 之前。否则 `reload()` 读数据时会走**事务路径**（返回工作集里的未提交深拷贝），导致"回滚后元数据还在"。见 §3.6。

**Q：`ThreadLocal` 用事务有什么风险？**
A：忘记清理会在线程复用时串事务。本项目的防线是每个连接一个线程（用完就结束）+ COMMIT/ROLLBACK 时显式 `clearCurrent()`。见 §3.4。

### 关于存储

**Q：一页能存多少行？**
A：`(4096 - 16) / (行字节数 + 4)`。假设一行 30 字节，约 120 行。见 §2.2。

**Q：为什么用槽位化页？**
A：支持 O(1) 定位某条记录（槽位直接给偏移）、支持变长字符串、支持删除留空槽。见 §2.2。

**Q：LRU 怎么实现的？**
A：`new LinkedHashMap<>(16, 0.75f, true)` + 重写 `removeEldestEntry`。**两个 Java 细节就实现了 LRU**。见 §2.4。

**Q：LRU 和 FIFO 差在哪？**
A：LRU 按"访问顺序"排（`get` 会移动条目），FIFO 按"插入顺序"排（`get` 不动）。所以 FIFO 会淘汰"插入早但一直在用"的页。见 §2.4。

**Q：WAL 里为什么每条记录带长度前缀？**
A：为了容忍撕裂尾部——"声明的长度 > 实际剩余字节"就停止解析。见 §2.6。

**Q：`RandomAccessFile` 写到 EOF 之外会怎样？**
A：自动扩展文件（填充零字节）。这让事务提交时**乱序写新页**也能得到正确的文件长度。见 §2.3。

### 关于索引 ★ 重点准备

**Q：为什么用 B+ 树不用哈希表？**
A：哈希表只支持等值查询，不支持范围（`age > 22`）。B+ 树有序，且支持范围扫描 + 叶子链表右扫。

**Q：为什么要用 B+ 树不用 B 树？**
A：①数据只在叶子 → 内部节点能塞更多键 → 树更矮；②叶子有链表 → 范围查询不用回溯。

**Q：树为什么建在内存里？**
A：索引是**派生数据**，从基表能完整重建。所以只持久化元数据到 `sys_indexes`，树在启动/回滚时重建。这样索引代码完全不碰存储层，逻辑解耦。见 §6.3。

**Q：建索引要多久？**
A：O(n)，扫一遍全表。本项目几十行数据是毫秒级。

**Q：`where a > 1 or b > 2` 为什么不用索引？**
A：**会用索引的话结果就错了**。`a` 的索引只能覆盖 `a > 1` 的行，但 `b > 2` 而 `a <= 1` 的行也应该输出——它们会漏掉。见 §6.6 规则③。

**Q：`where a > 1 and b > 2` 呢？**
A：可以用 `a` 的索引。因为满足整个 WHERE 的行**必然满足** `a > 1`，所以用 `a` 的索引缩小范围不会漏行。剩下的 `b > 2` 交给上层的 Filter。见 §6.6。

**Q：走了索引，为什么 Filter 还在？**
A：**保险**。索引只负责"缩小扫描范围"，正确性由 Filter 判定。这样索引的边界算错了也不会出错（多扫的行被 Filter 滤掉）。**唯一的风险是索引漏行**——所以测试里最重的一组是"有索引和无索引的结果完全相同"（差分测试）。见 §6.6 规则⑧。

**Q：`<>` 为什么用不了索引？**
A：因为 `<>` 对应的集合是两个不相交的区间 `(-∞, v) ∪ (v, +∞)`，破坏了 B+ 树"一个连续区间"的优势。扫两遍再合并不如直接全表扫。

**Q：重复的键怎么办？**
A：叶子存**有序的 `(key, RID)` 条目**，允许重复。`search(key)` 返回所有相等键的 RID。因为 `age` 这种列本来就有大量重复值。见 §6.2。

**Q：索引能加速 JOIN 吗？**
A：**目前不能**。`buildSelectRich` 里 Join 两侧都是硬编码的 `SeqScan`。改造方向是"索引嵌套循环连接"——右表换成 `IndexScan`，用左行当前的连接列值作为查询键。这是明确的扩展方向。见 §6.9。

**Q：插入新行时索引怎么更新？**
A：`Insert` 算子里一个钩子：`indexes.onInsert(table, row, pageNo, slot)`。因为系统**没有 DELETE/UPDATE**，`insert` 是唯一写路径，所以**一个钩子就完备**了。见 §5.4。

**Q：回滚一个 `create index` 会怎样？**
A：`sys_indexes` 的新行在影子页工作集里，回滚时被丢弃；`reload()` → `rebuildIndexes()` 清空并重建全部索引（读磁盘上的 `sys_indexes`，没有新行）→ 索引消失。**跟回滚建表是同一套机制**。见 §6.5。

**Q：并发建索引会怎样？**
A：`Session.lockRequest` 给目标表加 **X 锁**。如果不加，会出现"建索引扫表期间有并发插入，那行漏进索引"的不一致。见 §6.5 的并发陷阱分析。

**Q：`MAX_KEYS = 4` 是不是太小了？**
A：是刻意的。小阶数让**插入 5 个键就分裂**、插 100 个键树高就到 3 层——便于演示和测试。真实数据库的一个节点存几百个键。见 §6.2。

**Q：`IndexScan` 输出有序，为什么不省掉 `Sort`？**
A：两个理由：①只是"按索引键有序"，不是"按 ORDER BY 的列有序"；②有/无索引两条路径的输出顺序必须一致，否则同一句 SQL 的结果顺序会随索引有无而变。见 §6.2。

### 可能会被追问的"坑"

**Q：你的 `Row` 为什么没有 RID？**
A：早期设计时 `Row` 只装值。做索引时发现拿不到位置信息，所以在 `StorageEngine` 里加了 `Located` 载体类和 `scanLocated` 方法。**如果重来，会让 `Row` 带一个可空 RID 字段**——但那样要改几十处 `Row.of(...)` 调用，风险大。见 §5.1。

**Q：`Page.records()` 为什么不给槽位号？**
A：它设计时只为"读全部记录"（`scan`）。做索引时需要"记录 + 位置"，所以 `scanLocated` 手工数槽位。**更好的设计是 `records()` 返回带 slot 的包装对象**。见 §5.2。

**Q：Lexer 为什么不支持注释？**
A：功能取舍，不是遗漏。加 `--` 和 `/* */` 要处理"注释不能出现在字符串里"等边界。**不支持的语法我们都明确报错**（`LX-0001`），不会静默吞掉。见 §4.3。

**Q：错误码 `SE-0007` 有冲突？**
A：是的。`StorageEngine.rowToBytes` 的 `catch (IOException)` 用它表示"序列化失败"，`Analyzer`/`Catalog` 用它表示"索引名已存在"。两个场景都很罕见且在不同模块，但严格的错误码表应该集中定义。**这是个已知的可改进点。**

**Q：`Server.handlers` 列表会无限增长？**
A：是的，`ConnectionHandler` 结束时没有从列表里移除。对短生命周期的演示无害，但连接多了会累积。**已知的小内存泄漏**。

## 11.4 如果时间不够（3 分钟极简版）

1. **一页纸摘要**（30 秒）
2. **`.\run.ps1 demo`** —— 一键跑 `DEMO.sql`，展示全部特性的输出（1 分钟）
3. **索引演示**：`create index` → 同一条 SQL 结果不变但计划从 `SeqScan` 变 `IndexScan`（1 分钟）
4. **事务演示**：`begin; insert; select（能看到）; rollback; select（没了）`（30 秒）

## 11.5 收尾金句（可选）

> "这个项目最大的特点是**每一层都能往下讲到底**。从一条 SQL 的字符，到 TCP 帧的字节，到页内的偏移，到磁盘上的文件——中间没有黑盒。"
>
> "而且**每一个设计决策都有明确的取舍**：全物化换代码简洁、影子页换回滚简单、内存索引换逻辑解耦、表级锁换实现简单。这些取舍在课程规模下都是对的，但如果数据量放大 1000 倍，我知道该改哪里。"

---

# 附录

## 附录 A：错误码总表

| 错误码 | 含义 | 抛出位置 |
|---|---|---|
| `ST-0001` | 通用存储错误（`Error.st`） | 多处 |
| `ST-0010` | 打开文件失败 | `DiskManager.raf` |
| `ST-0011` | 读文件长度失败 | `DiskManager.pageCount` |
| `ST-0012` | 读页失败 | `DiskManager.readPage` |
| `ST-0013` | 写页失败 | `DiskManager.writePage` |
| `ST-0014` | 截断失败 | `DiskManager.truncateLastPage` |
| `ST-0015` | WAL 追加失败 | `Log.writeCommit` |
| `ST-0016` | WAL 解析失败 | `Log.recover` |
| `ST-0017` | 读 WAL 失败 | `Log.readAll` |
| `ST-0018` | 截断 WAL 失败 | `Log.truncate` |
| `CS-0001` | 通用缓存错误（`Error.cs`） | 预留 |
| `TB-0001` | 表不存在 | `Catalog.getTable` |
| `CT-0001` | 通用目录错误（`Error.ct`） | 预留 |
| `CT-0002` | 表已存在 | `Catalog.createTable` |
| `LX-0001` | 词法错误（非法字符、字符串未闭合） | `Lexer` |
| `SY-0001` | 语法错误 | `Parser` |
| `SE-0000` | 未知语句类型 | `Analyzer.analyze` |
| `SE-0001` | 表至少需要一列 | `Analyzer.analyzeCreate` |
| `SE-0002` | 列名重复 / 未知类型 | `Analyzer.analyzeCreate` / `FieldType.fromName` |
| `SE-0003` | 插入值个数与列数不一致 | `Analyzer.analyzeInsert` |
| `SE-0004` | 列不存在 / 列名有歧义 / 表别名重复 | 多处 |
| `SE-0005` | 插入值类型与列不匹配 | `StorageEngine.cast` |
| `SE-0006` | 序列化失败 / 非聚合列不在 GROUP BY | `StorageEngine` / `Analyzer` / `PlanBuilder` |
| `SE-0007` | 索引名已存在 / 序列化失败（**冲突**） | `Analyzer` / `Catalog` / `StorageEngine` |
| `SE-0008` | 反序列化失败 | `StorageEngine.rowFromBytes` |
| `PL-0001` | 未知语句 | `PlanBuilder.build` |
| `PL-0002` | 事务语句不该进入计划 | `PlanBuilder.build` |
| `EX-0001` | 未知比较运算符 | `Compare.apply` |
| `EX-0002` | 未知聚合函数 | `Aggregate.accumulate/result` |
| `TX-0001` | 没有活动事务 | `Session.requireActive` |
| `TX-0002` | 已在事务中 | `Session.handleTx` |
| `TX-0003` | 等锁超时 / 被中断 | `LockManager.acquire` |
| `TX-0004` | 事务内不能释放页 | `PageManager.freeLastPage` |
| `TX-0005` | 未知事务操作 | `Session.handleTx` |

## 附录 B：类索引（一句话职责）

| 类 | 文件 | 一句话 |
|---|---|---|
| `Consts` | `common/Consts.java` | 全局常量（页大小 4096、端口 9999、锁超时 5000ms） |
| `Error` | `common/Error.java` | 带错误码的运行时异常 |
| `Log` | `common/Log.java` | 极简日志（4 个级别，输出 stdout） |
| `Page` | `storage/Page.java` | 槽位化页：页头 + 记录区 + 槽位目录 |
| `DiskManager` | `storage/DiskManager.java` | 一表一文件，页号 → 文件偏移 |
| `Cache` | `storage/Cache.java` | 页缓存接口 |
| `AbstractCache` | `storage/AbstractCache.java` | 缓存基类：命中率统计 |
| `LruCache` | `storage/LruCache.java` | `LinkedHashMap(accessOrder=true)` 实现 LRU |
| `FifoCache` | `storage/FifoCache.java` | `LinkedHashMap(accessOrder=false)` 实现 FIFO |
| `PageManager` | `storage/PageManager.java` | 页管理器：读/写/分配/释放（事务感知） |
| `Log`（WAL） | `storage/Log.java` | 预写日志：提交协议 + 崩溃恢复 |
| `Lock` | `txn/Lock.java` | 锁模式枚举（SHARED / EXCLUSIVE） |
| `LockManager` | `txn/LockManager.java` | 表级锁、等待队列、超时、写者饥饿规避 |
| `Txn` | `txn/Txn.java` | 事务 + ThreadLocal 影子页工作集 |
| `TokenType` | `compiler/token/TokenType.java` | 词法单元类型枚举 |
| `Token` | `compiler/token/Token.java` | 一个词法单元（type/text/value/pos） |
| `Lexer` | `compiler/Lexer.java` | 词法分析：文本 → Token 流 |
| `Parser` | `compiler/Parser.java` | 递归下降语法分析：Token 流 → AST |
| `Analyzer` | `compiler/Analyzer.java` | 语义分析：存在性/类型/列数校验（只读） |
| `PlanBuilder` | `compiler/PlanBuilder.java` | 计划生成：AST → 算子树（含索引选择） |
| `Plan` | `compiler/Plan.java` | 计划：算子树根 + 输出列名 |
| `Stmt` | `compiler/ast/Stmt.java` | 语句 AST 基类（marker） |
| `CreateStmt` | `compiler/ast/CreateStmt.java` | `create table` |
| `CreateIndexStmt` | `compiler/ast/CreateIndexStmt.java` | `create index` |
| `InsertStmt` | `compiler/ast/InsertStmt.java` | `insert` |
| `SelectStmt` | `compiler/ast/SelectStmt.java` | `select`（含 `isRich()` 双路径） |
| `ShowStmt` | `compiler/ast/ShowStmt.java` | `show tables/table/indexes` |
| `TxnStmt` | `compiler/ast/TxnStmt.java` | `begin/commit/rollback` |
| `TxOp` | `compiler/ast/TxOp.java` | 事务操作枚举 |
| `Cond` | `compiler/ast/Cond.java` | WHERE/ON 条件（CMP/AND/OR，含列 vs 列） |
| `ColRef` | `compiler/ast/ColRef.java` | 列引用（可带限定符） |
| `TableRef` | `compiler/ast/TableRef.java` | FROM 里的表引用（含别名） |
| `SelectItem` | `compiler/ast/SelectItem.java` | SELECT 列表项（列或聚合） |
| `FieldType` | `engine/table/FieldType.java` | 6 种字段类型 + 编解码 |
| `Column` | `engine/table/Column.java` | 一列定义（名字 + 类型） |
| `Schema` | `engine/table/Schema.java` | 有序列集合（流式 API） |
| `Row` | `engine/table/Row.java` | 一行值（无 RID） |
| `Table` | `engine/table/Table.java` | 表元信息（id/name/schema） |
| `CombinedSchema` | `engine/table/CombinedSchema.java` | 多表拼接的逻辑列集合（解决歧义） |
| `Catalog` | `engine/table/Catalog.java` | 系统目录：3 张系统表 + reload + 索引重建 |
| `StorageEngine` | `engine/storage/StorageEngine.java` | 行↔页映射（cast/rowToBytes/insert/scan/fetch/scanLocated） |
| `BPlusTree` | `engine/index/BPlusTree.java` | 内存 B+ 树（MAX_KEYS=4，含分裂与叶链表） |
| `Index` | `engine/index/Index.java` | 索引元数据 + 一棵 B+ 树 |
| `IndexManager` | `engine/index/IndexManager.java` | 索引注册表 + 建树 + onInsert 维护 |
| `Operator` | `engine/exec/op/Operator.java` | 算子抽象基类（child/execute/name/toString） |
| `SeqScan` | `engine/exec/op/SeqScan.java` | 全表扫描（叶子） |
| `IndexScan` | `engine/exec/op/IndexScan.java` | 索引扫描 + 回表（叶子） |
| `Filter` | `engine/exec/op/Filter.java` | 条件过滤（两个构造器：单表 / 多表） |
| `Project` | `engine/exec/op/Project.java` | 投影（按源列下标） |
| `Sort` | `engine/exec/op/Sort.java` | 排序（降序用 `-c` 取反） |
| `Join` | `engine/exec/op/Join.java` | 嵌套循环连接（含 LEFT OUTER） |
| `Aggregate` | `engine/exec/op/Aggregate.java` | 分组聚合（count/sum/avg/min/max） |
| `CreateTable` | `engine/exec/op/CreateTable.java` | 建表（叶子 DDL） |
| `CreateIndex` | `engine/exec/op/CreateIndex.java` | 建索引（叶子 DDL） |
| `Insert` | `engine/exec/op/Insert.java` | 插入 + 索引维护钩子 |
| `ShowTables` | `engine/exec/op/ShowTables.java` | 列出用户表 |
| `ShowTable` | `engine/exec/op/ShowTable.java` | 展示表结构 |
| `ShowIndexes` | `engine/exec/op/ShowIndexes.java` | 列出索引 |
| `Compare` | `engine/exec/op/Compare.java` | 值比较的唯一真相来源 |
| `CondEval` | `engine/exec/op/CondEval.java` | 条件求值（Filter 和 Join 共用） |
| `Executor` | `engine/exec/Executor.java` | 驱动程序（`plan.root.execute()`） |
| `Result` | `engine/Result.java` | 查询结果（columns + rows） |
| `Package` | `protocol/Package.java` | 一个消息（error 标志 + payload） |
| `Packager` | `protocol/Packager.java` | 帧读写（长度前缀） |
| `Encoder` | `protocol/Encoder.java` | Result → 字节 |
| `Decoder` | `protocol/Decoder.java` | 字节 → Result |
| `Server` | `server/Server.java` | TCP 服务端（每连接一线程） |
| `ConnectionHandler` | `server/ConnectionHandler.java` | 一个连接的请求循环 |
| `ServerLauncher` | `server/ServerLauncher.java` | 服务端 main（create/open + 端口） |
| `ClientLauncher` | `client/ClientLauncher.java` | 客户端 main（-e/-f/交互） |
| `Shell` | `client/Shell.java` | 交互 shell（分号缓冲 + 元命令） |
| `Renderer` | `client/Renderer.java` | ASCII 表格渲染 |
| `Database` | `db/Database.java` | 门面（组装 7 个组件） |
| `Session` | `db/Session.java` | 会话（事务编排 + 加锁） |

## 附录 C：Java 8 兼容注意事项

项目用 **JDK 8** 编译（`pom.xml` 里 `source/target = 1.8`）。如果用了更高版本的 API，编译会失败。

**禁用的 API**：

| 禁用 | 替代 |
|---|---|
| `List.of(...)` (Java 9+) | `Arrays.asList(...)` / `new ArrayList<>()` |
| `Map.of(...)` (Java 9+) | 手写 `put` 或用 `HashMap` |
| `Set.of(...)` (Java 9+) | `new HashSet<>(Arrays.asList(...))` |
| `var` (Java 10+) | 显式类型 |
| `Stream.toList()` (Java 16+) | `collect(Collectors.toList())` |
| `String.isBlank()` (Java 11+) | `s.trim().isEmpty()` |
| `String.strip()` (Java 11+) | `s.trim()` |
| `Optional.isEmpty()` (Java 11+) | `!opt.isPresent()` |
| 文本块 `"""` (Java 15+) | 字符串拼接 |
| `record` (Java 16+) | 普通类 |
| switch 表达式 `->` (Java 14+) | 传统 `switch` + `break` |

**可以用的 Java 8 特性**（项目里大量使用）：

| 特性 | 用在哪 |
|---|---|
| Lambda 表达式 | `Filter` 的 `Resolver`、`Sort` 的 `Comparator`、`Sort`/`Aggregate` 里的比较 |
| 方法引用 `::` | `PlanBuilder` 里的 `csF::resolve`、`DiskManager` 的 `this::close` |
| 接口默认方法 | `Cache` 接口（实际上没用默认方法，但可以用） |
| `java.util.stream` | 项目里**几乎没用**（选了传统循环，更直观） |
| `try-with-resources` | `ConnectionHandler`、`ClientLauncher`、`PageManager` |
| 多重 catch | `StorageEngine.cast` 的 `catch (ClassCastException \| NumberFormatException e)` |
| `@FunctionalInterface` 语义 | `CondEval.Resolver`（没加注解，但语义上是） |
| Diamond 运算符 `<>` | 到处都用 |
| `String.join` | 可用 |

**编译命令**：

```bash
JAVA_HOME="D:\Java\jdk1.8.0_331" mvn -o clean test
```

## 附录 D：已知限制与扩展方向

### 已知限制

| 限制 | 影响 | 原因 |
|---|---|---|
| **不支持 DELETE / UPDATE** | 只能插不能改 | 需要 B+ 树删除、页内空间回收、索引维护 |
| **不支持注释**（`--` / `/* */`） | SQL 文件不能写注释 | 词法器的功能取舍 |
| **SQL 无法表达 `NULL` 字面量** | 不能 `insert null` / `where x is null`；NULL 只由 `LEFT JOIN` 合成 | 无 `NULL` 关键字，`expectLiteral` 只收 NUMBER/STR_LIT/TRUE/FALSE。**好处**：表的列值永不为 NULL，索引键和比较逻辑都少一大类边界情况；语义与修复见 §5.4 |
| **不支持 `NOT` / `<>` 走索引** | 少数查询回退全表扫 | `NOT` 未实现；`<>` 是两段区间 |
| **不支持多列索引** | `create index i on t(a, b)` 报语法错 | 单列索引已够演示 |
| **不支持 OR 走索引** | 回退全表扫 | 需要索引合并（union） |
| **JOIN 不能走索引** | 大表 JOIN 慢 | 需要索引嵌套循环连接 |
| **全物化执行** | 无早停、内存占用高 | 代码简洁优先 |
| **影子页全量拷贝** | 大事务占内存 | 简化 undo log |
| **表级锁** | 并发度低 | 实现简单 |
| **索引树在内存** | 启动要重建、大表慢 | 逻辑解耦 |
| **`MAX_KEYS = 4`** | 树高增长快 | 便于演示 |
| **协议传字符串** | 丢失类型信息 | 客户端只负责展示 |
| **`Sever.handlers` 不清除** | 小的内存泄漏 | 演示规模无影响 |
| **`SE-0007` 错误码冲突** | 定位略模糊 | 错误码未集中定义 |

### 扩展方向（按性价比排序）

| 优先级 | 功能 | 工作量 | 怎么做 |
|---|---|---|---|
| ★★★ | **DELETE / UPDATE** | 中 | `Parser` 加语法 → `DeleteStmt`/`UpdateStmt` → `Analyzer` → `PlanBuilder` → `Delete` 算子（按 RID 标记空槽）+ `BPlusTree.delete` |
| ★★★ | **索引嵌套循环连接** | 小 | `buildSelectRich` 里右表的 `SeqScan` 换成 `IndexScan`；用左行当前的连接列值作查询键 |
| ★★☆ | **`where a = 1 or b = 2` 用索引合并** | 中 | 分别用两个索引取 RID 集合，求并集 |
| ★★☆ | **多列索引** | 中 | `BPlusTree` 的键从 `Object` 改成 `List<Object>`（复合键），`Cond` 里多个列合起来做键 |
| ★★☆ | **选择度估算** | 中 | 记录每个索引的 `size` 和表的行数，选选择性更高的索引；简单的统计信息 |
| ★☆☆ | **SQL 注释** | 小 | `Lexer.next()` 里加 `--` 和 `/* */` 分支（注意字符串内的不处理） |
| ★☆☆ | **`NOT` 运算符** | 小 | `TokenType.NOT` 已有；加 `Cond.Kind.NOT` + `Parser` 分支 + `CondEval` 分支 |
| ★☆☆ | **`LIMIT` 子句** | 小 | `Parser` 加语法 + `Limit` 算子（全物化下只能截断，不能早停） |
| ★☆☆ | **`select` 能查系统表** | 小 | `Catalog.listTables` 里加上 `sys_*`（或者加个开关） |
| ★☆☆ | **`\d+` 显示索引** | 小 | `Shell` 里加元命令 → 转成 `show indexes on t` |
| ★☆☆ | **CLOCK 缓存** | 小 | 实现第二个 `Cache`（用 `LinkedHashMap` 的 `accessOrder` 不够，要手写环形缓冲） |
| ★☆☆ | **Range 扫描走索引的顺序保证** | 小 | 已经按 key 有序了；可以在 `sortAndIndexCombine` 里优化（索引顺序与 ORDER BY 一致时省略 Sort） |

## 附录 E：快速命令参考

```bash
# 编译 + 跑全部测试
cd dbms
JAVA_HOME="D:\Java\jdk1.8.0_331" mvn -o test

# 只跑某个测试类
JAVA_HOME="D:\Java\jdk1.8.0_331" mvn -o test -Dtest=BPlusTreeTest
JAVA_HOME="D:\Java\jdk1.8.0_331" mvn -o test -Dtest=IndexTest

# 清理 + 编译
JAVA_HOME="D:\Java\jdk1.8.0_331" mvn -o clean test
```

```powershell
# 一键演示（自动起服务端 → 跑 DEMO.sql → 停服务端）
.\run.ps1 demo

# 新建库并启动服务端（前台）
.\run.ps1 create data\db

# 打开已有库启动服务端
.\run.ps1 open data\db

# 启动交互客户端
.\run.ps1 client

# 客户端单条 SQL
.\run.ps1 client -e "show tables"

# 停掉服务端（Git Bash）
taskkill //F //IM java.exe
```

---

**手册结束。**

> 如果读到这里，你已经比 95% 能把这个项目跑起来的人更懂它了。
> 剩下的就是：**打开源码，对着每一章再走一遍**。
> 每个 `★` 标记的地方都是值得你在汇报时主动讲出来的点。
