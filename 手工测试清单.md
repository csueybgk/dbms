# 客户端手工测试清单（逐条执行）

对着**真实客户端**一条一条敲、一条一条核对的手工验收脚本。

和 `DEMO.sql` 的区别：`DEMO.sql` 是 `-f` 批量跑（顺得很快，中间过程看不到）；这份是**交互式 REPL 逐条**执行，每条都能停下看输出、看错误码、看索引建前建后的结果对比，适合自己过一遍或者给老师演示。

> **期望输出的来源分两类，别混着看**：
>
> - **①–㊳（第 1–7 部分，共 39 步）**：把当前代码的真实引擎按顺序跑了一遍，输出**原样抄回来**的。可以逐字符对照。
> - **㊴–㊷（第 8–9 部分：重启 / 崩溃 / 并发，共 4 步）**：要重启进程、开多个客户端，没法脚本化，是按 `CrashRecoveryTest` 和 `ConcurrencyTest` 的断言写的。这四步只核**该不该看到某一行 / 该不该卡住**，不用对照格式。

---

## 第 0 部分：准备

### 0.1 开两个终端

**终端 A**（服务端，前台运行，别关）：

```powershell
cd C:\Users\gxds2\Desktop\实训\dbms
.\run.ps1 create data\demo
```

看到 `[SERVER] listening on 0.0.0.0/0.0.0.0:9999` 就算起来了。`create` 会**清空** `data\demo` 重建，保证从零开始。

**终端 B**（客户端，交互 REPL）：

```powershell
cd C:\Users\gxds2\Desktop\实训\dbms
.\run.ps1 client
```

看到 banner：

```
DBMS client -- type SQL, end with ';'.  \dt  \d <table>  \q
dbms>
```

### 0.2 三条必须知道的规矩

| 规矩 | 说明 |
|---|---|
| **必须写分号** | REPL 累积到 `;` 才发送。少打分号会看到缩进的续行提示符 `     `（5 个空格），不是 `dbms>` |
| **不能写注释** | 词法器不支持 `--` 和 `/* */`，写了直接语法错 |
| **NULL 显示成空白** | 不是 `null`、不是 `NULL`，是**空单元格**。这是渲染层的刻意设计 |

另：`\q` 退出、`\dt` 等价 `show tables`、`\d users` 等价 `show table users`（这三个是客户端本地识别的，服务端没有这些语法）。

### 0.3 查不出来怎么办

每条输出结尾都有 `N row(s)`，**先对行数，再对内容**。行数错 → 数据或过滤条件问题；行数对但内容错 → 投影/排序问题。

`show tables` 的**行序不保证**（元数据存在 `ConcurrentHashMap` 里，按哈希走，既不是建表序也不是字典序）。只核行数和表名，别核顺序。

---

## 第 1 部分：建表 · 插入 · 基础查询

**① 建表**

```sql
create table users (id int32, name string, age int32, score float64);
```

| result |
|---|
| table 'users' created |

`1 row(s)` —— DDL 也返回一张单行结果表，不是静默的。

**② 插入三行**（三条分别敲）

```sql
insert into users values (1, 'alice', 23, 95.5);
insert into users values (2, 'bob', 30, 88.0);
insert into users values (3, 'carol', 22, 91.5);
```

每条都是：

| affected |
|---|
| 1 |

**③ 全表查**

```sql
select * from users;
```

| id | name | age | score |
|---|---|---|---|
| 1 | alice | 23 | 95.5 |
| 2 | bob | 30 | 88.0 |
| 3 | carol | 22 | 91.5 |

`3 row(s)`，行序 = 插入序（页内槽位序）。

**④ 过滤**

```sql
select id, name from users where age > 24;
```

| id | name |
|---|---|
| 2 | bob |

`1 row(s)`

**⑤ 排序（降序）**

```sql
select id, name, score from users order by score desc;
```

| id | name | score |
|---|---|---|
| 1 | alice | 95.5 |
| 3 | carol | 91.5 |
| 2 | bob | 88.0 |

`3 row(s)` —— 顺手把 `desc` 去掉再敲一次，应变成 bob / carol / alice，确认排序真的在动。

**⑥ 投影 + 过滤 + 排序三件套**

```sql
select id from users where age > 22 order by id;
```

| id |
|---|
| 1 |
| 2 |

`2 row(s)` —— 只出了 `id` 一列，说明 Project 生效。

**⑦ 元命令**

```sql
\dt
```

| table | columns |
|---|---|
| users | 4 |

`1 row(s)`。等价于 `show tables;`。

```sql
\d users
```

| column | type |
|---|---|
| id | int32 |
| name | string |
| age | int32 |
| score | float64 |

`4 row(s)`。等价于 `show table users;`。

---

## 第 2 部分：错误拦截（四类错误码）

按编译管线四阶段顺序各打一枪。**每条都应该只回一行 `✗ [码] 说明`，不带表格**。

**⑧ 语法错**（Parser 拦截）

```sql
select id from users where;
```

```
✗ [SY-0001] expected IDENTIFIER but got  at 26
```

**⑨ 表不存在**（Analyzer 拦截）

```sql
select * from ghost;
```

```
✗ [TB-0001] table not found: ghost
```

**⑩ 列不存在**（Analyzer 拦截）

```sql
select nosuch from users;
```

```
✗ [SE-0004] 列不存在: nosuch（表 users）
```

**⑪ 重复建表**（DDL 拦截）

```sql
create table users (id int32);
```

```
✗ [CT-0002] table already exists: users
```

**⑫ ORDER BY 未知列**（Analyzer 拦截）

```sql
select id from users order by nosuch;
```

```
✗ [SE-0004] 列不存在: nosuch（表 users）
```

> 这五条的价值：证明**错误在编译期被拦下**，不是执行到一半崩的。而且错误码能反推出是哪一阶段拦的（`SY-` 语法 / `TB-` `SE-` 语义 / `CT-` DDL）。

---

## 第 3 部分：多表联查（JOIN）

**⑬ 建订单表**

```sql
create table orders (oid int32, uid int32, amount float64);
```

**⑭ 插两行**（alice 和 bob 各一单，**carol 故意没有订单**）

```sql
insert into orders values (10, 1, 100.0);
insert into orders values (11, 2, 50.0);
```

**⑮ 内连接**

```sql
select u.name, o.amount from users u join orders o on u.id = o.uid order by o.amount;
```

| name | amount |
|---|---|
| bob | 50.0 |
| alice | 100.0 |

`2 row(s)` —— **carol 不出现**，因为她没有订单（内连接只保留匹配上的）。

**⑯ 左连接**

```sql
select u.id, u.name, o.amount from users u left join orders o on u.id = o.uid order by u.id;
```

| id | name | amount |
|---|---|---|
| 1 | alice | 100.0 |
| 2 | bob | 50.0 |
| 3 | carol | |

`3 row(s)` —— carol 那行 **amount 是空单元格**，这就是 NULL。左连接保留左表所有行，右表列补 NULL。

> 记住 `carol / amount = NULL` 这个状态，下一部分全靠它。

---

## 第 4 部分：NULL 三值逻辑 ★

**这一部分是本次修的一个真 bug 的回归验收**（`Compare.apply` 原先让 NULL 表现得"比任何数都大"）。数据集：alice=100.0、bob=50.0、carol=**NULL**。

**⑰ NULL 不满足「大于」**

```sql
select u.id from users u left join orders o on u.id = o.uid where o.amount > 60 order by u.id;
```

| id |
|---|
| 1 |

`1 row(s)` —— **只有 alice**。

> **这条是重点**：修之前 carol（id=3）会**错误地出现在结果里**，因为 NULL 走了兜底的字符串比较，`"null"` 的首字符 `'n'` 的 ASCII 是 110，比所有数字字符（48–57）都大，于是"NULL > 60"被判成了真。现在 NULL 走三值逻辑，判为 UNKNOWN，不保留。

**⑱ NULL 也不满足「不等于」**

```sql
select u.id from users u left join orders o on u.id = o.uid where o.amount <> 100 order by u.id;
```

| id |
|---|
| 2 |

`1 row(s)` —— **只有 bob**。carol 的 NULL `<> 100` 同样是 UNKNOWN。很多人以为 `<>` 能"顺手把 NULL 捞出来"，不能。

**⑲ 自比也捞不出 NULL**

```sql
select u.id from users u left join orders o on u.id = o.uid where o.amount = o.amount order by u.id;
```

| id |
|---|
| 1 |
| 2 |

`2 row(s)` —— `x = x` 在 x 为 NULL 时**不是真**。这是 SQL 三值逻辑最经典的反直觉点，也是教科书上最爱考的。

**⑳ 排序：NULL 垫底**

```sql
select u.id, o.amount from users u left join orders o on u.id = o.uid order by o.amount;
```

| id | amount |
|---|---|
| 2 | 50.0 |
| 1 | 100.0 |
| 3 | |

`3 row(s)` —— carol 在**最后**（NULLS LAST，与 PostgreSQL 一致）。

**㉑ 排序：降序时 NULL 翻到最前**

```sql
select u.id, o.amount from users u left join orders o on u.id = o.uid order by o.amount desc;
```

| id | amount |
|---|---|
| 3 | |
| 1 | 100.0 |
| 2 | 50.0 |

`3 row(s)` —— carol 到了**最前**。降序就是取反比较结果，NULLS LAST 取反自然变成 NULLS FIRST，**和 PostgreSQL 的 `ORDER BY x DESC` 行为一致**。

**㉒ ≤ 和 > 恰好划分非 NULL 行**

```sql
select u.id from users u left join orders o on u.id = o.uid where o.amount >= 50 order by u.id;
```

应得 `{1,2}`（alice 100.0、bob 50.0），再敲：

```sql
select u.id from users u left join orders o on u.id = o.uid where o.amount < 50 order by u.id;
```

应得 **`0 row(s)`**（空表）。

> **两条合起来看**：`>= 50` 得 `{1,2}`、`< 50` 得 `{}`，并起来正好是 `{1,2}` = 全部非 NULL 行，**NULL 行两边都不出现，也不留缝隙**。这就是三值逻辑该有的样子。

---

## 第 5 部分：索引

演示的核心是：**建索引前后，同一条查询的行必须逐行一致**，变的只是底层算子。

**㉓ 无索引时先各查一次（记下结果）**

```sql
select id, name from users where age = 30;
```

| id | name |
|---|---|
| 2 | bob |

`1 row(s)`

```sql
select id, name from users where age > 22 order by age;
```

| id | name |
|---|---|
| 1 | alice |
| 2 | bob |

`2 row(s)`

**㉔ 建索引**

```sql
create index idx_age on users(age);
```

| result |
|---|
| index 'idx_age' created |

**㉕ 看索引**

```sql
show indexes;
```

| index | table | column |
|---|---|---|
| idx_age | users | age |

`1 row(s)`。`show indexes on users;` 输出相同（带 `on <表>` 只是限定范围）。

**㉖ 重敲 ㉓ 的两条查询**

```sql
select id, name from users where age = 30;
select id, name from users where age > 22 order by age;
```

**必须与 ㉓ 逐行完全一致** —— bob；alice+bob。这是索引正确性的第一道关（差分验证：拿没走索引时的结果当基准）。

> **注意**：客户端输出里**看不到**走的是 `SeqScan` 还是 `IndexScan`——计划树没有回传给客户端。要看计划形状得跑 `PlanBuilderTest`（下面 §7 有）。客户端这边能观察到的是**结果一致**。

**㉗ 索引会跟着新插入维护**

```sql
insert into users values (4, 'dave', 30, 99.0);
select id, name from users where age = 30 order by id;
```

| id | name |
|---|---|
| 2 | bob |
| 4 | dave |

`2 row(s)` —— **dave 被索引查到了**，说明 `Insert` 算子的索引维护钩子生效，不是"建完索引就定格了"。

**㉘ 索引只对能被区间表达的条件生效**

```sql
select id from users where age > 22 and age < 24;
```

| id |
|---|
| 1 |

`1 row(s)`（alice 23）—— `AND` 的合取项可以选索引。

```sql
select id from users where age > 29 or age < 23 order by id;
```

| id |
|---|
| 2 |
| 3 |

`2 row(s)`（bob 30、carol 22）—— **`OR` 不会被拆成两段索引扫描**，规划器直接回退全表扫。结果是对的，只是没走索引（这也是附录里的已知限制 + 扩展方向）。

```sql
select id, name from users where age <> 30 order by id;
```

| id | name |
|---|---|
| 1 | alice |
| 3 | carol |

`2 row(s)` —— `<>` 是两段区间，规划器不做拼接，同样回退全表扫。

**㉙ 重复索引名被拒**

```sql
create index idx_age on users(age);
```

```
✗ [SE-0007] 索引名已存在: idx_age
```

---

## 第 6 部分：聚合与分组

**㉚ 给 alice 加第二单**（这样 alice 有 2 单、bob 1 单、carol 和 dave 0 单，四种情况齐了）

```sql
insert into orders values (13, 1, 20.0);
```

**㉛ 内连接上分组**

```sql
select u.name, count(*), sum(o.amount), avg(o.amount)
from users u join orders o on u.id = o.uid group by u.name order by u.name;
```

| name | count | sum | avg |
|---|---|---|---|
| alice | 2 | 120.0 | 60.0 |
| bob | 1 | 50.0 | 50.0 |

`2 row(s)` —— carol/dave 没有订单，内连接后不存在，所以**分组里也没有他们**。

**㉜ 左连接上分组：NULL 进聚合** ★

```sql
select u.id, count(o.amount), sum(o.amount), min(o.amount), max(o.amount)
from users u left join orders o on u.id = o.uid group by u.id order by u.id;
```

| id | count | sum | min | max |
|---|---|---|---|---|
| 1 | 2 | 120.0 | 20.0 | 100.0 |
| 2 | 1 | 50.0 | 50.0 | 50.0 |
| 3 | 0 | | | |
| 4 | 0 | | | |

`4 row(s)` —— 四个用户都在。carol/dave 的 `count` 是 **0**（不是 1！`count(o.amount)` 跳过 NULL），`sum/min/max` 全是空。

**㉜′ 直接对照 `count(*)` 和 `count(列)`**（把上一条的两种 count 并排放）

```sql
select u.id, count(*), count(o.amount)
from users u left join orders o on u.id = o.uid group by u.id order by u.id;
```

| id | count | count |
|---|---|---|
| 1 | 2 | 2 |
| 2 | 1 | 1 |
| 3 | **1** | **0** |
| 4 | **1** | **0** |

`4 row(s)` —— carol/dave 那一行是最值得看的：

- `count(*)` 得 **1**：数的是**行数**，左连接给他们各造了一行，所以是 1
- `count(o.amount)` 得 **0**：数的是**非 NULL 的列值**，他们那行的 `amount` 是 NULL，跳过

> 两个列头都叫 `count`（没写别名，按表达式给名），并排看容易串。这也说明了"聚合函数怎么对待 NULL"：`count(*)` 不看值，其余聚合都跳过 NULL。

**㉝ 无分组的聚合**

```sql
select count(*), avg(score) from users;
```

| count | avg |
|---|---|
| 4 | 93.5 |

`1 row(s)` —— (95.5+88.0+91.5+99.0)/4 = 93.5。

```sql
select max(age), min(age) from users;
```

| max | min |
|---|---|
| 30 | 22 |

`1 row(s)` —— 这两条走 `Compare.compare`（不是 `apply`），NULL 排序规则那条路径，顺带被覆盖到了。

---

## 第 7 部分：事务

**㉞ 回滚**

```sql
begin;
```

| result |
|---|
| transaction started |

```sql
insert into users values (5, 'eve', 40, 70.0);
select id, name from users where id = 5 order by id;
```

| id | name |
|---|---|
| 5 | eve |

`1 row(s)` —— **读己之写**：事务内能看到自己还没提交的改动（影子页）。

```sql
rollback;
```

| result |
|---|
| transaction rolled back |

```sql
select id, name from users where id = 5 order by id;
```

```
+----+------+
| id | name |
+----+------+
+----+------+
0 row(s)
```

**`0 row(s)`，表里只剩表头** —— eve 无影无踪。回滚是丢弃影子页工作集，从不写盘。

**㉟ 提交 + 无效提交**

```sql
commit;
```

```
✗ [TX-0001] 没有活动事务
```

（刚回滚完，确实没有活动事务 —— 错误信息准确。）

**㊱ 嵌套 BEGIN 被拒**

```sql
begin;
begin;
```

第二条：

```
✗ [TX-0002] 已在事务中，请先 COMMIT 或 ROLLBACK
```

**㊲ DDL 也能回滚**

```sql
create table tmp (id int32);
show tables;
```

此时 `show tables` 是 **3 行**：`tmp` / `orders` / `users` —— 事务内**建的表自己看得见**。

```sql
rollback;
show table tmp;
```

```
✗ [TB-0001] table not found: tmp
```

```sql
show tables;
```

**2 行**：`orders` / `users` —— **tmp 消失了**。回滚会 `catalog.reload()`，未提交的建表被撤销。

**㊳ 提交后持久**

```sql
begin;
insert into users values (6, 'frank', 33, 80.0);
commit;
```

| result |
|---|
| transaction committed |

```sql
select id, name from users where id = 6 order by id;
```

| id | name |
|---|---|
| 6 | frank |

`1 row(s)`

---

## 第 8 部分：持久化与崩溃恢复

这两步要**动服务端进程**，客户端先保持连着。

**㊴ 正常重启：数据还在**

1. 终端 A 按 `Ctrl+C` 停掉服务端
2. 终端 B 的客户端会断开（再敲任何语句会报连接错）
3. 终端 A 重新 `.\run.ps1 open data\demo`（注意是 `open`，不是 `create`）
4. 终端 B 重新 `.\run.ps1 client`
5. 再敲：

```sql
select id, name from users order by id;
```

应看到 **5 行**：alice(1) / bob(2) / carol(3) / dave(4) / frank(6) —— **eve(5) 不在**（上面回滚掉了），**frank(6) 在**（提交过）。

```sql
show indexes;
```

| index | table | column |
|---|---|---|
| idx_age | users | age |

`1 row(s)` —— **索引元数据也持久了**。索引内容是从基表重建的，不是存在磁盘上的树。

```sql
select id, name from users where age = 30 order by id;
```

| id | name |
|---|---|
| 2 | bob |
| 4 | dave |

`2 row(s)` —— 重启后索引重建成功，查询照样走索引。

**㊵ 崩溃恢复：提交的活下来，未提交的没了**

1. 客户端：

```sql
begin;
insert into users values (7, 'grace', 27, 77.0);
select id, name from users where id = 7 order by id;
```

能看到 grace（`1 row(s)`）—— 此时**不要提交**。

2. 终端 A **直接杀掉服务端进程**（`Ctrl+C` 或直接关掉终端窗口，不要走任何"正常关闭"）。数据库没有优雅关闭钩子，这就是一次真崩溃。

3. 终端 A `.\run.ps1 open data\demo`，终端 B 重连，然后：

```sql
select id, name from users where id = 7 order by id;
```

**`0 row(s)`** —— grace 没了。未提交的改动只在内存的影子页里，从没落盘，WAL 里也没有记录，恢复时无从重放。

```sql
select id, name from users order by id;
```

**5 行**，和 ㊴ 结束时一样 —— 之前**提交过**的 frank 依然在。

> **这两条合起来就是 ACID 里的 A 和 D**：原子性（未提交的整个消失）+ 持久性（提交过的扛得住崩溃）。
>
> 想再确认一下 WAL 真的在干活：`dir data\demo` 应该能看到 `wal.log`。提交时先把页后像写进 WAL 并 fsync，再改数据页；恢复时只重放**带 COMMIT 标记**的事务。

---

## 第 9 部分：并发与锁

需要**三个终端**：A 服务端、B 客户端 1、C 客户端 2。

**㊶ 写事务阻塞读**

1. 确认数据基线（终端 B）：

```sql
select count(*) from users;
```

`5`（alice/bob/carol/dave/frank）

2. **终端 B** 开一个事务写一行，**不要提交**：

```sql
begin;
insert into users values (8, 'heidi', 35, 66.0);
```

3. **立刻切到终端 C**，敲：

```sql
select * from users;
```

**光标会卡住，不返回。** 这就是 B 持着 `users` 的写锁（X 锁）没放，C 的读要申请 S 锁，被挡住。可以去终端 A 的服务端日志看到请求已经打进来了，只是没回。

4. 回**终端 B**：

```sql
commit;
```

终端 C 的 `select` **立刻返回**，而且是 **6 行** —— 包含 heidi。

> **两个看点**：① 锁是**表级**的，S/X 互斥，写没提交读就得等；② 读到的不是快照，是**提交后的最新状态**（`6 row(s)` 而不是 `5`），说明隔离靠的是"写锁持有到提交"这种简单粗暴但正确的方式。

**㊷ 两个连接并发插入不丢更新**

终端 B 和终端 C 同时各敲一批（各 20 条足够，手敲太累的话这步可以跳过 —— `ConcurrencyTest` 用 2×50 条自动测过了）：

```sql
insert into users values (100, 'n1', 1, 1.0);
```

两边都敲完后在任一终端：

```sql
select count(*) from users;
```

行数应等于 `6 + 两边成功插入的总数`，**不多不少**。自动提交模式下每条 insert 都是"短命持锁 + 立即释放"，写-写互斥保证不会互相覆盖。

---

## 第 10 部分：收尾

```sql
\q
```

```
bye
```

终端 A `Ctrl+C` 停服务端。清理演示库：`Remove-Item data\demo -Recurse -Force`。

---

## 附录 A：这份清单和自动测试的对应关系

手工跑完，对照自动测试（`.\run.ps1 test`，138 个用例）：

| 本清单 | 对应自动测试 | 备注 |
|---|---|---|
| 第 1–2 部分 | `DatabaseTest`、`ParserTest`、`AnalyzerTest` | 端到端 + 编译期四阶段 |
| 第 3 部分 | `db/JoinAggTest` | INNER/LEFT JOIN |
| **第 4 部分** | **`db/NullSemanticsTest`（8 个）** | **本次修的 NULL 缺陷的回归防线** |
| 第 5 部分 | `db/IndexTest`（13 个）、`engine/index/BPlusTreeTest`（7 个） | 含"建索引前后逐行一致"的差分对照 |
| 第 6 部分 | `db/JoinAggTest` 的聚合用例 | |
| 第 7 部分 | `db/TxnTest`（7 个） | |
| 第 8 部分 | `db/CrashRecoveryTest`（2 个） | 手工版更能看到"崩溃"这个动作 |
| 第 9 部分 | `server/ConcurrencyTest`（2 个）、`txn/LockManagerTest`（6 个） | |

**一件事客户端看不到**：走 `SeqScan` 还是 `IndexScan`。计划树不回传客户端。想验证物理计划选择，跑：

```powershell
cd dbms
$env:JAVA_HOME="D:\Java\jdk1.8.0_331"; mvn -o test "-Dtest=PlanBuilderTest"
```

里面有"建索引后 `where age = 30` → `Project→Filter→IndexScan`"、"没索引 → `SeqScan`"、"`OR`/`<>` → `SeqScan`"的断言。

## 附录 B：卡住了怎么排查

| 症状 | 原因 |
|---|---|
| 提示符是 5 个空格不是 `dbms>` | 上一条语句少写分号，还在续行。补个 `;` 或 `Ctrl+C` |
| `✗ [SY-0001] expected IDENTIFIER but got  at N` | 语句缺东西。`N` 是出错位置（字符下标，从 0 数） |
| `✗ [SE-0004]` | 列名写错，或 `order by` / `group by` 里用了投影之外的列名 |
| 第 ① 步就报 `CT-0002 table already exists` | **连到旧库了**。停服务端，`.\run.ps1 create data\demo` 重来（见下条） |
| 第 ② 步报 `SE-0003 值个数与列数不一致` | 同上。旧库里有张**同名但列数不同**的 `users`，所以建表撞名、插入撞列数。先 `show table users;` 看清旧表长什么样 |
| 数据对不上 | 多半是**没从干净库开始**。停服务端，重新 `.\run.ps1 create data\demo` |
| 想删表/删行却报 `SY-0001 ... got delete` | `DROP TABLE` / `DELETE` / `UPDATE` **本项目都没实现**（附录 D 已知限制），连关键字都不是。清数据只能删库目录重来 |
| 明明 `create` 了新库，数据却还是旧的 | 有个**忘记关的旧服务端**占着 9999，新服务端起不来（`BindException`），客户端静默连到了旧服务端。看终端 A 的 `[OPEN] database dir = ...` 确认目录 |
| 客户端报连接错 | 服务端挂了或者端口不是 9999。看终端 A 还在不在 |
| `show tables` 行序每次不一样 | 正常，元数据在 `ConcurrentHashMap` 里。只核行数和表名 |
| 第 9 部分 C 终端卡死不动 | **是预期行为**，回 B 终端 `commit;` 就解开 |
